Slide 1

\documentclass{article}
\usepackage{amsmath}

\begin{document}

\section*{Duality (General Form)}

\begin{itemize}
    \item \textbf{Primal Problem:}
    \begin{align*}
        \text{maximize } \zeta &= f_0(x) \\
        x \in \mathbb{R}^d \\
        \text{subject to } f_i(x) &\le 0 \quad i=1,\dots,m \\
        g_j(x) &= 0 \quad j=1,\dots,n
    \end{align*}
    \begin{itemize}
        \item $\zeta$ is the objective function.
        \item $f_0(x)$ is the objective function of the primal problem.
        \item $f_i(x)$ are inequality constraints.
        \item $g_j(x)$ are equality constraints.
        \item $x \in \mathbb{R}^d$ indicates that the variables $x$ are real-valued.
        \item $i=1,\dots,m$ and $j=1,\dots,n$ denote the number of inequality and equality constraints, respectively.
    \end{itemize}
    \item \textbf{Dual Problem:}
    \begin{align*}
        \text{maximize } \zeta &= f_0(x) \\
        x \in \mathbb{R}^d \\
        \text{subject to } g_j(x) &= 0 \quad j=1,\dots,n
    \end{align*}
    \begin{itemize}
        \item $\zeta$ is the objective function.
        \item $f_0(x)$ is the objective function of the dual problem.
        \item $g_j(x)$ are equality constraints.
        \item $x \in \mathbb{R}^d$ indicates that the variables $x$ are real-valued.
        \item $j=1,\dots,n$ denotes the number of equality constraints.
    \end{itemize}
\end{itemize}

\end{document}

\vspace{1cm}

\textbf{Description:}

This slide introduces the general form of the primal and dual linear programming problems.  It shows the objective function and constraints for both problems.  The notation uses $f_0(x)$ for the objective function of the primal problem and $g_j(x)$ for the equality constraints.  The slide also includes the notation $x \in \mathbb{R}^d$, indicating that the variables are real-valued.  The use of $i=1,\dots,m$ and $j=1,\dots,n$ indicates the number of inequality and equality constraints, respectively.  The red arrows and the handwritten 6 are likely to indicate the variables associated with the constraints.  This is the first slide of a 22-slide presentation on duality in linear programming.


Slide 2

\documentclass{article}
\usepackage{amsmath}

\begin{document}

\section*{Equality Constraints}

\begin{itemize}
    \item \textbf{Problem:}
    \begin{align*}
        \text{maximize } \zeta &= f_0(x) \\
        x \in \mathbb{R}^d \\
        \text{subject to } g_j(x) &= 0 \quad j=1,\dots,n
    \end{align*}
    \item \textbf{Lagrange Multiplier( $\mu$ ) \& Lagrangian (L)}
    \begin{align*}
        L(x,\mu) &= f_0(x) - \sum_{j=1}^n \mu_j g_j(x)
    \end{align*}
    \begin{itemize}
        \item $x_i, \mu_1, \dots, \mu_m$ are variables.
        \item $j=1,\dots,n$ denotes the number of equality constraints.
    \end{itemize}
\end{itemize}

\end{document}

\vspace{1cm}

\textbf{Description:}

This slide builds upon the previous slide by focusing specifically on equality constraints in a linear programming problem.  It introduces the concept of Lagrange multipliers ($\mu$) and the Lagrangian function (L). The Lagrangian function is a way to incorporate equality constraints into the optimization problem.  The equation $L(x,\mu) = f_0(x) - \sum_{j=1}^n \mu_j g_j(x)$ defines the Lagrangian, where $f_0(x)$ is the objective function, $\mu_j$ are the Lagrange multipliers, and $g_j(x)$ are the equality constraints.  The variables $x_i, \mu_1, \dots, \mu_m$ are explicitly mentioned, highlighting the variables involved in the Lagrangian formulation.  The notation $j=1,\dots,n$ again indicates the number of equality constraints.  The slide is likely part of a larger presentation on duality and optimization techniques in linear programming.


Slide 3

\documentclass{article}
\usepackage{amsmath}

\begin{document}

\section*{Equality Constraints}

\begin{itemize}
    \item Compare $\max f_0$ with constraints $\leftrightarrow$ $\max L$ with no constraints
    \item Let $x \in \mathcal{G}$, i.e. $g_j(x) \ge 0$
    \item $f_0(x) = f_0(\tilde{x}) - \sum_j \mu_j g_j(\tilde{x}) = 0$
    \item $\implies \max_x [f_0(x) - \sum_j \mu_j g_j(x)] = \max_x L(x,\mu) = \xi(\mu)$
\end{itemize}

\end{document}

\vspace{1cm}

\textbf{Description:}

This slide continues the discussion of equality constraints in linear programming, building on the previous slide's introduction of the Lagrangian.  It explicitly shows how to compare the maximization of the objective function ($f_0$) with constraints to the maximization of the Lagrangian function ($L$) without constraints.  The key idea is that if a point $\tilde{x}$ satisfies the equality constraints ($g_j(\tilde{x}) = 0$), then the objective function value at $\tilde{x}$ is equal to the Lagrangian value at $\tilde{x}$ and $\mu$.  The slide uses the notation $\xi(\mu)$ to represent the maximum value of the Lagrangian function with respect to $x$.  This is likely part of a larger presentation on duality and optimization techniques in linear programming, demonstrating how to formulate and solve optimization problems with equality constraints using Lagrange multipliers.


Slide 4

\documentclass{article}
\usepackage{amsmath}

\begin{document}

\section*{Equality Constraints}

\begin{itemize}
    \item Compare $\max f_0$ with constraints $\leftrightarrow$ $\max L$ with no constraints
    \item Let $x \in \mathcal{G}$, i.e. $g_j(x) \ge 0$
    \item $\xi(x) = f_0(x) \le \xi(\mu)$ for any $\tilde{x}$ satisfies constraint
    \item $\xi = \max_{x \in \mathcal{G}} \xi(x) \le \min_{\mu} \xi(\mu) = \xi^*$
\end{itemize}

\end{document}

\vspace{1cm}

\textbf{Description:}

This slide continues the discussion of equality constraints in linear programming, building on the previous slides' introduction of the Lagrangian and the comparison between optimization with and without constraints.  It introduces the crucial relationship between the maximum value of the objective function ($\xi(x)$) subject to the constraints ($\mathcal{G}$) and the minimum value of the Lagrangian function ($\xi(\mu)$) without constraints.  The inequality $\xi(x) \le \xi(\mu)$ is stated, indicating that the maximum value of the objective function subject to constraints is always less than or equal to the minimum value of the Lagrangian function without constraints.  The notation $\xi^*$ represents the minimum value of $\xi(\mu)$.  This slide is likely part of a larger presentation on duality and optimization techniques in linear programming, demonstrating the fundamental duality relationship between primal and dual problems.


Slide 5

\documentclass{article}
\usepackage{amsmath}

\begin{document}

\section*{Equality Constraints}

\begin{itemize}
    \item Compare $\max f_0$ with constraints $\leftrightarrow$ $\max L$ with no constraints
    \item Let $x \in \mathcal{G}$, i.e. $g_j(x) \ge 0$
    \item $\xi(x) = f_0(x) \le \xi(\mu)$ for any $\tilde{x}$ satisfies constraint
    \item $\xi = \max_{x \in \mathcal{G}} \xi(x) \le \min_{\mu} \xi(\mu) = \xi^*$  (Weak Duality)
\end{itemize}

\end{document}

\vspace{1cm}

\textbf{Description:}

This slide continues the discussion of equality constraints in linear programming, building on the previous slides' introduction of the Lagrangian and the comparison between optimization with and without constraints.  It formally states the **Weak Duality Theorem**.  The inequality $\xi(x) \le \xi(\mu)$ is restated, emphasizing that the maximum value of the objective function ($\xi(x)$) subject to the constraints ($\mathcal{G}$) is always less than or equal to the minimum value of the Lagrangian function ($\xi(\mu)$) without constraints.  The notation $\xi^*$ represents the minimum value of $\xi(\mu)$.  The statement "Weak Duality" is explicitly included, highlighting the fundamental property of duality in optimization.  This slide is likely part of a larger presentation on duality and optimization techniques in linear programming, demonstrating the fundamental duality relationship between primal and dual problems.


Slide 6

\documentclass{article}
\usepackage{amsmath}

\begin{document}

\section*{Equality Constraints}

\begin{itemize}
    \item \textbf{Primal Problem (P)}
    \begin{align*}
        \max \xi(x) &= f_0(x) \\
        \text{s.t.} \quad g_j(x) &= 0, \quad j=1,2,\dots,n
    \end{align*}
    \item \textbf{Dual Problem (D)}
    \begin{align*}
        \min \xi(\mu) &= \max_{x \in \mathbb{R}^n} [f_0(x) - \sum_{j=1}^n \mu_j g_j(x)]
    \end{align*}
    \item \textbf{Weak Duality}
    \begin{align*}
        \max_{x \in \mathcal{G}} \xi(x) &\le \min_{\mu} \xi(\mu)
    \end{align*}
\end{itemize}

\end{document}

\vspace{1cm}

\textbf{Description:}

This slide formally defines the primal and dual problems in the context of linear programming, focusing on equality constraints.  It explicitly shows the objective function and constraints for both problems.  The primal problem (P) is to maximize the objective function $\xi(x) = f_0(x)$ subject to the equality constraints $g_j(x) = 0$ for $j = 1, 2, \dots, n$.  The dual problem (D) is to minimize the function $\xi(\mu)$ which is defined as the maximum of the Lagrangian function over all possible values of $x$.  The key takeaway is the statement of the Weak Duality Theorem, which states that the optimal value of the primal problem ($\max_{x \in \mathcal{G}} \xi(x)$) is always less than or equal to the optimal value of the dual problem ($\min_{\mu} \xi(\mu)$).  This is a fundamental result in linear programming duality.


Slide 7

\documentclass{article}
\usepackage{amsmath}

\begin{document}

\section*{Inequality Constraints}

\begin{itemize}
    \item \textbf{Primal Problem}
    \begin{align*}
        \text{maximize } \xi &= f_0(x) \\
        x \in \mathbb{R}^d \\
        \text{subject to } f_i(x) &\le 0 \quad i=1,\dots,m \\
        g_j(x) &= 0 \quad j=1,\dots,n
    \end{align*}
    \item \textbf{Lagrangian Function}
    \begin{align*}
        L(x,\lambda,\mu) &= f_0(x) - \sum_{i=1}^m \lambda_i f_i(x) - \sum_{j=1}^n \mu_j g_j(x)
    \end{align*}
\end{itemize}

\end{document}

\vspace{1cm}

\textbf{Description:}

This slide introduces the Lagrangian function for a linear programming problem with both inequality and equality constraints.  The primal problem (shown in the first bullet point) is to maximize the objective function $f_0(x)$ subject to inequality constraints $f_i(x) \le 0$ for $i = 1, \dots, m$ and equality constraints $g_j(x) = 0$ for $j = 1, \dots, n$.  The second bullet point introduces the Lagrangian function $L(x,\lambda,\mu)$, which incorporates both types of constraints.  The Lagrangian function is defined as $L(x,\lambda,\mu) = f_0(x) - \sum_{i=1}^m \lambda_i f_i(x) - \sum_{j=1}^n \mu_j g_j(x)$, where $\lambda_i$ are the Lagrange multipliers associated with the inequality constraints and $\mu_j$ are the Lagrange multipliers associated with the equality constraints.  This slide is a crucial step in the development of the dual problem, which will be used to solve the optimization problem more efficiently.


Slide 8

Let $x \in \mathcal{G}$, i.e., constraints are satisfied.
$\overline{f_0(x)} \le f_0(\tilde{x}) - \sum_{i=1}^m \lambda_i f_i(\tilde{x}) - \sum_{j=1}^n \mu_j g_j(\tilde{x}) = L(x,\lambda,\mu)$
$\le \max_x L(x,\lambda,\mu) = \xi(\lambda,\mu)$
$\overline{f_0(x)} \le \xi(\lambda,\mu)$
(no constraint)

\textbf{Description:}

This slide continues the development of the Lagrangian function for a linear programming problem with both inequality and equality constraints.  It shows how the objective function value $\overline{f_0(x)}$ for a point $x$ satisfying the constraints is related to the maximum value of the Lagrangian function $\xi(\lambda,\mu)$ without constraints.  The key idea is that if $x$ satisfies the constraints, then the objective function value at $x$ is less than or equal to the maximum value of the Lagrangian function, which is a function of the Lagrange multipliers $\lambda$ and $\mu$.  This is a crucial step in establishing the duality relationship between the primal and dual problems.  The notation $\overline{f_0(x)}$ likely represents the optimal value of the objective function subject to the constraints.  The slide is part of a larger presentation on duality and optimization techniques in linear programming.


Slide 9

Let $x \in \mathcal{G}$, i.e., constraints are satisfied.
$\implies f_0(x) \le f_0(\tilde{x}) - \sum_{i=1}^m \lambda_i f_i(\tilde{x}) - \sum_{j=1}^n \mu_j g_j(\tilde{x}) = L(x,\lambda,\mu)$
$\le \max_x L(x,\lambda,\mu) = \xi(\lambda,\mu)$
$f_0(x) \le \xi(\lambda,\mu)$
$\lambda_i \ge 0$
$\max_{x \in \mathcal{G}} f_0(x) \le \min_{\lambda \ge 0, \mu} \xi(\lambda,\mu) = \xi^*$
(Weak Duality)

\textbf{Description:}

This slide continues the development of the Lagrangian function for a linear programming problem with both inequality and equality constraints.  It shows how the objective function value $f_0(x)$ for a point $x$ satisfying the constraints is related to the maximum value of the Lagrangian function $\xi(\lambda,\mu)$ without constraints, but with the added condition that $\lambda_i \ge 0$.  The key idea is that if $x$ satisfies the constraints, then the objective function value at $x$ is less than or equal to the maximum value of the Lagrangian function, which is a function of the Lagrange multipliers $\lambda$ and $\mu$, and the additional constraint that $\lambda_i \ge 0$ for all $i$.  This is a crucial step in establishing the duality relationship between the primal and dual problems, and the statement "Weak Duality" is explicitly included.  The notation $\xi^*$ represents the minimum value of $\xi(\lambda,\mu)$ subject to the non-negativity constraint on $\lambda$.  The slide is part of a larger presentation on duality and optimization techniques in linear programming.


Slide 10

\textbf{Primal Problem (P)}
\begin{itemize}
    \item $\max \xi(x) = f_0(x)$
    \item s.t. $f_i(x) \le 0$, $i=1,2,\dots,m$
    \item $g_j(x) = 0$, $j=1,2,\dots,n$
\end{itemize}

\textbf{Dual Problem (D)}
\begin{itemize}
    \item $\min \xi(\lambda,\mu) = \max_{x \in \mathbb{R}^n} [f_0(x) - \sum_{i=1}^m \lambda_i f_i(x) - \sum_{j=1}^n \mu_j g_j(x)]$
    \item s.t. $\lambda_i \ge 0$, $i=1,2,\dots,m$
\end{itemize}

\textbf{Weak Duality}
$\xi^* = \max_{x \in \mathcal{G}} \xi(x) \le \min_{\lambda \ge 0, \mu} \xi(\lambda,\mu) = \xi^*$

\textbf{Description:}

This slide presents the primal and dual problems in linear programming, explicitly showing the objective function and constraints for both.  The primal problem (P) is to maximize the objective function $\xi(x) = f_0(x)$ subject to inequality constraints $f_i(x) \le 0$ for $i = 1, \dots, m$ and equality constraints $g_j(x) = 0$ for $j = 1, \dots, n$.  The dual problem (D) is to minimize the function $\xi(\lambda,\mu)$, which is the maximum of the Lagrangian function over all possible values of $x$, subject to the non-negativity constraint $\lambda_i \ge 0$ for $i = 1, \dots, m$.  The slide restates the Weak Duality Theorem, emphasizing that the optimal value of the primal problem ($\xi^*$) is always less than or equal to the optimal value of the dual problem ($\xi^*$).  This is a fundamental result in linear programming duality.  The slide is likely part of a larger presentation on duality and optimization techniques in linear programming.


Slide 11

\textbf{Duality for General LP}

\begin{itemize}
    \item \textbf{Primal Problem (P)}
    \begin{align*}
        \max \xi(x) &= c^T x \\
        \text{s.t.} \quad Ax &\le b \\
        x &\ge 0
    \end{align*}
    \item \textbf{Dual Problem (D)}
    \begin{align*}
        \min \xi(\lambda) &= \lambda^T b \\
        \text{s.t.} \quad A^T \lambda &= c \\
        \lambda &\ge 0
    \end{align*}
    \item \textbf{Lagrangian Function}
    \begin{align*}
        L(x,\lambda) &= c^T x - \lambda^T (Ax - b)
    \end{align*}
\end{itemize}

\textbf{Description:}

This slide presents the primal and dual problems for a general linear program (LP).  The primal problem (P) is to maximize the objective function $c^T x$ subject to the constraints $Ax \le b$ and $x \ge 0$.  The dual problem (D) is to minimize the function $\lambda^T b$ subject to the constraints $A^T \lambda = c$ and $\lambda \ge 0$.  The Lagrangian function $L(x,\lambda)$ is also shown, which is a key component in the duality relationship.  This slide is a crucial step in the development of the duality theory for linear programming, showing the standard forms of the primal and dual problems.  The notation $c^T x$ represents the dot product of the vectors $c$ and $x$.  The notation $A^T$ represents the transpose of the matrix $A$.  The slide is likely part of a larger presentation on duality and optimization techniques in linear programming.


Slide 12

\textbf{Duality for General LP}

\begin{itemize}
    \item \textbf{Primal Problem (P)}
    \begin{align*}
        \max \xi(x) &= c^T x \\
        \text{s.t.} \quad Ax &\le b \\
        x &\ge 0
    \end{align*}
    \item \textbf{Dual Problem (D)}
    \begin{align*}
        \min \xi(\lambda) &= \lambda^T b \\
        \text{s.t.} \quad A^T \lambda &\ge c \\
        \lambda &\ge 0
    \end{align*}
\end{itemize}

\textbf{Description:}

This slide presents the primal and dual problems for a general linear program (LP), but now explicitly showing the standard form of the dual problem. The primal problem (P) is to maximize the objective function $c^T x$ subject to the constraints $Ax \le b$ and $x \ge 0$.  The dual problem (D) is to minimize the function $\lambda^T b$ subject to the constraints $A^T \lambda \ge c$ and $\lambda \ge 0$.  This is a crucial step in the development of the duality theory for linear programming, showing the standard forms of the primal and dual problems.  The notation $c^T x$ represents the dot product of the vectors $c$ and $x$.  The notation $A^T$ represents the transpose of the matrix $A$.  The slide is likely part of a larger presentation on duality and optimization techniques in linear programming.  The key difference from slide 11 is the inequality constraint in the dual problem.


Slide 13

\textbf{Duality for General LP}

\begin{itemize}
    \item \textbf{Primal Problem (P)}
    \begin{align*}
        \max \xi(x) &= c^T x \\
        \text{s.t.} \quad Ax &\le b \\
        x &\ge 0
    \end{align*}
    \item \textbf{Dual Problem (D)}
    \begin{align*}
        \min \xi(\lambda) &= \lambda^T b \\
        \text{s.t.} \quad A^T \lambda &\ge c \\
        \lambda &\ge 0
    \end{align*}
\end{itemize}

\textbf{Description:}

This slide presents the primal and dual problems for a general linear program (LP), but now explicitly showing the standard form of the dual problem. The primal problem (P) is to maximize the objective function $c^T x$ subject to the constraints $Ax \le b$ and $x \ge 0$.  The dual problem (D) is to minimize the function $\lambda^T b$ subject to the constraints $A^T \lambda \ge c$ and $\lambda \ge 0$.  This is a crucial step in the development of the duality theory for linear programming, showing the standard forms of the primal and dual problems.  The notation $c^T x$ represents the dot product of the vectors $c$ and $x$.  The notation $A^T$ represents the transpose of the matrix $A$.  The slide is likely part of a larger presentation on duality and optimization techniques in linear programming.  The key difference from slide 11 is the inequality constraint in the dual problem.  The slide is likely to be followed by a discussion of the relationship between the primal and dual problems.


Slide 14

\textbf{Duality for General LP}

\begin{itemize}
    \item \textbf{Primal Problem (P)}
    \begin{align*}
        \max \xi(x) &= c^T x \\
        \text{s.t.} \quad Ax &\le b \\
        x &\ge 0
    \end{align*}
    \item \textbf{Dual Problem (D)}
    \begin{align*}
        \min \xi(\lambda) &= \lambda^T b \\
        \text{s.t.} \quad A^T \lambda &\ge c \\
        \lambda &\ge 0
    \end{align*}
    \item \textbf{Lagrangian Function}
    \begin{align*}
        L(x,\lambda) &= c^T x - \lambda^T (Ax - b) = c^T x - \lambda^T Ax + \lambda^T b
    \end{align*}
\end{itemize}

\textbf{Description:}

This slide presents the primal and dual problems for a general linear program (LP), along with the Lagrangian function. The primal problem (P) is to maximize the objective function $c^T x$ subject to the constraints $Ax \le b$ and $x \ge 0$.  The dual problem (D) is to minimize the function $\lambda^T b$ subject to the constraints $A^T \lambda \ge c$ and $\lambda \ge 0$.  The Lagrangian function $L(x,\lambda)$ is also shown, which is a key component in the duality relationship.  The slide explicitly shows the expanded form of the Lagrangian function, which is crucial for understanding the relationship between the primal and dual problems.  The notation $c^T x$ represents the dot product of the vectors $c$ and $x$.  The notation $A^T$ represents the transpose of the matrix $A$.  The slide is likely part of a larger presentation on duality and optimization techniques in linear programming.  The slide is likely to be followed by a discussion of the relationship between the primal and dual problems, and how the Lagrangian function is used to establish the duality relationship.




Slide 15

\textbf{Duality for General LP}

\begin{itemize}
    \item \textbf{Primal Problem (P)}
    \begin{align*}
        \max \xi(x) &= c^T x \\
        \text{s.t.} \quad Ax &\le b \\
        x &\ge 0
    \end{align*}
    \item \textbf{Dual Problem (D)}
    \begin{align*}
        \min \xi(\lambda) &= \lambda^T b \\
        \text{s.t.} \quad A^T \lambda &\ge c \\
        \lambda &\ge 0
    \end{align*}
    \item \textbf{Lagrangian Function}
    \begin{align*}
        L(x,\lambda) &= c^T x - \lambda^T (Ax - b) = c^T x - \lambda^T Ax + \lambda^T b
    \end{align*}
    \item $\max_x L(x,\lambda) = c^T x - \lambda^T Ax + \lambda^T b$
    \item $\max_x L(x,\lambda) = \begin{cases}
        \infty & \text{if } c^T x - \lambda^T Ax + \lambda^T b \text{ is unbounded} \\
        \lambda^T b & \text{if } c^T x - \lambda^T Ax + \lambda^T b \text{ is bounded}
    \end{cases}$
\end{itemize}

\textbf{Description:}

This slide continues the discussion of duality in linear programming, focusing on the Lagrangian function and its relationship to the primal and dual problems.  It restates the primal and dual problems in standard form.  The Lagrangian function $L(x,\lambda)$ is shown, and the slide then analyzes the maximum value of the Lagrangian function with respect to $x$.  The key point is that the maximum value of $L(x,\lambda)$ is either unbounded or equal to $\lambda^T b$ if the expression is bounded.  This analysis is crucial for understanding the conditions under which the dual problem can be solved.  The slide is likely part of a larger presentation on duality and optimization techniques in linear programming, preparing the audience for the next steps in the derivation of the strong duality theorem.




Slide 16

\textbf{Duality for General LP}

\begin{itemize}
    \item \textbf{Primal Problem (P)}
    \begin{align*}
        \max \xi(x) &= c^T x \\
        \text{s.t.} \quad Ax &\le b \\
        x &\ge 0
    \end{align*}
    \item \textbf{Dual Problem (D)}
    \begin{align*}
        \min \xi(\lambda) &= \lambda^T b \\
        \text{s.t.} \quad A^T \lambda &\ge c \\
        \lambda &\ge 0
    \end{align*}
    \item \textbf{Lagrangian Function}
    \begin{align*}
        L(x,\lambda) &= c^T x - \lambda^T (Ax - b) = c^T x - \lambda^T Ax + \lambda^T b
    \end{align*}
    \item $\xi(\lambda,\mu) = \begin{cases}
        \lambda^T b & \text{if } A^T \lambda = c \\
        \infty & \text{otherwise}
    \end{cases}$
\end{itemize}

\textbf{Description:}

This slide continues the discussion of duality in linear programming, focusing on the Lagrangian function and its relationship to the primal and dual problems.  It restates the primal and dual problems in standard form.  The Lagrangian function $L(x,\lambda)$ is shown, and the slide then analyzes the maximum value of the Lagrangian function with respect to $x$.  The key point is that the maximum value of $L(x,\lambda)$ is either unbounded or equal to $\lambda^T b$ if $A^T \lambda = c$.  This analysis is crucial for understanding the conditions under which the dual problem can be solved.  The slide is likely part of a larger presentation on duality and optimization techniques in linear programming, preparing the audience for the next steps in the derivation of the strong duality theorem.  The slide is likely to be followed by a discussion of the conditions under which the maximum of the Lagrangian is finite.


Slide 17

\textbf{Duality for General LP}

\begin{itemize}
    \item \textbf{Primal Problem (P)}
    \begin{align*}
        \max \xi(x) &= c^T x \\
        \text{s.t.} \quad Ax &\le b \\
        x &\ge 0
    \end{align*}
    \item \textbf{Dual Problem (D)}
    \begin{align*}
        \min \xi(\lambda) &= \lambda^T b \\
        \text{s.t.} \quad A^T \lambda &\ge c \\
        \lambda &\ge 0
    \end{align*}
    \item \textbf{Lagrangian Function}
    \begin{align*}
        L(x,\lambda) &= c^T x - \lambda^T (Ax - b) = c^T x - \lambda^T Ax + \lambda^T b
    \end{align*}
    \item $\xi(\lambda,\mu) = \begin{cases}
        \lambda^T b & \text{if } A^T \lambda = c \\
        \infty & \text{otherwise}
    \end{cases}$
    \item \textbf{Strong Duality}
    \begin{itemize}
        \item If $x^*$ is optimal for (P) and $\lambda^*$ is optimal for (D), then $\xi(x^*) = \xi(\lambda^*)$.
    \end{itemize}
\end{itemize}

\textbf{Description:}

This slide continues the discussion of duality in linear programming, focusing on the conditions for strong duality.  It restates the primal and dual problems in standard form.  The Lagrangian function $L(x,\lambda)$ is shown, and the slide then analyzes the maximum value of the Lagrangian function with respect to $x$.  The key point is that the maximum value of $L(x,\lambda)$ is either unbounded or equal to $\lambda^T b$ if $A^T \lambda = c$.  The slide introduces the concept of strong duality, stating that if an optimal solution $x^*$ exists for the primal problem (P) and an optimal solution $\lambda^*$ exists for the dual problem (D), then the optimal values of the primal and dual problems are equal ($\xi(x^*) = \xi(\lambda^*)$).  This is a crucial result in linear programming duality, as it provides a way to find the optimal solution to one problem by solving the other.  The slide is likely part of a larger presentation on duality and optimization techniques in linear programming, preparing the audience for the next steps in the derivation of the strong duality theorem.




Slide 18

\textbf{Duality for General LP}

\begin{itemize}
    \item \textbf{Primal Problem (P)}
    \begin{align*}
        \max \xi(x) &= c^T x \\
        \text{s.t.} \quad Ax &\le b \\
        x &\ge 0
    \end{align*}
    \item \textbf{Dual Problem (D)}
    \begin{align*}
        \min \xi(\lambda) &= \lambda^T b \\
        \text{s.t.} \quad A^T \lambda &\ge c \\
        \lambda &\ge 0
    \end{align*}
    \item \textbf{Weak Duality}
    \begin{itemize}
        \item If $x^*$ is feasible for (P) and $\lambda^*$ is feasible for (D), then $\xi(x^*) \le \xi(\lambda^*)$.
    \end{itemize}
\end{itemize}

\textbf{Description:}

This slide presents the primal and dual problems for a general linear program (LP), along with the statement of weak duality. The primal problem (P) is to maximize the objective function $c^T x$ subject to the constraints $Ax \le b$ and $x \ge 0$.  The dual problem (D) is to minimize the function $\lambda^T b$ subject to the constraints $A^T \lambda \ge c$ and $\lambda \ge 0$.  The slide emphasizes the weak duality property, stating that if a feasible solution $x^*$ exists for the primal problem (P) and a feasible solution $\lambda^*$ exists for the dual problem (D), then the objective function value for the primal problem is always less than or equal to the objective function value for the dual problem.  This is a fundamental result in linear programming duality, providing a lower bound on the optimal value of the primal problem.  The slide is likely part of a larger presentation on duality and optimization techniques in linear programming, preparing the audience for the next steps in the derivation of the strong duality theorem.




Slide 19

\textbf{Duality for "Traditional" LP}

\begin{itemize}
    \item \textbf{Primal Problem (P)}
    \begin{align*}
        \max \xi(x) &= c^T x \\
        \text{s.t.} \quad Ax &\le b \\
        x &\ge 0
    \end{align*}
    \item \textbf{Dual Problem (D)}
    \begin{align*}
        \min \xi(\lambda) &= \lambda^T b \\
        \text{s.t.} \quad A^T \lambda &\ge c \\
        \lambda &\ge 0
    \end{align*}
    \item \textbf{Lagrangian Function}
    \begin{align*}
        L(x,\lambda) &= c^T x - \lambda^T (Ax - b) = c^T x - \lambda^T Ax + \lambda^T b
    \end{align*}
\end{itemize}

\textbf{Description:}

This slide presents the primal and dual problems for a "traditional" linear program (LP), focusing on the specific form often encountered in applications.  The primal problem (P) is to maximize the objective function $c^T x$ subject to the constraints $Ax \le b$ and $x \ge 0$.  The dual problem (D) is to minimize the function $\lambda^T b$ subject to the constraints $A^T \lambda \ge c$ and $\lambda \ge 0$.  The Lagrangian function $L(x,\lambda)$ is also shown, which is a key component in the duality relationship.  The slide is likely part of a larger presentation on duality and optimization techniques in linear programming, specifically focusing on the standard form of the primal and dual problems for a traditional LP.  The notation $c^T x$ represents the dot product of the vectors $c$ and $x$.  The notation $A^T$ represents the transpose of the matrix $A$.  The slide is likely to be followed by a discussion of the relationship between the primal and dual problems, and how the Lagrangian function is used to establish the duality relationship.




Slide 20

\textbf{Duality for "Traditional" LP}

\begin{itemize}
    \item \textbf{Primal Problem (P)}
    \begin{align*}
        \max \xi(x) &= c^T x \\
        \text{s.t.} \quad Ax &\le b \\
        x &\ge 0
    \end{align*}
    \item \textbf{Dual Problem (D)}
    \begin{align*}
        \min \xi(\lambda) &= \lambda^T b \\
        \text{s.t.} \quad A^T \lambda &\ge c \\
        \lambda &\ge 0
    \end{align*}
    \item \textbf{Lagrangian Function}
    \begin{align*}
        L(x,\lambda) &= c^T x - \lambda^T (Ax - b) = c^T x - \lambda^T Ax + \lambda^T b
    \end{align*}
    \item $\max_x L(x,\lambda) = \begin{cases}
        \lambda^T b & \text{if } c^T - \lambda^T A = 0 \\
        \infty & \text{otherwise}
    \end{cases}$
\end{itemize}

\textbf{Description:}

This slide continues the discussion of duality in linear programming, focusing on the Lagrangian function and its relationship to the primal and dual problems, specifically for a "traditional" linear program (LP).  It restates the primal and dual problems in standard form.  The Lagrangian function $L(x,\lambda)$ is shown, and the slide then analyzes the maximum value of the Lagrangian function with respect to $x$.  The key point is that the maximum value of $L(x,\lambda)$ is either unbounded or equal to $\lambda^T b$ if $c^T - \lambda^T A = 0$.  This analysis is crucial for understanding the conditions under which the dual problem can be solved.  The slide is likely part of a larger presentation on duality and optimization techniques in linear programming, preparing the audience for the next steps in the derivation of the strong duality theorem.  The slide is likely to be followed by a discussion of the conditions under which the maximum of the Lagrangian is finite.




Slide 21

\textbf{Strong Duality for "Traditional" LP}

\begin{itemize}
    \item \textbf{Primal Problem (P)}
    \begin{align*}
        \max \xi(x) &= c^T x \\
        \text{s.t.} \quad Ax &\le b \\
        x &\ge 0
    \end{align*}
    \item \textbf{Dual Problem (D)}
    \begin{align*}
        \min \xi(\lambda) &= \lambda^T b \\
        \text{s.t.} \quad A^T \lambda &\ge c \\
        \lambda &\ge 0
    \end{align*}
    \item \textbf{Strong Duality Theorem}
    \begin{itemize}
        \item If the primal problem (P) has a finite optimal solution, then the dual problem (D) also has a finite optimal solution, and the optimal values of the primal and dual problems are equal.
    \end{itemize}
\end{itemize}

\textbf{Description:}

This slide presents the strong duality theorem for linear programming, specifically for the "traditional" form of the primal and dual problems.  It restates the primal and dual problems in standard form.  The slide emphasizes the strong duality property, stating that if the primal problem has a finite optimal solution, then the dual problem also has a finite optimal solution, and the optimal values of the primal and dual problems are equal.  This is a crucial result in linear programming duality, as it provides a way to find the optimal solution to one problem by solving the other.  The slide is likely part of a larger presentation on duality and optimization techniques in linear programming, summarizing the key conditions for strong duality.  The slide is likely to be followed by a discussion of the conditions under which the maximum of the Lagrangian is finite.




Slide 22

\textbf{Summary of Duality}

\begin{itemize}
    \item \textbf{Primal Problem (P)}
    \begin{align*}
        \max \xi(x) &= c^T x \\
        \text{s.t.} \quad Ax &\le b \\
        x &\ge 0
    \end{align*}
    \item \textbf{Dual Problem (D)}
    \begin{align*}
        \min \xi(\lambda) &= \lambda^T b \\
        \text{s.t.} \quad A^T \lambda &\ge c \\
        \lambda &\ge 0
    \end{align*}
    \item \textbf{Strong Duality Theorem}
    \begin{itemize}
        \item If the primal problem (P) has a finite optimal solution, then the dual problem (D) also has a finite optimal solution, and the optimal values of the primal and dual problems are equal.
    \end{itemize}
    \item \textbf{Complementary Slackness}
    \begin{itemize}
        \item If $x^*$ is optimal for (P) and $\lambda^*$ is optimal for (D), then $x^*_i \lambda^*_i = 0$ for all $i$.
    \end{itemize}
\end{itemize}

\textbf{Description:}

This slide summarizes the key concepts of duality in linear programming, specifically for the "traditional" form of the primal and dual problems.  It restates the primal and dual problems in standard form.  The slide emphasizes the strong duality property, stating that if the primal problem has a finite optimal solution, then the dual problem also has a finite optimal solution, and the optimal values of the primal and dual problems are equal.  Crucially, it introduces the concept of complementary slackness, which states that if $x^*$ is optimal for the primal problem and $\lambda^*$ is optimal for the dual problem, then the product of the $i$-th primal variable and the $i$-th dual variable must be zero for all $i$.  This complementary slackness condition provides a powerful tool for finding optimal solutions to both the primal and dual problems.  The slide is likely part of a larger presentation on duality and optimization techniques in linear programming, summarizing the key conditions for strong duality and complementary slackness.


