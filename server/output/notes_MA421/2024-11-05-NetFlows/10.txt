Slide 10

Path = $(n_1, n_2, ..., n_k)$
for all $l$, $(n_l, n_{l+1}) \in \mathcal{A}$ on $(n_k, n_1) \in \mathcal{A}$
$n_k = n_1$

Cycle = $(n_1, n_2, ..., n_k)$
$n_k = n_1$

Tree: connected, acyclic
Spanning tree $\mathcal{T} = (N, \mathcal{A})$, tree and $N = \tilde{N}$


\textbf{Description:}

This slide continues the Network Flow discussion, likely from the previous slides. It defines key concepts related to graphs and trees, which are relevant to network flow problems.

* **Path:** A sequence of nodes $(n_1, n_2, ..., n_k)$ where each consecutive pair of nodes is connected by an arc in the network.  The condition $(n_l, n_{l+1}) \in \mathcal{A}$ ensures that the sequence forms a valid path.  The condition $n_k = n_1$ defines a cycle.
* **Cycle:** A path that starts and ends at the same node.  This is a closed path in the network.
* **Tree:** A connected graph that contains no cycles.  This is a fundamental structure in graph theory.
* **Spanning Tree:** A tree that includes all the nodes of the network.  The notation $\mathcal{T} = (N, \mathcal{A})$ indicates that the spanning tree $\mathcal{T}$ is defined by the set of nodes $N$ and the set of arcs $\mathcal{A}$ in the network.  The condition $N = \tilde{N}$ likely means that the spanning tree includes all the nodes in the original network.

The slide likely connects these graph-theoretic concepts to the linear programming formulation of network flow problems.  The notation suggests that the lecture is covering how to formulate and solve network flow problems as linear programming problems.  The context implies that the concepts of paths, cycles, and trees are crucial for understanding and solving network flow problems.


