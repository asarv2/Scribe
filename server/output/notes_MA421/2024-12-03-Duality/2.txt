Slide 2

\textbf{Complementary Slackness (at optimal)}

\vspace{0.5cm}

$c^T x = y^T A x = y^T b$

\vspace{0.5cm}

$0 = (y^T A - c^T) x$
\vspace{0.5cm}

$0 = y^T (Ax - b)$

\vspace{0.5cm}

$x_j z_j = 0$
\vspace{0.5cm}

$y_i w_i = 0$

\vspace{0.5cm}

\textbf{Description:}

This slide continues the discussion of duality in linear programming, focusing on the concept of complementary slackness.  It shows the conditions that must hold at the optimal solution for both the primal and dual problems.  The equations highlight that at optimality, the product of the primal variable $x_j$ and the corresponding dual slack variable $z_j$ must equal zero, and similarly, the product of the dual variable $y_i$ and the corresponding primal slack variable $w_i$ must equal zero.  This is a crucial property for determining the optimal solutions to both the primal and dual problems.  The notation uses the same variables as the previous slide, with $z_j$ representing the slack variable for the $j^{th}$ constraint in the primal problem and $w_i$ representing the slack variable for the $i^{th}$ constraint in the dual problem.  The slide emphasizes that these conditions are only valid at the optimal solution.  This is likely part of a linear programming lecture, where the presenter is explaining how to find optimal solutions using duality and complementary slackness.
