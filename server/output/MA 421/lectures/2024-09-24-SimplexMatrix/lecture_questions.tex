\documentclass{article}%
\usepackage[T1]{fontenc}%
\usepackage[utf8]{inputenc}%
\usepackage{lmodern}%
\usepackage{textcomp}%
\usepackage{lastpage}%
\usepackage{geometry}%
\geometry{margin=1in,headheight=14pt,headsep=25pt}%
\usepackage{hyperref}%
\usepackage{enumitem}%
\usepackage{fancyhdr}%
\usepackage{xcolor}%
\usepackage{url}%
\usepackage{breakurl}%
%

            \hypersetup{
                colorlinks=true,
                linkcolor=blue,
                filecolor=magenta,
                urlcolor=blue
            }
            \pagestyle{fancy}
            \fancyhf{}
            \rhead{Generated on \today}
            \cfoot{\thepage}
            
            % Configure enumeration settings
            \setlist[enumerate,1]{label=\arabic*.}
            \setlist[enumerate,2]{label=\alph*.}
            \setlist[enumerate,3]{label=\Alph*.}
            \setlist[enumerate]{itemsep=0.5em}
            
            % Define a command for red text
            \newcommand{\incorrect}[1]{\textcolor{red}{#1}}
        %
\lhead{2024{-}09{-}24{-}SimplexMatrix}%
\title{Practice Questions for 2024{-}09{-}24{-}SimplexMatrix}%
\author{Generated by Scribe.AI}%
\date{\today}%
%
\begin{document}%
\normalsize%
\maketitle%
\section{Questions}%
\label{sec:Questions}%
\begin{enumerate}%
\item Consider the linear program in Slide 1, expressed in matrix notation.  If we introduce slack variables to convert the inequality constraints into equalities, what is the dimension of the augmented constraint matrix $\begin{bmatrix} A & I \end{bmatrix}$, where $A$ is the original constraint matrix and $I$ is the identity matrix?%
\begin{enumerate}%
\item $m \times n$%
\item $m \times (m+n)$%
\item $n \times (m+n)$%
\item $(m+n) \times n$%
\item $n \times m$%
\end{enumerate}%
\vspace{1em}%
\item Referring to Slide 4, explain the significance of partitioning the variable vector $X$ into basic ($X_B$) and non-basic ($X_N$) variables in the context of the simplex method. How does this partitioning simplify the representation of the objective function and constraints?%
\begin{enumerate}%
\item It allows for easier calculation of the inverse of the constraint matrix.%
\item It simplifies the calculation of the dual problem.%
\item It allows for iterative solution by setting non-basic variables to zero and solving for basic variables.%
\item It simplifies the graphical representation of the feasible region.%
\item It is not significant; it is merely a notational convenience.%
\end{enumerate}%
\vspace{1em}%
\item In Slide 8, the objective function is rewritten as $J = \bar{J} + \bar{C}^T X_N$. What is the significance of the vector $\bar{C}$ (often called the reduced cost vector) in determining whether the current solution is optimal?%
\begin{enumerate}%
\item If any element of $\bar{C}$ is positive, the solution is optimal.%
\item If any element of $\bar{C}$ is zero, the solution is optimal.%
\item If all elements of $\bar{C}$ are non-negative, the solution is optimal.%
\item If all elements of $\bar{C}$ are negative, the solution is optimal.%
\item The vector $\bar{C}$ is irrelevant to optimality; only the value of $\bar{J}$ matters.%
\end{enumerate}%
\vspace{1em}%
\item Slides 10 and 11 present two tables representing the primal and dual simplex methods in matrix form.  Explain the relationship between the two tables, focusing on the connection between the primal and dual objective function values and the basic variable values.%
\begin{enumerate}%
\item The tables are unrelated; they represent different algorithms.%
\item The dual table is the negative transpose of the primal table.%
\item The dual table is the inverse of the primal table.%
\item The dual table is obtained by swapping rows and columns of the primal table.%
\item The dual table is identical to the primal table.%
\end{enumerate}%
\vspace{1em}%
\item%
\begin{enumerate}%
\item Consider the linear program presented in Slide 1: Maximize $ \zeta = 3x_1 + 2x_2 $ subject to $ -x_1 + 3x_2 \le 12 $, $ x_1 + x_2 \le 8 $, $ 2x_1 - x_2 \le 10 $, and $ x_1, x_2 \ge 0 $. After introducing slack variables $w_1, w_2, w_3$ (Slide 1), and expressing the problem in matrix form (Slide 7), explain how the initial basic feasible solution is determined, and what its corresponding value of the objective function $\zeta$ is.%
\begin{enumerate}%
\item The initial basic feasible solution is $x_1 = 0, x_2 = 0, w_1 = 12, w_2 = 8, w_3 = 10$, and $\zeta = 0$.%
\item The initial basic feasible solution is $x_1 = 12, x_2 = 8, w_1 = 0, w_2 = 0, w_3 = 0$, and $\zeta = 52$.%
\item The initial basic feasible solution cannot be determined without performing at least one simplex iteration.%
\item The initial basic feasible solution is $x_1 = 5, x_2 = 0, w_1 = 17, w_2 = 3, w_3 = 0$, and $\zeta = 15$.%
\item The initial basic feasible solution is $x_1 = 0, x_2 = 4, w_1 = 0, w_2 = 4, w_3 = 6$, and $\zeta = 8$.%
\end{enumerate}%
\vspace{0.5em}%
\item Referring to Slide 8, explain the significance of the reduced cost vector $\bar{C}$ in determining whether the current solution is optimal.  How does the interpretation of $\bar{C}$ relate to the concept of the reduced gradient in unconstrained optimization?%
\begin{enumerate}%
\item If all elements of $\bar{C}$ are positive, the current solution is optimal; otherwise, it is not.%
\item If all elements of $\bar{C}$ are zero, the current solution is optimal; otherwise, it is not.%
\item If all elements of $\bar{C}$ are negative, the current solution is optimal; otherwise, it is not.%
\item The reduced cost vector $\bar{C}$ is irrelevant in determining optimality; only the objective function value matters.%
\item If any element of $\bar{C}$ is positive, the current solution is optimal; otherwise, it is not.%
\end{enumerate}%
\vspace{0.5em}%
\item Slides 10 and 11 present two tables representing the primal and dual simplex methods in matrix form. Explain the relationship between the two tables, focusing on the connection between the primal and dual objective function values and the basic variable values.  How does complementary slackness manifest in these tables?%
\begin{enumerate}%
\item The tables are identical, except for a change of sign in the objective function value.%
\item The tables are transposes of each other, with a change of sign in the objective function value.%
\item The tables are unrelated; they represent different algorithms for solving linear programs.%
\item One table represents the primal simplex method, and the other represents the dual simplex method. The optimal objective function values are equal, and complementary slackness is reflected in the relationship between basic and non-basic variables in both tables.%
\item The tables are identical, except for a change of sign in the basic variable values.%
\end{enumerate}%
\vspace{0.5em}%
\end{enumerate}%
\item Consider the linear program in Slide 1, expressed in matrix notation. After introducing slack variables, the constraints become $Ax + I_m w = b$, where $x \in \mathbb{R}^n$ are the decision variables, $w \in \mathbb{R}^m$ are the slack variables, $A \in \mathbb{R}^{m \times n}$ is the constraint matrix, $I_m$ is the $m \times m$ identity matrix, and $b \in \mathbb{R}^m$ is the right-hand side vector.  If we partition the variable vector as $X = \begin{bmatrix} x \\ w \end{bmatrix}$, what is the dimension of the augmented constraint matrix $\begin{bmatrix} A & I_m \end{bmatrix}$?%
\begin{enumerate}%
\item $m \times n$%
\item $n \times m$%
\item $m \times (m+n)$%
\item $(m+n) \times m$%
\item $n \times (m+n)$%
\end{enumerate}%
\vspace{1em}%
\item Referring to Slide 4, the objective function is expressed as $c^T X = c_B^T X_B + c_N^T X_N$, where $X_B$ and $X_N$ are vectors of basic and non-basic variables, respectively, and $c_B$ and $c_N$ are the corresponding cost vectors.  In the initial basic feasible solution, all non-basic variables are set to zero ($X_N = 0$).  Given that the objective function after simplex iterations is rewritten in Slide 8 as $J = \bar{J} + \bar{C}^T X_N$, where $\bar{J}$ is a constant and $\bar{C}$ is the reduced cost vector, what is the significance of the reduced cost vector $\bar{C}$ in determining the optimality of the current solution?%
\begin{enumerate}%
\item If any element of $\bar{C}$ is positive, the solution is optimal.%
\item If all elements of $\bar{C}$ are zero, the solution is optimal.%
\item If any element of $\bar{C}$ is negative, the solution is optimal.%
\item If all elements of $\bar{C}$ are positive, the solution is optimal.%
\item If all elements of $\bar{C}$ are negative, the solution is optimal.%
\end{enumerate}%
\vspace{1em}%
\item In Slide 10, two tables are presented, representing the primal and dual simplex methods in matrix form. The primal table shows the objective function value $c_B^T B^{-1} b$ and the basic variable values $B^{-1} b$, while the dual table shows $-c_B^T B^{-1} b$ and $-(B^{-1}b)^T$.  Explain the relationship between the primal and dual objective function values in these tables, and how this relates to the concept of duality in linear programming.%
\begin{enumerate}%
\item The primal and dual objective function values are always equal.%
\item The primal objective function value is the negative of the dual objective function value.%
\item The primal objective function value is the transpose of the dual objective function value.%
\item There is no direct relationship between the primal and dual objective function values.%
\item The primal objective function value is always greater than or equal to the dual objective function value.%
\end{enumerate}%
\vspace{1em}%
\item Consider Slide 7, where the constraints are expressed in matrix form as $\begin{bmatrix} B & N \end{bmatrix} \begin{bmatrix} X_B \\ X_N \end{bmatrix} = b$, where $B$ and $N$ are matrices formed from the columns of the augmented matrix $\begin{bmatrix} A & I_m \end{bmatrix}$ corresponding to basic and non-basic variables, respectively.  Explain how the initial basic feasible solution is determined in the simplex method, and how this relates to the matrices $B$ and $N$.%
\begin{enumerate}%
\item The initial basic feasible solution is obtained by setting $X_B = B^{-1}b$ and $X_N = 0$.%
\item The initial basic feasible solution is obtained by setting $X_B = 0$ and $X_N = N^{-1}b$.%
\item The initial basic feasible solution is obtained by setting $X_B = b$ and $X_N = 0$.%
\item The initial basic feasible solution is obtained by setting $X_B = 0$ and $X_N = b$.%
\item The initial basic feasible solution cannot be determined from the given information.%
\end{enumerate}%
\vspace{1em}%
\item%
\begin{enumerate}%
\item Consider the linear program presented in Slide 1: Maximize $ \zeta = 3x_1 + 2x_2 $ subject to $ -x_1 + 3x_2 \le 12 $, $ x_1 + x_2 \le 8 $, $ 2x_1 - x_2 \le 10 $, and $ x_1, x_2 \ge 0 $. After introducing slack variables $ w_1, w_2, w_3 $ (Slide 1) and expressing the problem in matrix form (Slide 7), what is the dimension of the augmented constraint matrix $ \begin{bmatrix} A & I \end{bmatrix} $, where $ A $ is the original constraint matrix and $ I $ is the identity matrix?%
\begin{enumerate}%
\item 3 x 3%
\item 3 x 5%
\item 5 x 3%
\item 2 x 3%
\item 5 x 5%
\end{enumerate}%
\vspace{0.5em}%
\item Referring to Slide 4, explain how partitioning the variable vector $ X $ into basic ($ X_B $) and non-basic ($ X_N $) variables simplifies the representation of the constraints in matrix form.  Specifically, how does this partitioning lead to the equation $ BX_B + NX_N = b $?%
\begin{enumerate}%
\item It allows for the direct solution of $X_B$ without matrix inversion.%
\item It separates the variables into dependent and independent sets, allowing for a simpler representation of the constraints.%
\item It reduces the number of variables in the problem, simplifying the calculations.%
\item It allows for the use of Gaussian elimination to solve the system of equations.%
\item It is not directly related to simplifying the constraint representation.%
\end{enumerate}%
\vspace{0.5em}%
\item In Slide 8, the objective function is rewritten as $ J = \bar{J} + \bar{C}^T X_N $. What is the significance of the reduced cost vector $ \bar{C} $ in determining whether the current solution is optimal?  How does the interpretation of $ \bar{C} $ relate to the concept of the reduced gradient in unconstrained optimization?%
\begin{enumerate}%
\item If any element of $ \bar{C} $ is positive, the current solution is optimal.%
\item If all elements of $ \bar{C} $ are zero, the current solution is optimal.%
\item If any element of $ \bar{C} $ is negative, the current solution is optimal.%
\item The reduced cost vector is irrelevant to optimality.%
\item If all elements of $ \bar{C} $ are non-negative, the current solution is optimal.%
\end{enumerate}%
\vspace{0.5em}%
\end{enumerate}%
\end{enumerate}

%
\newpage%
\section{Answers}%
\label{sec:Answers}%
\begin{enumerate}%
\item Consider the linear program in Slide 1, expressed in matrix notation.  If we introduce slack variables to convert the inequality constraints into equalities, what is the dimension of the augmented constraint matrix $\begin{bmatrix} A & I \end{bmatrix}$, where $A$ is the original constraint matrix and $I$ is the identity matrix?%
\begin{enumerate}%
\item \incorrect{This is the dimension of the original constraint matrix $A$, not the augmented matrix.}%
\item The original constraint matrix $A$ has dimensions $m \times n$.  The identity matrix $I$ has dimensions $m \times m$ to match the number of constraints.  Therefore, the augmented matrix $\begin{bmatrix} A & I \end{bmatrix}$ has dimensions $m \times (m+n)$.%
\item \incorrect{This is incorrect; the number of rows should be $m$, corresponding to the number of constraints.}%
\item \incorrect{This is incorrect; the number of rows should be $m$, corresponding to the number of constraints.}%
\item \incorrect{This is the transpose of the original constraint matrix $A$, not the augmented matrix.}%
\end{enumerate}%
\vspace{1em}%
\item Referring to Slide 4, explain the significance of partitioning the variable vector $X$ into basic ($X_B$) and non-basic ($X_N$) variables in the context of the simplex method. How does this partitioning simplify the representation of the objective function and constraints?%
\begin{enumerate}%
\item \incorrect{While the inverse of the basic variable matrix ($B$) is calculated, the partitioning itself doesn't directly simplify this calculation.}%
\item \incorrect{The partitioning simplifies the primal problem's solution; the dual problem's simplification is a consequence, not the direct result of the partitioning.}%
\item The partitioning is crucial because at each iteration of the simplex method, the non-basic variables are set to zero, simplifying the constraints to a system of $m$ equations in $m$ unknowns (the basic variables). This allows for easy calculation of the basic variable values and the objective function value at each vertex of the feasible region.%
\item \incorrect{The partitioning doesn't directly simplify the graphical representation; it simplifies the algebraic representation used in the simplex algorithm.}%
\item \incorrect{The partitioning is fundamental to the simplex algorithm's efficiency and is not merely a notational convenience.}%
\end{enumerate}%
\vspace{1em}%
\item In Slide 8, the objective function is rewritten as $J = \bar{J} + \bar{C}^T X_N$. What is the significance of the vector $\bar{C}$ (often called the reduced cost vector) in determining whether the current solution is optimal?%
\begin{enumerate}%
\item \incorrect{A positive element in $\bar{C}$ indicates that increasing the corresponding non-basic variable would improve the objective function, meaning the current solution is not optimal.}%
\item \incorrect{A zero element in $\bar{C}$ indicates that increasing the corresponding non-basic variable would not change the objective function value.  While this doesn't necessarily mean the solution is optimal, it suggests the possibility of alternative optimal solutions.}%
\item \incorrect{If any element of $\bar{C}$ is positive, the solution is not optimal because increasing the corresponding non-basic variable would improve the objective function.}%
\item The reduced cost vector $\bar{C}$ represents the change in the objective function value for a unit increase in each non-basic variable. If all elements of $\bar{C}$ are negative, increasing any non-basic variable will not improve the objective function, indicating that the current solution is optimal.%
\item \incorrect{Both $\bar{J}$ and $\bar{C}$ are crucial for determining optimality. $\bar{J}$ gives the current objective function value, while $\bar{C}$ determines whether any improvement is possible.}%
\end{enumerate}%
\vspace{1em}%
\item Slides 10 and 11 present two tables representing the primal and dual simplex methods in matrix form.  Explain the relationship between the two tables, focusing on the connection between the primal and dual objective function values and the basic variable values.%
\begin{enumerate}%
\item \incorrect{The tables are closely related, representing the primal and dual formulations of the simplex method, which are intimately connected through duality theory.}%
\item The relationship between the primal and dual simplex methods is highlighted by the fact that many entries in the dual table are the negative transpose of corresponding entries in the primal table. This reflects the duality theorem's connection between the primal and dual problems.%
\item \incorrect{While there's a mathematical relationship, it's not a simple inverse; it involves transposition and negation.}%
\item \incorrect{The relationship is more complex than simply swapping rows and columns; it involves transposition and negation of specific entries.}%
\item \incorrect{The tables are not identical; they represent different but related perspectives (primal and dual) on the same optimization problem.}%
\end{enumerate}%
\vspace{1em}%
\item%
\begin{enumerate}%
\item Consider the linear program presented in Slide 1: Maximize $ \zeta = 3x_1 + 2x_2 $ subject to $ -x_1 + 3x_2 \le 12 $, $ x_1 + x_2 \le 8 $, $ 2x_1 - x_2 \le 10 $, and $ x_1, x_2 \ge 0 $. After introducing slack variables $w_1, w_2, w_3$ (Slide 1), and expressing the problem in matrix form (Slide 7), explain how the initial basic feasible solution is determined, and what its corresponding value of the objective function $\zeta$ is.%
\begin{enumerate}%
\item The initial basic feasible solution is found by setting the non-basic variables ($x_1$ and $x_2$) to zero.  This directly gives the values of the basic variables ($w_1, w_2, w_3$) from the constraints: $w_1 = 12, w_2 = 8, w_3 = 10$. Substituting $x_1 = 0$ and $x_2 = 0$ into the objective function yields $\zeta = 3(0) + 2(0) = 0$.%
\item \incorrect{This solution violates the constraints.  The values of $x_1$ and $x_2$ are not feasible.}%
\item \incorrect{The initial basic feasible solution is directly obtained by setting the non-basic variables to zero and solving for the basic variables.}%
\item \incorrect{This is a feasible solution, but it is not the initial basic feasible solution. This solution is reached after several iterations of the simplex method.}%
\item \incorrect{This is a feasible solution, but it is not the initial basic feasible solution. This solution is reached after several iterations of the simplex method.}%
\end{enumerate}%
\vspace{0.5em}%
\item Referring to Slide 8, explain the significance of the reduced cost vector $\bar{C}$ in determining whether the current solution is optimal.  How does the interpretation of $\bar{C}$ relate to the concept of the reduced gradient in unconstrained optimization?%
\begin{enumerate}%
\item \incorrect{A positive element in $\bar{C}$ indicates that increasing the corresponding non-basic variable will *decrease* the objective function.}%
\item \incorrect{If all elements of $\bar{C}$ are zero, it means the current solution is optimal, but there might be alternative optimal solutions.}%
\item A negative element in $\bar{C}$ indicates that increasing the corresponding non-basic variable will improve the objective function.  If all elements of $\bar{C}$ are non-negative, no such improvement is possible, and the current solution is optimal. This is analogous to the reduced gradient in unconstrained optimization, where a zero gradient indicates a stationary point (potentially optimal).%
\item \incorrect{The reduced cost vector is crucial for determining optimality in the simplex method. It indicates the marginal improvement in the objective function by changing the non-basic variables.}%
\item \incorrect{This is incorrect; all elements of $\bar{C}$ must be non-negative for optimality.}%
\end{enumerate}%
\vspace{0.5em}%
\item Slides 10 and 11 present two tables representing the primal and dual simplex methods in matrix form. Explain the relationship between the two tables, focusing on the connection between the primal and dual objective function values and the basic variable values.  How does complementary slackness manifest in these tables?%
\begin{enumerate}%
\item \incorrect{The tables are not identical; they represent different aspects of the primal and dual problems.}%
\item \incorrect{While there's a relationship involving transposition, it's not a simple transpose with a sign change.}%
\item \incorrect{The tables are closely related, representing the primal and dual simplex methods, which are intimately connected through duality theory.}%
\item The tables represent the primal and dual simplex methods.  Strong duality implies that the optimal objective function values are equal (or negatives of each other, depending on the maximization/minimization). Complementary slackness is reflected in the relationship between the basic and non-basic variables in both tables.  If a primal variable is basic (non-zero), its corresponding dual variable is non-basic (zero), and vice-versa.%
\item \incorrect{The basic variable values are not simply negated; the relationship is more complex and involves the duality theorem.}%
\end{enumerate}%
\vspace{0.5em}%
\end{enumerate}%
\item Consider the linear program in Slide 1, expressed in matrix notation. After introducing slack variables, the constraints become $Ax + I_m w = b$, where $x \in \mathbb{R}^n$ are the decision variables, $w \in \mathbb{R}^m$ are the slack variables, $A \in \mathbb{R}^{m \times n}$ is the constraint matrix, $I_m$ is the $m \times m$ identity matrix, and $b \in \mathbb{R}^m$ is the right-hand side vector.  If we partition the variable vector as $X = \begin{bmatrix} x \\ w \end{bmatrix}$, what is the dimension of the augmented constraint matrix $\begin{bmatrix} A & I_m \end{bmatrix}$?%
\begin{enumerate}%
\item \incorrect{This is the dimension of the original constraint matrix $A$, before introducing slack variables.}%
\item \incorrect{This is the transpose of the dimension of the original constraint matrix $A$.}%
\item The augmented constraint matrix $\begin{bmatrix} A & I_m \end{bmatrix}$ has $m$ rows (from the original $m$ constraints) and $n + m$ columns (from the $n$ decision variables and $m$ slack variables). Therefore, its dimension is $m \times (m+n)$.%
\item \incorrect{This is incorrect; the number of rows is $m$, not $m+n$.}%
\item \incorrect{This is incorrect; the number of rows is $m$, not $n$.}%
\end{enumerate}%
\vspace{1em}%
\item Referring to Slide 4, the objective function is expressed as $c^T X = c_B^T X_B + c_N^T X_N$, where $X_B$ and $X_N$ are vectors of basic and non-basic variables, respectively, and $c_B$ and $c_N$ are the corresponding cost vectors.  In the initial basic feasible solution, all non-basic variables are set to zero ($X_N = 0$).  Given that the objective function after simplex iterations is rewritten in Slide 8 as $J = \bar{J} + \bar{C}^T X_N$, where $\bar{J}$ is a constant and $\bar{C}$ is the reduced cost vector, what is the significance of the reduced cost vector $\bar{C}$ in determining the optimality of the current solution?%
\begin{enumerate}%
\item \incorrect{A positive element in $\bar{C}$ indicates that the objective function can be improved by increasing the corresponding non-basic variable.}%
\item \incorrect{If all elements of $\bar{C}$ are zero, there might be alternative optimal solutions, but the current solution is still optimal.}%
\item \incorrect{A negative element in $\bar{C}$ means increasing the corresponding non-basic variable would worsen the objective function.}%
\item \incorrect{This is incorrect; a positive element indicates that the solution is not optimal.}%
\item If all elements of $\bar{C}$ are non-positive, the current solution is optimal.  A positive element in $\bar{C}$ indicates that increasing the corresponding non-basic variable would improve the objective function, meaning the current solution is not optimal.%
\end{enumerate}%
\vspace{1em}%
\item In Slide 10, two tables are presented, representing the primal and dual simplex methods in matrix form. The primal table shows the objective function value $c_B^T B^{-1} b$ and the basic variable values $B^{-1} b$, while the dual table shows $-c_B^T B^{-1} b$ and $-(B^{-1}b)^T$.  Explain the relationship between the primal and dual objective function values in these tables, and how this relates to the concept of duality in linear programming.%
\begin{enumerate}%
\item \incorrect{This is only true at the optimal solution, according to strong duality.}%
\item The primal objective function value ($c_B^T B^{-1} b$) is the negative of the dual objective function value ($-c_B^T B^{-1} b$). This reflects the fundamental relationship in duality: the optimal objective function values of the primal and dual problems are equal (but opposite in sign if one is maximization and the other is minimization).%
\item \incorrect{The relationship is not simply a transpose; it involves a negative sign.}%
\item \incorrect{There is a fundamental relationship between the primal and dual objective function values, based on duality theory.}%
\item \incorrect{This is only true for feasible solutions before reaching optimality (weak duality).}%
\end{enumerate}%
\vspace{1em}%
\item Consider Slide 7, where the constraints are expressed in matrix form as $\begin{bmatrix} B & N \end{bmatrix} \begin{bmatrix} X_B \\ X_N \end{bmatrix} = b$, where $B$ and $N$ are matrices formed from the columns of the augmented matrix $\begin{bmatrix} A & I_m \end{bmatrix}$ corresponding to basic and non-basic variables, respectively.  Explain how the initial basic feasible solution is determined in the simplex method, and how this relates to the matrices $B$ and $N$.%
\begin{enumerate}%
\item In the initial basic feasible solution, all non-basic variables are set to zero ($X_N = 0$).  The basic variables are then determined by solving $BX_B = b$, which gives $X_B = B^{-1}b$.  This assumes that $B$ is invertible (which is true for a feasible problem).%
\item \incorrect{This is incorrect; the non-basic variables are set to zero initially.}%
\item \incorrect{This is incorrect; the basic variables are obtained by solving $BX_B = b$.}%
\item \incorrect{This is incorrect; the non-basic variables are set to zero initially.}%
\item \incorrect{The initial basic feasible solution can be determined using the given matrix equation.}%
\end{enumerate}%
\vspace{1em}%
\item%
\begin{enumerate}%
\item Consider the linear program presented in Slide 1: Maximize $ \zeta = 3x_1 + 2x_2 $ subject to $ -x_1 + 3x_2 \le 12 $, $ x_1 + x_2 \le 8 $, $ 2x_1 - x_2 \le 10 $, and $ x_1, x_2 \ge 0 $. After introducing slack variables $ w_1, w_2, w_3 $ (Slide 1) and expressing the problem in matrix form (Slide 7), what is the dimension of the augmented constraint matrix $ \begin{bmatrix} A & I \end{bmatrix} $, where $ A $ is the original constraint matrix and $ I $ is the identity matrix?%
\begin{enumerate}%
\item \incorrect{This is incorrect. The original constraint matrix has 2 variables, and 3 slack variables are added.}%
\item The original constraint matrix $A$ is 3x2 (3 constraints, 2 variables).  After introducing 3 slack variables, the augmented matrix $ \begin{bmatrix} A & I \end{bmatrix} $ becomes 3x5 (3 constraints, 2 original variables + 3 slack variables).%
\item \incorrect{This is incorrect. The number of rows represents the number of constraints, and the number of columns represents the number of variables.}%
\item \incorrect{This is incorrect. There are 3 constraints in the problem.}%
\item \incorrect{This is incorrect. The identity matrix added has dimensions 3x3, not 5x5.}%
\end{enumerate}%
\vspace{0.5em}%
\item Referring to Slide 4, explain how partitioning the variable vector $ X $ into basic ($ X_B $) and non-basic ($ X_N $) variables simplifies the representation of the constraints in matrix form.  Specifically, how does this partitioning lead to the equation $ BX_B + NX_N = b $?%
\begin{enumerate}%
\item \incorrect{While solving for $X_B$ is a consequence, the simplification is primarily in the representation of the constraints.}%
\item Partitioning $X$ into basic and non-basic variables allows us to rewrite the constraint equation $ \begin{bmatrix} A & I \end{bmatrix} \begin{bmatrix} X \\ W \end{bmatrix} = b $ as $ BX_B + NX_N = b $, where $B$ contains the columns of $ \begin{bmatrix} A & I \end{bmatrix} $ corresponding to basic variables and $N$ contains the columns corresponding to non-basic variables.  This separates the variables into a dependent set ($X_B$, determined by $X_N$) and an independent set ($X_N$).%
\item \incorrect{The number of variables remains the same; the simplification is in how they are handled algebraically.}%
\item \incorrect{Gaussian elimination can be used, but the partitioning simplifies the application of Gaussian elimination.}%
\item \incorrect{The partitioning is fundamental to the matrix representation of the simplex method.}%
\end{enumerate}%
\vspace{0.5em}%
\item In Slide 8, the objective function is rewritten as $ J = \bar{J} + \bar{C}^T X_N $. What is the significance of the reduced cost vector $ \bar{C} $ in determining whether the current solution is optimal?  How does the interpretation of $ \bar{C} $ relate to the concept of the reduced gradient in unconstrained optimization?%
\begin{enumerate}%
\item \incorrect{This is incorrect; a positive element indicates that increasing the corresponding non-basic variable would improve the objective function.}%
\item \incorrect{This is incorrect; all elements being zero indicates multiple optimal solutions.}%
\item \incorrect{This is incorrect; a negative element indicates that increasing the corresponding non-basic variable would improve the objective function.}%
\item \incorrect{This is incorrect; the reduced cost vector is crucial for determining optimality in the simplex method.}%
\item If all elements of $ \bar{C} $ are non-negative, it means that increasing any non-basic variable will not improve the objective function. This indicates that the current solution is optimal.  Similar to the reduced gradient in unconstrained optimization, $ \bar{C} $ represents the change in the objective function per unit increase in a non-basic variable, holding other non-basic variables constant.%
\end{enumerate}%
\vspace{0.5em}%
\end{enumerate}%
\end{enumerate}

%
\end{document}