QUESTION 1
In the context of the Network Simplex Method, what is the significance of a spanning tree in representing the network flow problem, and how does it relate to the simplex method's basis?
A. A spanning tree is an arbitrary subgraph used for visualization purposes, unrelated to the simplex method's basis.
B. A spanning tree represents a feasible solution, and its arcs correspond to the basic variables in the simplex method's basis.
C. A spanning tree is a cycle in the network, representing a non-feasible solution, and its arcs are non-basic variables.
D. A spanning tree is only relevant in the dual simplex method, not the primal.
E. A spanning tree is a disconnected subgraph, and its arcs are not directly related to the simplex method's basis.

ANSWER: B

EXPLANATION:
A. Incorrect. The spanning tree is not arbitrary; it's fundamental to the algorithm's efficiency and directly relates to the simplex method's basis.
B. A spanning tree is crucial because it defines a basis for the simplex method.  Each arc in the spanning tree corresponds to a basic variable in the linear program representing the network flow.  The flow on these arcs determines the solution, while arcs not in the spanning tree have zero flow (non-basic variables).
C. Incorrect. A spanning tree is acyclic (contains no cycles) and represents a feasible (though not necessarily optimal) solution.  Cycles are formed when adding arcs to the spanning tree.
D. Incorrect. Spanning trees are fundamental to both primal and dual network simplex methods.
E. Incorrect. A spanning tree is a connected subgraph, and its arcs are directly related to the basic variables in the simplex tableau.


QUESTION 2
Why might the initial primal flow obtained by setting flows on arcs outside the spanning tree to zero be infeasible in the Network Simplex Method?
A. The spanning tree might not be connected, leading to infeasibility.
B. The initial spanning tree might contain cycles, causing infeasibility.
C. Some flow variables corresponding to arcs within the spanning tree might have negative values.
D. The supplies and demands at the nodes might not be balanced.
E. The costs associated with the arcs might be negative.

ANSWER: C

EXPLANATION:
A. Incorrect. A spanning tree, by definition, is connected.
B. Incorrect. A spanning tree, by definition, is acyclic (contains no cycles).
C. The initial solution sets flows on arcs outside the spanning tree to zero.  However, solving for the flows within the spanning tree might result in some flows having negative values, violating the non-negativity constraints of the problem.
D. Incorrect. While unbalanced supplies and demands would lead to an infeasible problem, the initial solution assumes balanced supplies and demands.
E. Incorrect. Negative arc costs do not directly cause infeasibility in the initial solution; they affect the optimality but not the feasibility.


QUESTION 3
In the Dual Network Simplex Method, what is the significance of the condition that the reduced costs ($z_{ij}$) for arcs not in the spanning tree are all non-negative?
A. This condition guarantees primal feasibility.
B. This condition guarantees optimality.
C. This condition guarantees dual feasibility.
D. This condition is only relevant for the primal simplex method.
E. This condition is irrelevant to the feasibility or optimality of the solution.

ANSWER: C

EXPLANATION:
A. Incorrect. Primal feasibility is achieved when all flow variables are non-negative.
B. Incorrect. Optimality is achieved when all reduced costs are non-negative and primal feasibility is satisfied.
C. In the dual simplex method, the non-negativity of the reduced costs for non-basic variables (arcs not in the spanning tree) is the condition for dual feasibility.  The algorithm starts with a dual feasible solution and iteratively improves it until primal feasibility is also achieved.
D. Incorrect. This condition is crucial for dual feasibility in the dual network simplex method.
E. Incorrect. This condition is fundamental to the dual network simplex method's approach to finding an optimal solution.


QUESTION 4
Explain the key difference in how the primal and dual network simplex methods handle the selection of entering and leaving arcs during an iteration.
A. Both methods use the same rules for selecting entering and leaving arcs.
B. The primal method selects the entering arc based on reduced cost and the leaving arc based on the minimum flow in a cycle; the dual method selects the leaving arc based on negative flow and the entering arc based on minimum dual slack.
C. The primal method focuses on improving primal feasibility, while the dual method focuses on improving dual feasibility, but the arc selection rules are identical.
D. The dual method does not use the concept of a spanning tree, unlike the primal method.
E. The entering and leaving arc selection is random in both methods.

ANSWER: B

EXPLANATION:
A. Incorrect. The selection rules are fundamentally different, reflecting the different approaches to achieving optimality.
B. The primal network simplex method starts with a primal feasible solution (possibly with some arcs having zero flow) and improves it by selecting an entering arc with a negative reduced cost. The leaving arc is chosen to maintain a spanning tree structure. The dual network simplex method starts with a dual feasible solution (all reduced costs non-negative) and improves it by selecting a leaving arc with a negative flow. The entering arc is chosen to maintain a spanning tree and dual feasibility.
C. While the methods aim to improve primal and dual feasibility, respectively, the arc selection rules are not identical.
D. Incorrect. Both methods utilize the concept of a spanning tree, although they handle it differently.
E. Incorrect. The selection of entering and leaving arcs is deterministic and follows specific rules in both methods.


QUESTION 5
In Slide 2, an initial primal flow is obtained by setting $x_{ij} = 0$ for $(i, j) \in \mathcal{A} \setminus T$. What is a potential issue with this approach?
A. The resulting flow might not be optimal.
B. The spanning tree $T$ might not be connected.
C. The resulting flow might contain negative values.
D. The basis matrix $B$ might be singular.
E. The vector $\tilde{b}$ might not be consistent.

ANSWER: C

EXPLANATION:
A. Answer A is incorrect. While the initial flow might not be optimal, the primary concern highlighted in Slide 2 is the possibility of negative flow values.
B. Answer B is incorrect. The definition of a spanning tree in Slide 1 ensures that $T$ is connected.
C. Answer C is correct.  The slide explicitly states that this initial flow might be infeasible because some $x_{ij}$ could be negative.
D. Answer D is incorrect.  The slide doesn't discuss the singularity of $B$, and it's not the primary concern of the initial flow construction.
E. Answer E is incorrect. The consistency of $\tilde{b}$ is not directly addressed in the context of this initial flow construction.


QUESTION 6
According to Slide 3, what equation defines the relationship between dual variables ($y_i$, $y_j$), reduced costs ($z_{ij}$), and arc costs ($c_{ij}$) for an arbitrary arc $(i, j)$ in the network?
A. $y_i + y_j + z_{ij} = c_{ij}$
B. $y_j - y_i + z_{ij} = c_{ij}$
C. $y_i - y_j + z_{ij} = c_{ij}$
D. $y_i + y_j - z_{ij} = c_{ij}$
E. $y_j - y_i - z_{ij} = c_{ij}$

ANSWER: B

EXPLANATION:
A. Answer A is incorrect. This equation does not correctly represent the relationship between the dual variables, reduced costs, and arc costs.
B. Answer B is correct. This equation is explicitly given and highlighted in a box on Slide 3.
C. Answer C is incorrect. This equation does not correctly represent the relationship between the dual variables, reduced costs, and arc costs.
D. Answer D is incorrect. This equation does not correctly represent the relationship between the dual variables, reduced costs, and arc costs.
E. Answer E is incorrect. This equation does not correctly represent the relationship between the dual variables, reduced costs, and arc costs.


QUESTION 7
Slide 4 states that adding one arc to a spanning tree $T$ results in what?
A. A disconnected graph
B. A spanning tree with increased cost
C. A cycle
D. A graph with reduced connectivity
E. A minimum spanning tree

ANSWER: C

EXPLANATION:
A. Answer A is incorrect. Adding an arc to a spanning tree does not disconnect the graph; it remains connected.
B. Answer B is incorrect. While the cost might increase, the key consequence is the creation of a cycle.
C. Answer C is correct. The slide explicitly states that adding one arc to a spanning tree creates a cycle.
D. Answer D is incorrect. Adding an arc to a spanning tree does not reduce connectivity; it remains connected.
E. Answer E is incorrect.  Adding an arc to a spanning tree does not necessarily result in a minimum spanning tree.


QUESTION 8
In Slide 5, what does the presence of red arcs in the network flow diagram indicate?
A. Arcs with zero flow
B. Arcs with positive reduced costs ($z_{ij} > 0$)
C. Arcs with negative reduced costs ($z_{ij} < 0$)
D. Arcs in the current spanning tree
E. Arcs not in the current spanning tree

ANSWER: C

EXPLANATION:
A. Answer A is incorrect. The slide does not indicate that red arcs have zero flow.
B. Answer B is incorrect. Red arcs have negative, not positive, reduced costs.
C. Answer C is correct. The slide caption explicitly states that red arcs represent arcs with negative reduced costs, indicating non-optimality.
D. Answer D is incorrect. The slide does not indicate that red arcs are in the current spanning tree.
E. Answer E is incorrect. The slide does not indicate that red arcs are not in the current spanning tree.


QUESTION 9
In the context of the Network Simplex Method, what is the fundamental significance of a spanning tree in representing the network flow problem, and how does this representation facilitate the algorithm's iterative solution process?
A. A spanning tree provides a convenient way to visualize the network, but it doesn't directly impact the algorithm's efficiency.
B. The spanning tree ensures that the network is connected, preventing disconnected components that would hinder the optimization process.
C. A spanning tree defines a basis for the linear program representing the network flow problem, allowing for efficient updates during simplex iterations.
D. The spanning tree guarantees that the initial solution is feasible, eliminating the need for Phase I of the simplex method.
E. A spanning tree simplifies the calculation of the objective function, but it doesn't affect the feasibility or optimality of the solution.

ANSWER: C

EXPLANATION:
A. While a spanning tree aids visualization, its significance goes far beyond mere visualization. It's integral to the algorithm's efficiency and convergence.
B. While connectivity is important, the spanning tree's role is more profound. It's not just about connectivity; it's about defining a basis for the linear program.
C. A spanning tree is crucial because it directly corresponds to a basis in the simplex method.  Each arc in the spanning tree represents a basic variable, and the flow values on these arcs determine the basic feasible solution.  The acyclic nature of the tree prevents redundancy and ensures that the basis is non-singular.  The algorithm iteratively modifies the spanning tree (by adding and removing arcs) which corresponds to pivoting operations in the simplex method, efficiently moving from one basic feasible solution to another until optimality is reached.
D. The initial solution based on a spanning tree might be infeasible (negative flows). Phase I might still be necessary to achieve primal feasibility.
E. The spanning tree significantly impacts feasibility and optimality by defining the basis and enabling efficient updates during the simplex iterations.


QUESTION 10
In Slide 2, an initial primal flow is obtained by setting $x_{ij} = 0$ for $(i, j) \in \mathcal{A} \setminus T$. What is a potential issue with this approach?
A. The resulting flow might not be optimal.
B. The resulting spanning tree might not be connected.
C. The resulting flow might contain cycles.
D. The resulting flow might have negative values for some $x_{ij}$
E. The resulting flow might not satisfy the supply/demand constraints at each node.

ANSWER: D

EXPLANATION:
A. Answer A is incorrect. While the initial flow might not be optimal, that's not the primary issue highlighted in the slide. The focus is on feasibility, not optimality.
B. Answer B is incorrect. The spanning tree $T$ is already defined as connected. Setting flows to zero on arcs outside $T$ doesn't affect the connectivity of $T$.
C. Answer C is incorrect.  The spanning tree $T$ by definition contains no cycles.  Setting flows to zero on arcs outside $T$ cannot introduce cycles.
D. Answer D is correct.  The slide explicitly states that this initial primal flow might be infeasible because some $x_{ij}$ could be negative.
E. Answer E is incorrect. While it's possible the initial flow might not satisfy supply/demand constraints, the slide's primary concern is the potential for negative flow values.


