SLIDE 1
<LATEX>
\textbf{Convex Analysis} (LVJ Chapter 10)

\underline{\textbf{Def 1}} \quad $S \subseteq \mathbb{R}^m$ is \underline{convex} if
for any $z_1, z_2 \in S$, $\implies tz_1 + (1-t)z_2 \in S$ \quad $0 \le t \le 1$

\underline{\textbf{Def 2}} Convex combination of $z_1, z_2, \dots, z_n \in \mathbb{R}^m$
$t_1 z_1 + t_2 z_2 + \dots + t_n z_n$ \quad $t_i \ge 0$
$\sum_i t_i = 1$
</LATEX>

<FIGURE [100.0, 50.0, 400.0, 450.0]>Definition 1 and its illustration.</FIGURE>
<FIGURE [400.0, 50.0, 700.0, 450.0]>Definition 2 and its illustrations.</FIGURE>

<DESCRIPTION>This slide introduces the concepts of convex sets and convex combinations in the context of convex analysis, specifically chapter 10 of a textbook by LVJ.  Definition 1 states that a set $S$ in $\mathbb{R}^m$ is convex if, for any two points $z_1$ and $z_2$ in $S$, the line segment connecting them (represented by the convex combination $tz_1 + (1-t)z_2$ where $0 \le t \le 1$) is entirely contained within $S$.  A simple illustration shows two points $z_1$ and $z_2$ and the line segment between them. Definition 2 defines a convex combination of $n$ points $z_1, z_2, \dots, z_n$ in $\mathbb{R}^m$ as a weighted sum $t_1 z_1 + t_2 z_2 + \dots + t_n z_n$, where the weights $t_i$ are non-negative and sum to 1.  The illustrations show examples of convex combinations for two and three points, forming a line segment and a triangle respectively.  The context of linear programming is relevant because convexity is a crucial property of feasible regions in linear programming problems.</DESCRIPTION>

SLIDE 2
<LATEX>
\textbf{Thm 10.1} \quad $S$ is convex $\iff$
it contains all conv. comb. of points in $S$
$pf$ $\iff$

\underline{Suppose $S$ contain all conv. comb. of pts in $S$}
Then clearly, for any $z_1, z_2 \in S$
\underline{$tz_1 + (1-t)z_2 \in S$}
Conv. comb. of $z_1, z_2$
$\implies$ \underline{$S$ is convex}
</LATEX>

<FIGURE [20.0, 20.0, 400.0, 480.0]>Theorem 10.1 statement and proof outline.</FIGURE>

<DESCRIPTION>This slide presents Theorem 10.1, which states that a set $S$ is convex if and only if it contains all convex combinations of its points. The proof focuses on one direction of the implication: if $S$ contains all convex combinations of its points, then $S$ is convex.  The proof begins by assuming that $S$ contains all convex combinations of its points. Then, it shows that for any two points $z_1$ and $z_2$ in $S$, their convex combination $tz_1 + (1-t)z_2$ (where $0 \le t \le 1$) is also in $S$ by the assumption. This directly satisfies the definition of a convex set, thus proving that $S$ is convex. The underlining highlights the key assumption and conclusion of this part of the proof. The notation "pf" indicates "proof," and the double-headed arrow indicates the "if and only if" nature of the theorem. "conv. comb." is an abbreviation for "convex combination". The context of linear programming is crucial because this theorem is fundamental to understanding the properties of feasible regions in linear programming problems, which are often convex sets.</DESCRIPTION>

SLIDE 3
<LATEX>
\textbf{Thm 10.1} \quad $S$ is convex $\iff$
it contains all conv. comb. of points in $S$
$pf$ $\implies$ \underline{Suppose $S$ is convex.}

$n=2$: $z_1, z_2 \in S \implies t_1 z_1 + t_2 z_2 \in S$, $t_1, t_2 \ge 0$
$t_1 + t_2 = 1$

$n=3$: $z_1, z_2, z_3 \in S \implies t_1 z_1 + t_2 z_2 + t_3 z_3 = (t_1 + t_2) \left( \frac{t_1}{t_1 + t_2} z_1 + \frac{t_2}{t_1 + t_2} z_2 \right) + t_3 z_3$
$t_1 + t_2 + t_3 = 1$
$t_1, t_2 \ge 0, t_3 \ge 0$
$\implies t_1 z_1 + t_2 z_2 + t_3 z_3 \in S$
</LATEX>

<FIGURE [20.0, 20.0, 480.0, 480.0]>Theorem 10.1 statement and proof outline for n=2 and n=3.</FIGURE>

<DESCRIPTION>This slide presents Theorem 10.1 and outlines a proof by induction showing that if a set $S$ is convex, then it contains all convex combinations of its points. The theorem statement asserts the equivalence between the convexity of $S$ and the inclusion of all convex combinations of points in $S$. The proof starts by assuming $S$ is convex. The base case ($n=2$) demonstrates that if $z_1$ and $z_2$ are in $S$, then any convex combination $t_1z_1 + t_2z_2$ (with $t_1, t_2 \ge 0$ and $t_1 + t_2 = 1$) is also in $S$ due to the definition of convexity. The inductive step ($n=3$) shows that if $z_1, z_2, z_3 \in S$, then a convex combination $t_1z_1 + t_2z_2 + t_3z_3$ can be rewritten as a convex combination of a convex combination of $z_1$ and $z_2$ and $z_3$.  Since the inner convex combination is in $S$ (by the base case), and the outer convex combination is also in $S$ (by the definition of convexity), the entire expression is in $S$. This inductive argument can be extended to any number of points.  The underlining highlights the key assumption. The notation "pf" stands for "proof," and "conv. comb." is short for "convex combination." The context of linear programming is crucial because this theorem is fundamental to understanding the properties of feasible regions in linear programming problems, which are often convex sets.  The implication arrow ($\implies$) indicates the direction of the proof.</DESCRIPTION>

SLIDE 4
<LATEX>
\textbf{Thm 10.1} \quad $S$ is convex $\iff$
it contains all conv. comb. of points in $S$
$pf$ $\implies$ \underline{Suppose $S$ is convex.}

$n=2$: $z_1, z_2 \in S \implies t_1 z_1 + t_2 z_2 \in S$, $t_1, t_2 \ge 0$
$t_1 + t_2 = 1$

$n=4$: $z_1, z_2, z_3, z_4 \in S$
$t_1 z_1 + t_2 z_2 + t_3 z_3 + t_4 z_4 = (t_1 + t_2 + t_3) \left( \frac{t_1}{t_1 + t_2 + t_3} z_1 + \frac{t_2}{t_1 + t_2 + t_3} z_2 + \frac{t_3}{t_1 + t_2 + t_3} z_3 \right) + t_4 z_4$
$(t_1 + t_2 + t_3) + t_4 = 1$
$t_1, t_2, t_3 \ge 0, t_4 \ge 0$
$\implies t_1 z_1 + t_2 z_2 + t_3 z_3 + t_4 z_4 \in S$
</LATEX>

<FIGURE [20.0, 20.0, 480.0, 480.0]>Theorem 10.1 statement and proof outline for n=2 and n=4.</FIGURE>

<DESCRIPTION>This slide continues the proof of Theorem 10.1 by induction, showing that if $S$ is a convex set, then it contains all convex combinations of its points.  The previous slide covered the base case ($n=2$) and the inductive step for $n=3$. This slide extends the inductive step to $n=4$.  It assumes that $z_1, z_2, z_3, z_4 \in S$.  The convex combination $t_1 z_1 + t_2 z_2 + t_3 z_3 + t_4 z_4$ is rewritten as a convex combination of a convex combination of $z_1, z_2, z_3$ and $z_4$. Because the inner convex combination is in $S$ (by the inductive hypothesis, assuming the result holds for $n=3$), and the outer convex combination is also in $S$ (by the definition of convexity), the entire expression is in $S$. This demonstrates the inductive step, implying the theorem holds for all $n$. The underlining highlights the key assumption.  The notation "pf" stands for "proof," and "conv. comb." is short for "convex combination." The context of linear programming is crucial because this theorem is fundamental to understanding the properties of feasible regions in linear programming problems, which are often convex sets. The implication arrow ($\implies$) indicates the direction of the proof.  The handwritten notes show a slight error in the constraints on the $t_i$ values; they should all be non-negative and sum to 1.</DESCRIPTION>

SLIDE 5
<LATEX>
\underline{\text{Def}} \quad S \subseteq \mathbb{R}^m, \text{ Convex Hull of } S, \\
\text{Conv}(S) = \bigcap G \\
G \text{ convex}, S \subseteq G \\
(= \underline{\text{Smallest convex set containing } S}) \\
(\text{Note intersection of convex sets is convex.}) \\
\underline{\text{Thm 10.2}} \quad S \subseteq \mathbb{R}^m, \text{Conv}(S) = \text{collection of} \\
\text{all conv. comb. of finitely many pts from } S
</LATEX>

<FIGURE [20.0, 20.0, 480.0, 480.0]>Definitions of Convex Hull and Theorem 10.2.</FIGURE>

<DESCRIPTION>This slide introduces the definition of the convex hull of a set and presents Theorem 10.2.  The definition states that for a set $S$ in $\mathbb{R}^m$, the convex hull of $S$, denoted as Conv($S$), is the intersection of all convex sets containing $S$.  It is explicitly stated that this is equivalent to the smallest convex set containing $S$.  A note is added that the intersection of convex sets is itself a convex set.  Theorem 10.2 then states that for a set $S$ in $\mathbb{R}^m$, the convex hull of $S$ is equal to the collection of all convex combinations of finitely many points from $S$.  The underlining highlights the definition and the theorem statement.  The notation "Conv(S)" represents the convex hull of set $S$, and "conv. comb." is an abbreviation for "convex combination". The context of linear programming is crucial because the convex hull is a fundamental concept in understanding the feasible region of a linear program, which is always a convex set.  The theorem provides a practical way to compute or characterize the convex hull.</DESCRIPTION>

SLIDE 6
<LATEX>
\textbf{Thm 10.2} \quad $S \subseteq \mathbb{R}^m$, $\text{Conv}(S) = \text{collection of}$ \\
$\text{all conv. comb. of finitely many pts from } S$ \\
$pf.$ \quad $\text{Let } H = \text{collection of all conv. comb.}$ \\
$\text{of finitely many pts from } S$ \\
$\text{Need to show } \text{Conv}(S) = H$ \\
$(1) \quad \underline{\text{Conv}(S) \subseteq H} \quad (\because H \text{ is conv.})$
</LATEX>

<FIGURE [20.0, 20.0, 480.0, 480.0]>Theorem 10.2 statement and proof outline.</FIGURE>

<DESCRIPTION>This slide presents Theorem 10.2 and outlines the beginning of its proof.  Theorem 10.2 states that for a set $S$ in $\mathbb{R}^m$, the convex hull of $S$, denoted Conv($S$), is equal to the set of all convex combinations of finitely many points from $S$. The proof starts by defining $H$ as the set of all convex combinations of finitely many points from $S$. The goal is to show that Conv($S$) = $H$. The slide shows the first part of the proof, aiming to demonstrate that Conv($S$) is a subset of $H$.  The underlining highlights this key step in the proof. The notation "Conv(S)" represents the convex hull of set $S$, "conv. comb." is short for "convex combination", and "pts" is short for "points". The implication ($\because$) indicates that the inclusion follows because $H$ is a convex set. The context of linear programming is crucial because this theorem provides a practical way to compute or characterize the convex hull of a feasible region, which is essential in solving linear programming problems.</DESCRIPTION>

SLIDE 7
<LATEX>
\textbf{Thm 10.2} \quad $S \subseteq \mathbb{R}^m$, $\text{Conv}(S) = \text{collection of}$ \\
$\text{all conv. comb. of finitely many pts from } S$ \\
$pf.$ \quad $\text{Let } H = \text{collection of all conv. comb.}$ \\
$\text{of finitely many pts from } S$ \\
$\text{Need to show } \text{Conv}(S) = H$ \\
$(2) \quad H \subseteq \text{Conv}(S)$
</LATEX>

<FIGURE [20.0, 20.0, 480.0, 480.0]>Theorem 10.2 statement and proof outline.</FIGURE>

<DESCRIPTION>This slide continues the proof of Theorem 10.2, which states that the convex hull of a set $S$ in $\mathbb{R}^m$ is equal to the set of all convex combinations of finitely many points from $S$.  The proof strategy is to show that $H$, defined as the set of all convex combinations of finitely many points from $S$, is equal to Conv($S$). The previous slide started by showing Conv($S$) $\subseteq H$. This slide focuses on the second part of the proof: showing that $H \subseteq$ Conv($S$).  The underlining highlights this key step. The notation "Conv(S)" represents the convex hull of set $S$, "conv. comb." is short for "convex combination", and "pts" is short for "points".  A reference to Theorem 10.1 is made, suggesting that Theorem 10.1 will be used to prove this inclusion.  The context of linear programming is crucial because this theorem provides a practical way to compute or characterize the convex hull of a feasible region, which is essential in solving linear programming problems. The handwritten note indicates that the proof will utilize Theorem 10.1, which establishes that a convex set contains all convex combinations of its points.</DESCRIPTION>

SLIDE 8
<LATEX>
\underline{\text{Caratheodory Theorem}} \\
\text{THEOREM 10.3. The convex hull conv}(S) \text{ of a set } S \text{ in } \mathbb{R}^m \text{ consists of all convex} \\
\text{combinations of } m+1 \text{ points from } S: \\
\text{conv}(S) = \left\{ z = \sum_{j=1}^{m+1} t_j z_j : z_j \in S \text{ and } t_j \ge 0 \text{ for all } j, \text{ and } \sum_j t_j = 1 \right\} \\
\underline{pf} \quad \text{conv}(S) = \left\{ z = \sum_{j=1}^n x_j z_j : z_j \in S, x_j \ge 0, \sum_j x_j = 1 \right\} \\
\text{Consider the LP:} \quad \text{max } c^T x \\
\text{s.t. } Ax = z \\
\quad \sum x_i = 1 \\
x_i \ge 0 \\
(z \text{ is given, fixed, } A = [z_1, z_2, \dots, z_n]_{m \times n})
</LATEX>

<FIGURE [20.0, 20.0, 480.0, 480.0]>Caratheodory Theorem statement and proof outline.</FIGURE>

<DESCRIPTION>This slide presents Caratheodory's Theorem (Theorem 10.3) and outlines the beginning of its proof within the context of linear programming. The theorem states that the convex hull of a set $S$ in $\mathbb{R}^m$, denoted conv($S$), consists of all convex combinations of $m+1$ points from $S$.  The proof begins by restating the definition of the convex hull as the set of all convex combinations of points from $S$, but with the key difference that it uses only $n$ points instead of $m+1$.  The proof then introduces a linear program (LP) to find a convex combination of points in $S$ that equals a given point $z$. The LP maximizes $c^T x$ subject to $Ax = z$, where $A$ is a matrix whose columns are points from $S$, and $x$ is a vector of coefficients representing the convex combination. The constraint $\sum x_i = 1$ ensures that the combination is convex, and $x_i \ge 0$ ensures non-negativity of the coefficients.  The underlining highlights the theorem statement and the key equation in the proof. The notation "conv(S)" represents the convex hull of set $S$, and the subscript $m \times n$ indicates the dimensions of matrix $A$. The context of linear programming is crucial because the proof uses an LP to demonstrate the theorem.  The theorem itself is important because it provides a bound on the number of points needed to represent any point in the convex hull, which has implications for computational efficiency in linear programming algorithms.</DESCRIPTION>

SLIDE 9
<LATEX>
\textbf{Caratheodory Theorem} \\
\text{THEOREM 10.3. The convex hull conv}(S) \text{ of a set } S \text{ in } \mathbb{R}^m \text{ consists of all convex} \\
\text{combinations of } m+1 \text{ points from } S: \\
\text{conv}(S) = \left\{ z = \sum_{j=1}^{m+1} t_j z_j : z_j \in S \text{ and } t_j \ge 0 \text{ for all } j, \text{ and } \sum_j t_j = 1 \right\} \\
\underline{pf} \quad \text{conv}(S) = \left\{ z = \sum_{j=1}^n x_j z_j : z_j \in S, x_j \ge 0, \sum_j x_j = 1 \right\} \\
\text{Consider the LP:} \quad \text{max } c^T x \\
\text{s.t. } Ax = z \\
\quad \sum x_i = 1 \\
x_i \ge 0 \\
\left\{ \begin{array}{c} m+1 \\ \text{constraints} \end{array} \right\} \\
\text{This is feasible, and hence has a basic opt. soln} \\
\text{with } m+1 \text{ basic variables.}
</LATEX>

<FIGURE [20.0, 20.0, 480.0, 480.0]>Caratheodory Theorem statement and proof outline.</FIGURE>

<DESCRIPTION>This slide presents Caratheodory's Theorem (Theorem 10.3) and outlines the beginning of its proof. The theorem states that the convex hull of a set $S$ in $\mathbb{R}^m$ consists of all convex combinations of at most $m+1$ points from $S$. The proof starts by noting that the convex hull can be defined as the set of all convex combinations of points from $S$.  Then, a linear program (LP) is introduced to find a convex combination of points in $S$ that equals a given point $z$. The LP maximizes $c^T x$ subject to $Ax = z$, where $A$ is a matrix whose columns are points from $S$, and $x$ is a vector of coefficients representing the convex combination. The constraint $\sum x_i = 1$ ensures that the combination is convex, and $x_i \ge 0$ ensures non-negativity of the coefficients. The handwritten note points out that this LP has $m+1$ constraints and is feasible, therefore it has a basic optimal solution with $m+1$ basic variables. This implies that only $m+1$ points from $S$ are needed to represent $z$, proving the theorem. The underlining highlights the theorem statement and the key equation in the proof. The notation "conv(S)" represents the convex hull of set $S$, and the subscript $m \times n$ indicates the dimensions of matrix $A$. The context of linear programming is crucial because the proof uses an LP to demonstrate the theorem. The theorem itself is important because it provides a bound on the number of points needed to represent any point in the convex hull, which has implications for computational efficiency in linear programming algorithms.</DESCRIPTION>

SLIDE 10
<LATEX>
\textbf{Separation Theorem} \\
\text{THEOREM 10.4. Let } P \text{ and } \bar{P} \text{ be two disjoint nonempty polyhedra in } \mathbb{R}^n \text{. Then} \\
\text{there exist disjoint half-spaces } H \text{ and } \bar{H} \text{ such that } P \subset H \text{ and } \bar{P} \subset \bar{H}. \\
\downarrow \\
\{ x : v^T x \le c \} \qquad \qquad \{ x : Ax \le b \}
</LATEX>

<FIGURE [100.0, 70.0, 400.0, 400.0]>A graphical representation of a half-space defined by $v^Tx \le c$.</FIGURE>
<FIGURE [100.0, 470.0, 400.0, 800.0]>A graphical representation of a polyhedron defined as the intersection of finitely many half-spaces, represented by $\{x: Ax \le b\}$.</FIGURE>

<DESCRIPTION>This slide introduces the Separation Theorem (Theorem 10.4) in the context of linear programming. The theorem states that if $P$ and $\bar{P}$ are two disjoint nonempty polyhedra in $\mathbb{R}^n$, then there exist two disjoint half-spaces, $H$ and $\bar{H}$, such that $P$ is a subset of $H$ and $\bar{P}$ is a subset of $\bar{H}$.  The slide visually represents this concept. The left figure shows a half-space, which is represented mathematically as $\{x: v^Tx \le c\}$, where $v$ is a vector and $c$ is a scalar. The right figure depicts a polyhedron, which is shown as the intersection of multiple half-spaces, represented mathematically as $\{x: Ax \le b\}$, where $A$ is a matrix and $b$ is a vector.  The arrow points to the mathematical representation of a polyhedron as the intersection of finitely many half-spaces. The theorem is fundamental in linear programming because it provides a way to separate feasible and infeasible regions, which is crucial for developing algorithms to solve linear programming problems. The visual aids help to understand the geometric interpretation of the theorem.</DESCRIPTION>

SLIDE 11
<LATEX>
\textbf{Separation Theorem} \\
\text{THEOREM 10.4. Let } P \text{ and } \bar{P} \text{ be two disjoint nonempty polyhedra in } \mathbb{R}^n \text{. Then} \\
\text{there exist disjoint half-spaces } H \text{ and } \bar{H} \text{ such that } P \subset H \text{ and } \bar{P} \subset \bar{H}. \\
\{ x : v^T x \le c \} \qquad \qquad \text{intersection of finitely many half spaces} \qquad \{ x : Ax \le b \}
</LATEX>

<FIGURE [100.0, 70.0, 400.0, 400.0]>A graphical representation of a half-space defined by $v^Tx \le c$.  The figure shows a shaded region representing the half-space, bounded by a line. An arrow points to the mathematical representation of the half-space.</FIGURE>
<FIGURE [100.0, 470.0, 400.0, 800.0]>A graphical representation of a polyhedron defined as the intersection of finitely many half-spaces, represented by $\{x: Ax \le b\}$. The figure shows a shaded polyhedral region formed by the intersection of several half-spaces. An arrow points to the mathematical representation of the polyhedron.  A separating hyperplane is drawn between two polyhedra P and P.</FIGURE>

<DESCRIPTION>This slide presents the Separation Theorem (Theorem 10.4) which states that given two disjoint nonempty polyhedra, $P$ and $\bar{P}$, in $\mathbb{R}^n$, there exist two disjoint half-spaces, $H$ and $\bar{H}$, such that $P$ is contained in $H$ and $\bar{P}$ is contained in $\bar{H}$.  The theorem is illustrated graphically. The left figure depicts a half-space, $\{x : v^T x \le c\}$, represented as a shaded region bounded by a line. The right figure shows a polyhedron, $\{x : Ax \le b\}$, represented as a shaded region formed by the intersection of multiple half-spaces.  The handwritten text clarifies that a polyhedron is the intersection of finitely many half-spaces.  The drawing shows two polyhedra, $P$ and $\bar{P}$, separated by a line (a hyperplane in higher dimensions), which represents the separating half-spaces $H$ and $\bar{H}$. The arrows indicate the correspondence between the graphical representation and the mathematical notation. The theorem is fundamental in linear programming because it guarantees the existence of a separating hyperplane between disjoint feasible and infeasible regions, which is crucial for the development of algorithms to solve linear programming problems.  The context of linear programming is essential for understanding the significance of this theorem in optimization problems.</DESCRIPTION>

SLIDE 12
<LATEX>
\textbf{Separation Theorem} \\
\text{THEOREM 10.4. Let } P \text{ and } \tilde{P} \text{ be two disjoint nonempty polyhedra in } \mathbb{R}^n \text{. Then} \\
\text{there exist disjoint half-spaces } H \text{ and } \tilde{H} \text{ such that } P \subset H \text{ and } \tilde{P} \subset \tilde{H}. \\
P \ne \emptyset \quad P = \{ x : Ax < b \}, \quad \tilde{P} = \{ x : \tilde{A}x < \tilde{b} \} \\
P \cap \tilde{P} = \emptyset \iff \begin{cases} Ax < b \\ \tilde{A}x < \tilde{b} \end{cases} \text{ has no solution} \\
\iff \text{There is a } y, \tilde{y} \text{ s.t.} \\
y^T A + \tilde{y}^T \tilde{A} = 0 \\
y^T b + \tilde{y}^T \tilde{b} < 0 \\
y, \tilde{y} \ge 0
</LATEX>

<FIGURE [100.0, 100.0, 800.0, 800.0]>Handwritten notes outlining the Separation Theorem and its proof using Farkas' Lemma.</FIGURE>

<DESCRIPTION>This slide presents the Separation Theorem (Theorem 10.4) and its proof using Farkas' Lemma.  The theorem states that two disjoint nonempty polyhedra, $P$ and $\tilde{P}$, in $\mathbb{R}^n$ can be separated by disjoint half-spaces $H$ and $\tilde{H}$. The polyhedra are defined as $P = \{x : Ax < b\}$ and $\tilde{P} = \{x : \tilde{A}x < \tilde{b}\}$. The core of the slide is the equivalence: the intersection of $P$ and $\tilde{P}$ is empty if and only if the system of inequalities $\{Ax < b, \tilde{A}x < \tilde{b}\}$ has no solution. This is then shown to be equivalent to the existence of vectors $y$ and $\tilde{y}$ satisfying the conditions $y^T A + \tilde{y}^T \tilde{A} = 0$, $y^T b + \tilde{y}^T \tilde{b} < 0$, and $y, \tilde{y} \ge 0$.  This last part is a direct application of Farkas' Lemma, a fundamental result in linear programming. The notation "<" represents strict inequality, and the superscript $T$ denotes the transpose of a vector or matrix. The context of linear programming is crucial because the theorem and its proof are fundamental to the theory of linear programming, particularly in duality theory and the development of algorithms.</DESCRIPTION>

SLIDE 13
<LATEX>
\textbf{Separation Theorem} \\
\text{THEOREM 10.4. Let } P \text{ and } \tilde{P} \text{ be two disjoint nonempty polyhedra in } \mathbb{R}^n \text{. Then} \\
\text{there exist disjoint half-spaces } H \text{ and } \tilde{H} \text{ such that } P \subset H \text{ and } \tilde{P} \subset \tilde{H}. \\
P \ne \emptyset \quad P = \{ x : Ax < b \}, \quad \tilde{P} = \{ x : \tilde{A}x < \tilde{b} \} \\
P \cap \tilde{P} = \emptyset \iff \begin{cases} Ax < b \\ \tilde{A}x < \tilde{b} \end{cases} \text{ has no solution} \\
\text{Farkas' Lemma} \\
\text{LEMMA 10.5. The system } Ax \le b \text{ has no solutions if and only if there is a } y \text{ such that} \\
(10.8) \qquad A^Ty = 0 \quad \iff \quad \begin{pmatrix} y^TA = 0 \\ y \ge 0 \\ y^Tb < 0 \end{pmatrix}
</LATEX>

<FIGURE [100.0, 100.0, 800.0, 800.0]>Handwritten notes outlining the Separation Theorem and its proof using Farkas' Lemma.</FIGURE>

<DESCRIPTION>This slide presents the Separation Theorem (Theorem 10.4) and connects it to Farkas' Lemma (Lemma 10.5). The Separation Theorem states that two disjoint nonempty polyhedra, $P$ and $\tilde{P}$, in $\mathbb{R}^n$ can be separated by disjoint half-spaces.  The polyhedra are defined using strict inequalities: $P = \{x : Ax < b\}$ and $\tilde{P} = \{x : \tilde{A}x < \tilde{b}\}$. The slide then shows that the intersection $P \cap \tilde{P}$ is empty if and only if the system of inequalities $\{Ax < b, \tilde{A}x < \tilde{b}\}$ has no solution.  This is where Farkas' Lemma comes in.  Farkas' Lemma (Lemma 10.5) provides a condition for when a system of inequalities $Ax \le b$ has no solution.  It states that there is no solution if and only if there exists a vector $y$ such that $A^Ty = 0$, $y \ge 0$, and $y^Tb < 0$. The slide neatly presents this equivalence using a matrix notation on the right. The handwritten notes show the connection between the Separation Theorem and Farkas' Lemma, indicating that the non-existence of a solution to the combined system of inequalities from the Separation Theorem can be determined using Farkas' Lemma.  The number (10.8) suggests this is equation 10.8 in the textbook or lecture notes. The context of linear programming is crucial because both theorems are fundamental to the theory of linear programming, particularly in duality theory and the development of algorithms.  The use of strict inequalities (<) instead of weak inequalities (≤) in the definition of the polyhedra is a subtle but important detail.</DESCRIPTION>

SLIDE 14
<LATEX>
\textbf{Separation Theorem} \\
\text{THEOREM 10.4. Let } P \text{ and } \tilde{P} \text{ be two disjoint nonempty polyhedra in } \mathbb{R}^n \text{. Then} \\
\text{there exist disjoint half-spaces } H \text{ and } \tilde{H} \text{ such that } P \subset H \text{ and } \tilde{P} \subset \tilde{H}. \\
P \ne \emptyset \quad P = \{ x : Ax < b \}, \quad \tilde{P} = \{ x : \tilde{A}x < \tilde{b} \} \\
\text{There is a } y, \tilde{y} \text{ s.t.} \quad \begin{cases} y^T A + \tilde{y}^T \tilde{A} = 0 \\ y^T b + \tilde{y}^T \tilde{b} < 0 \\ y, \tilde{y} \ge 0 \end{cases} \\
\text{Define} \\
H := \{ x : y^T Ax < y^T b \}, \quad \tilde{H} := \{ x : \tilde{y}^T Ax < \tilde{y}^T \tilde{b} \} \\
H \cap \tilde{H} = \emptyset, \quad P \subset H, \quad \tilde{P} \subset \tilde{H}
</LATEX>

<FIGURE [100.0, 100.0, 900.0, 900.0]>Handwritten notes outlining the Separation Theorem and its proof.</FIGURE>

<DESCRIPTION>This slide presents the Separation Theorem (Theorem 10.4) and outlines a proof. The theorem states that two disjoint nonempty polyhedra, $P$ and $\tilde{P}$, in $\mathbb{R}^n$ can be separated by disjoint half-spaces $H$ and $\tilde{H}$.  The polyhedra are defined as $P = \{x : Ax < b\}$ and $\tilde{P} = \{x : \tilde{A}x < \tilde{b}\}$, using strict inequalities. The core of the slide is the assertion that there exist vectors $y$ and $\tilde{y}$ satisfying the conditions $y^T A + \tilde{y}^T \tilde{A} = 0$, $y^T b + \tilde{y}^T \tilde{b} < 0$, and $y, \tilde{y} \ge 0$.  These conditions are not explicitly proven on the slide but are likely derived from Farkas' Lemma (as seen in the previous slide).  The slide then defines the separating half-spaces $H$ and $\tilde{H}$ using these vectors $y$ and $\tilde{y}$.  The final statement asserts that the intersection of $H$ and $\tilde{H}$ is empty, and that $P$ is a subset of $H$ and $\tilde{P}$ is a subset of $\tilde{H}$.  The notation "<" represents strict inequality, and the superscript $T$ denotes the transpose. The context of linear programming is crucial because this theorem is fundamental to the theory of linear programming, particularly in duality theory and the development of algorithms. The use of strict inequalities (<) instead of weak inequalities (≤) in the definition of the polyhedra is a subtle but important detail.</DESCRIPTION>

SLIDE 15
<LATEX>
\textbf{Farkas' Lemma} \\
LEMMA 10.5. The system $Ax \le b$ has no solutions if and only if there is a $y$ such that
$$
\begin{aligned}
A^Ty &= 0 \\
y &\ge 0 \\
b^Ty &< 0
\end{aligned}
$$
(10.8) \\
PROOF. Consider the linear program
\begin{align*} \label{eq:1}
\text{maximize } & 0 \\
\text{subject to } & Ax \le b
\end{align*} 
and its dual
\begin{align*}
\text{minimize } & b^Ty \\
\text{subject to } & A^Ty = 0 \\
& y \ge 0
\end{align*} 
</LATEX>

<FIGURE [100.0, 100.0, 800.0, 800.0]>Handwritten notes outlining Farkas' Lemma and its proof using linear programming duality.</FIGURE>

<DESCRIPTION>This slide presents Farkas' Lemma (Lemma 10.5), a fundamental result in linear programming. The lemma states that the system of inequalities $Ax \le b$ has no solution if and only if there exists a vector $y$ satisfying the conditions $A^Ty = 0$, $y \ge 0$, and $b^Ty < 0$. The proof outline begins by considering a primal linear program that maximizes 0 subject to $Ax \le b$.  The dual of this linear program is then introduced, which minimizes $b^Ty$ subject to $A^Ty = 0$ and $y \ge 0$. The proof likely proceeds by showing the equivalence between the primal problem having no feasible solution and the dual problem being unbounded (or having an objective function value less than 0). This connection between the primal and dual problems is a key element of linear programming duality theory. The equation number (10.8) suggests this is equation 10.8 in the textbook or lecture notes. The context of linear programming is crucial because Farkas' Lemma is a cornerstone of the theory, used extensively in proving other important results, such as the strong duality theorem and the separation theorem.</DESCRIPTION>

