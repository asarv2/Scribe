# 18

# Work on a specific example

Hence, the four data points can be written as:

```
(y₁
y₂
y₃
y₄) = (1  V₁  W₁)  (β₀
                     (1  V₂  W₂)  (β₁)
                     (1  V₃  W₃)  (β₂)
                     (1  V₄  W₄)  (β₂)
```

This can be written in matrix form as:

```
y = Xβ
```

where:

```
y =  [ y₁ ]
     [ y₂ ]
     [ y₃ ]
     [ y₄ ]

X =  [ 1  V₁  W₁ ]
     [ 1  V₂  W₂ ]
     [ 1  V₃  W₃ ]
     [ 1  V₄  W₄ ]

β = [ β₀ ]
     [ β₁ ]
     [ β₂ ]
```

To find the optimal β, we need to solve:

```
β* = (XᵀX)⁻¹Xᵀy
```

| Car | Model | Volume (V) | Weight (W) | CO₂ |
|---|---|---|---|---|
| Toyota | Aygo | 1000 | 790 | 99 |
| Mitsubishi | Space Star | 1200 | 1160 | 95 |
| Skoda | Citigo | 1000 | 929 | 95 |
| Fiat |  | 500 | 900 | 90 |


**Description of the Figure:**

This slide continues the discussion of multiple linear regression, focusing on a specific example.  The top portion of the slide presents the mathematical formulation of the problem, showing how to represent the relationship between CO₂ emissions (dependent variable) and car volume (V<sub>i</sub>) and weight (W<sub>i</sub>) (independent variables) using matrix notation.  The equations are presented in a clear and concise manner, making the relationships between the variables explicit.  The equations are followed by a table.  The table displays data for different car models, including their volume, weight, and CO₂ emissions.  The table is positioned below the mathematical equations, suggesting a connection between the theoretical framework and the practical application of the model.  The table is formatted as a standard table, with rows representing different car models and columns representing the car's characteristics.  The table is aligned with the mathematical equations, suggesting a relationship between the theoretical framework and the practical application of the model.  The table is a key part of the slide, as it provides a concrete example of the type of data that might be used in a multiple linear regression model.  The table is formatted as a standard table, with rows representing different car models and columns representing the car's characteristics.  The table is aligned with the mathematical equations, suggesting a relationship between the theoretical framework and the practical application of the model.  The table is a key part of the slide, as it provides a concrete example of the type of data that might be used in a multiple linear regression model.


**Context:**

This slide is part of a lecture on AI Basics, specifically focusing on linear regression.  The slide builds upon the previous discussion of multiple linear regression by providing a concrete example of how to represent the data in a matrix form suitable for computation.  The table provides the data that will be used in the example.  The slide is a crucial part of the lecture, as it helps to illustrate the practical application of the mathematical concepts introduced earlier.  The slide is a continuation of the previous slide's discussion of multiple linear regression, focusing on a specific example.  The equations and table are designed to illustrate the process of representing the data in a matrix form suitable for computation.
