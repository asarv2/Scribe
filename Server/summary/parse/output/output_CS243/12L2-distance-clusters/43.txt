# 43

**Evaluation Approach: Objective**

Evaluate the clusters using known class labels

If you have class labels why cluster?
* The labeled dataset is usually hand-labeled specifically for evaluation
* Cluster the larger dataset automatically
* Assess the clustering of the hand labeled dataset in the automatic clustering


---

**Description of what you see:**

This slide, likely part of a lecture on clustering algorithms, discusses an objective approach to evaluating clustering results.  The text describes how to evaluate clusters when you already have known class labels for the data.  The content explains that if you have pre-labeled data, why would you cluster it?  The answer is that you would cluster a larger dataset automatically and then assess the clustering of the hand-labeled dataset in the automatic clustering.  The slide is likely part of a larger section on evaluating clustering results, where objective evaluation methods are discussed.  The title "Evaluation Approach: Objective" indicates that the slide is part of a discussion on how to objectively assess the quality of the clustering.  The text implies that the objective evaluation will use the known class labels to compare with the clusters produced by the algorithm.


