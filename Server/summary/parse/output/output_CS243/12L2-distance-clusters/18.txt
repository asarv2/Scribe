# 18

**Example with matplotlib**

```python
import numpy as np
import matplotlib
import matplotlib.pyplot as plt
from scipy.spatial import distance_matrix
xyLocations = [[random.randrange(0,10), random.gauss(5,3)] for i in range(25)]
xyLocs = pd.DataFrame(xyLocations)
distances = distance_matrix(xyLocs.values,xyLocs.values)
print(distances[:5,:5])
[[0.         4.95848922 5.50745126 2.31171825 4.68541685]
 [4.95848922 0.         1.96229046 2.67138844 0.4903888 ]
 [5.50745126 1.96229046 0.         3.600969   2.3972855 ]
 [2.31171825 2.67138844 3.600969   0.         2.37484206]
 [4.68541685 0.4903888  2.3972855  2.37484206 0.        ]]
plt.imshow(distances)
plt.show()
```

---

**Description of what you see:**

The slide displays Python code, which is used to generate a heatmap visualization of distances between data points.  The code imports necessary libraries (`numpy`, `matplotlib`, and `scipy.spatial`).  It creates a set of 25 data points (`xyLocations`) with x-coordinates randomly selected between 0 and 10 and y-coordinates drawn from a Gaussian distribution centered at 5 with a standard deviation of 3.  These points are then stored in a Pandas DataFrame (`xyLocs`).  The code calculates the distance matrix (`distances`) using `distance_matrix` from `scipy.spatial`, showing the Euclidean distance between each pair of points.  A snippet of the calculated distance matrix is printed, showing the distances between the first five data points.  Finally, the code uses `plt.imshow` to display the entire distance matrix as a heatmap, where colors represent the distances.  The `plt.show()` command displays the generated heatmap.

The heatmap itself is a square grid of colored cells.  The colors range from dark blue (representing small distances) to yellow and orange (representing larger distances).  The heatmap's arrangement visually represents the distances between the data points, with darker blue cells indicating closer proximity and warmer colors indicating greater distances.  The code and heatmap are designed to illustrate how distance matrices can be used to visualize the relationships between data points in a clustering context.  The code example is a practical demonstration of how to calculate and display distance matrices using Python libraries.
