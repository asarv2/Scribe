# Slide 9

**Structural Recursion: a Binary Tree Example**

This example is a binary search tree

* Each Node has a value and two children (Left and Right)
* The Left Child is less than the parent
* The Right Child is greater than the parent

**(Diagram):**

A binary search tree is presented.  The root node is at the top, labeled "36".  The tree branches downward, with each node having a value (e.g., 10, 78, 3, 21, etc.) and two child nodes (left and right).  The left child of a node always has a value less than the parent node, and the right child always has a value greater than the parent node.  The nodes are rectangular boxes, filled with a light-blue color, and contain numerical values.  Arrows connect the nodes, indicating the parent-child relationships within the tree structure.  The tree is oriented vertically, with the root node at the top and the leaf nodes at the bottom.  The diagram is centrally located on the slide and is oriented vertically, with the root node at the top and the leaf nodes at the bottom.  The diagram visually represents a binary search tree, illustrating the structural properties of the tree and the relationships between parent and child nodes.


**Description:**

This slide, part of a lecture on AI Basics, illustrates structural recursion using a binary search tree.  The diagram shows a specific binary search tree with numerical values assigned to each node.  The text explains the properties of a binary search tree: each node has a value, and its left child has a smaller value, while its right child has a larger value.  The diagram is crucial for understanding how recursion can be applied to traverse and process the data within the tree structure.  The slide is designed to illustrate the concept of structural recursion in the context of binary search trees.
