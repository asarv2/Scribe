# 21

**The derivative**

```
d
dx σ(x) = d
dx (
1
1 + e−x ) = d
dx (1 + e−x)−1
= −(1 + e−x)−2 ⋅d
dx(1 + e−x)
= −(1 + e−x)−2 ⋅(
d
dx (1) + d
dx (e−x))
= −(1 + e−x)−2 ⋅e−x ⋅d
dx (−x)
= −(1 + e−x)−2 ⋅(−e−x)
=
e−x
(1 + e−x)2
https://sebastianraschka.com/faq/docs/logisic-sigmoid-derivative.html
```

Rewritten with negative exponents. Apply the chain rule.
Apply the sum rule
Chain rule again


**Description:**

This slide presents a mathematical derivation of the derivative of the sigmoid function.  The formula for the sigmoid function's derivative is shown, along with the steps used to derive it.  The steps clearly show the application of the chain rule and the sum rule of differentiation.  The final result is the derivative of the sigmoid function, expressed as a fraction with `e<sup>-x</sup>` in the numerator and `(1 + e<sup>-x</sup>)<sup>2</sup>` in the denominator.  The URL at the bottom of the slide is a hyperlink to an external website, likely a resource providing further information or context about the sigmoid function and its derivative.  The slide is entirely text-based, with no figures other than the mathematical expressions.  The purpose is to demonstrate the calculation of the derivative of the sigmoid function, a crucial concept in the context of AI, particularly in neural networks.  The steps are clearly labeled with explanations, making the derivation easy to follow.  The URL is a reference to an external resource, likely a website with more details on the derivation or the sigmoid function itself.
