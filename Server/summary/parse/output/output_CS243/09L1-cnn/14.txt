# 14

# Pooling layer

224x224x64
112x112x64
pool
1
1
2
4
X
5
6
7
8
max pool with 2x2 filters
and stride 2
6
8
3
2
1
0
3
4
1
2
y
224
112
downsampling
112
224

Pooling layer downsamples the volume spatially, independently in each depth slice of the input volume. Left: In this example, the input volume of size [224x224x64] is pooled with filter size 2, stride 2 into output volume of size [112x112x64]. Notice that the volume depth is preserved. Right: The most common downsampling operation is max, giving rise to max pooling, here shown with a stride of 2. That is, each max is taken over 4 numbers (little 2x2 square).


**Description of the slide:**

This slide explains the pooling layer in convolutional neural networks (CNNs).

* **Title:** "Pooling layer" is displayed at the top, indicating the topic of the slide.

* **Visuals:** The slide contains a diagram illustrating the pooling operation.  On the left, a 3D block (224x224x64) is shown, representing an input volume.  A slice through this block is highlighted, showing a 224x224 grid.  An arrow points from this input volume to a smaller 3D block (112x112x64), representing the output volume.  The text "downsampling" is written on the arrow, indicating the reduction in size.  The text "pool" is written on the arrow, indicating the pooling operation.  The right side of the slide shows a 2x2 grid (4 numbers) with the numbers 6, 8, 3, and 4.  This represents the output of a single depth slice of the pooling operation.  The numbers in the 2x2 grid are the maximum values from a 2x2 region in the input slice.  The numbers 1, 1, 2, 4, 5, 6, 7, 8, 3, 2, 1, 0, 1, 2, 3, and 4 are arranged in a 4x4 grid, representing a single depth slice of the input volume.  The numbers 6, 8, 3, and 4 are the maximum values from the 2x2 regions in the input slice.  The x and y axes are labeled, indicating the spatial dimensions.  The numbers in the 4x4 grid are colored differently, making it easy to visually identify the maximum values in each 2x2 region.

* **Text:** The text below the diagram explains the pooling operation.  It states that the pooling layer downsamples the input volume spatially, independently in each depth slice.  The example shows a 224x224x64 input volume being downsampled to a 112x112x64 output volume using a 2x2 filter and a stride of 2.  The text also explains that the most common downsampling operation is max pooling, where the maximum value in each 2x2 region is selected as the output value.


**Overall:** The slide provides a visual and textual explanation of the pooling layer in a CNN, demonstrating how it reduces the spatial dimensions of the input volume while preserving the depth dimension.  The example clearly illustrates the max pooling operation.
