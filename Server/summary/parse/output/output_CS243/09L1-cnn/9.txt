# 9

# Multiple Convolutions

"Feature Map" usually has multiple layers

* An image has R, G, B layers, or "channels"
One layer has many convolution filters, which create a multichannel output map

**Input feature map**

![Input feature map](https://i.imgur.com/a700000.png)

**Filter 0**

![Filter 0](https://i.imgur.com/b700000.png)

**Output feature map 0**

![Output feature map 0](https://i.imgur.com/c700000.png)

**Filter 1**

![Filter 1](https://i.imgur.com/d700000.png)

**Output feature map 1**

![Output feature map 1](https://i.imgur.com/e700000.png)


**Description:**

This slide, part of a lecture on AI Basics, illustrates the concept of multiple convolutions in a convolutional neural network (CNN).  It shows how multiple filters are applied to an input feature map to produce multiple output feature maps.

* **Input feature map:** A light-gray square grid is shown, representing the input data.  This is a 3x3 grid, but the image is a simplified representation of a larger input feature map.  The input feature map is likely a portion of an image, or a feature map from a previous layer in the network.  The input feature map is positioned on the left side of the slide.

* **Filter 0:** A dark-blue square grid is shown, stacked vertically above the input feature map.  This is a 3x3 filter, and the numbers in the grid represent the weights used in the convolution operation.  The filter is positioned above the input feature map.  The filter is colored dark blue, and it is stacked vertically above the input feature map.  This filter is used to extract features from the input image.

* **Output feature map 0:** A bright-orange square grid is shown, positioned to the right of the input feature map.  This is the output of applying Filter 0 to the input feature map.  The output feature map is smaller than the input feature map, which is a common characteristic of convolution layers.  The output feature map is colored bright orange.

* **Filter 1:** A light-red square grid is shown, stacked vertically above the input feature map.  This is a 3x3 filter, and the numbers in the grid represent the weights used in the convolution operation.  The filter is positioned above the input feature map.  The filter is colored light red, and it is stacked vertically above the input feature map.  This filter is used to extract features from the input image.

* **Output feature map 1:** A bright-yellow square grid is shown, positioned to the right of the input feature map.  This is the output of applying Filter 1 to the input feature map.  The output feature map is smaller than the input feature map, which is a common characteristic of convolution layers.  The output feature map is colored bright yellow.

* **Arrows:** An arrow is shown pointing from the input feature map to the output feature maps.  This arrow visually connects the input to the outputs.

The overall purpose of the slide is to demonstrate how multiple filters can be applied to an input feature map to produce multiple output feature maps, highlighting the concept of feature extraction in CNNs.  The different colors used for the filters and output maps help distinguish the different operations.
