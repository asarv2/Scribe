# 11

**Training/test cycle**

*   **Sample Data**

    *   **Test Data**

        *   Apply model to test data (using chosen θ and hyperparameters)
        *   Evaluation function (applied to test data)
            *   1/N Σ<sub>i=1</sub><sup>N</sup> I(y<sub>i</sub> == f(x<sub>i</sub>))


**Description:**

This slide, part of a lecture on AI Basics, focuses on the final stage of the training/test cycle, specifically the evaluation of a model on test data.  A light gray rectangle, labeled "Sample Data," is positioned on the left side of the slide.  A larger, darker green rectangle, labeled "Test Data," is positioned to the right of the "Sample Data" box.  This visual representation emphasizes that the "Test Data" is a subset of the "Sample Data."  Within the "Test Data" box, there are two key concepts:

1.  **Apply model to test data (using chosen θ and hyperparameters):** This text indicates that the trained model, with its determined parameters (θ) and hyperparameters, is applied to the test data to generate predictions.

2.  **Evaluation function (applied to test data):** This text indicates that an evaluation function is applied to the predictions generated by the model on the test data. The formula 1/N Σ<sub>i=1</sub><sup>N</sup> I(y<sub>i</sub> == f(x<sub>i</sub>)) is provided. This is likely an evaluation function that measures the accuracy of the model on the test data.  The function calculates the average indicator function, which is 1 if the predicted value (f(x<sub>i</sub>)) matches the actual value (y<sub>i</sub>) and 0 otherwise.

The slide's layout and content clearly illustrate the final step in the training/test cycle, where the model's performance is evaluated on unseen test data.  The slide is likely to be followed by a discussion of how the evaluation results are interpreted and used to assess the model's generalization ability.  The use of the test data for final evaluation is crucial to determine how well the model will perform on new, unseen data.
