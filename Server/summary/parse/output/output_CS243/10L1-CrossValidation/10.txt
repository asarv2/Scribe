# 10

**Training/test cycle**

*   **Sample Data**

    *   **Training Data**
    *   **Held Out Data**

        *   Estimate model hyperparameters from validation data
        *   Evaluation function (applied to validation data)
            *   1/N Σ<sub>i=1</sub><sup>N</sup> I(y<sub>i</sub> == f(x<sub>i</sub>))


**Description:**

This slide, part of a lecture on AI Basics, continues the visual representation of the training/test cycle.  A light gray rectangle, labeled "Sample Data," is positioned on the left side of the slide.  A darker teal rectangle, labeled "Training Data," is positioned to the right of the "Sample Data" box.  A burnt orange rectangle, labeled "Held Out Data," is positioned to the right of the "Training Data" box.  The "Held Out Data" box is smaller than the "Training Data" box, visually emphasizing that the "Held Out Data" is a subset of the "Training Data."  Within the "Held Out Data" box, there are two key concepts:

1.  **Estimate model hyperparameters from validation data:** This text indicates that the model's hyperparameters are determined using the held-out data.  This is a crucial step in model tuning, as hyperparameters are not learned directly from the data but are set by the user.

2.  **Evaluation function (applied to validation data):** This text indicates that an evaluation function is applied to the held-out data. The formula 1/N Σ<sub>i=1</sub><sup>N</sup> I(y<sub>i</sub> == f(x<sub>i</sub>)) is provided. This is likely an evaluation function that measures the accuracy of the model on the held-out data.  The function calculates the average indicator function, which is 1 if the predicted value (f(x<sub>i</sub>)) matches the actual value (y<sub>i</sub>) and 0 otherwise.

The slide's layout and content clearly illustrate the process of using held-out data to tune model hyperparameters and evaluate the model's performance on that data.  The slide is likely to be followed by a discussion of how to use the evaluation function to assess the model's performance and potentially adjust the model hyperparameters.  The use of the held-out data for hyperparameter tuning is a crucial step in model development, as it allows for the selection of the best hyperparameters for the given data.
