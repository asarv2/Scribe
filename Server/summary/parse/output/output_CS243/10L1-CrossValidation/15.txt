# 15

**Fit a cubic function with a degree-9 polynomial**

*   **Figures:**
    *   **Three graphs:** The slide displays three separate graphs, each illustrating a different degree polynomial fit to a set of data points.  All graphs share the same x and y axes, labeled "x" and "t" respectively.  The x-axis ranges from approximately 0 to 1, and the y-axis ranges from approximately -1 to 1.  Each graph shows a set of scattered blue circles, representing data points.  A red line is plotted over the data points in each graph, representing the fitted polynomial.  A green line is also plotted over the data points in each graph, representing a simpler polynomial fit.

        *   **Graph 1 (M = 1):** This graph shows a simple linear (degree 1) fit to the data points. The red line is a straight line, and the green line is a slightly curved line that follows the data points more closely than the red line.

        *   **Graph 2 (M = 3):** This graph shows a cubic (degree 3) fit to the data points. The red line is a more complex curve, and the green line is a simpler curve that follows the data points more closely than the red line.

        *   **Graph 3 (M = 9):** This graph shows a 9th-degree polynomial fit to the data points. The red line is a highly complex curve, oscillating significantly to closely follow the data points. The green line is a simpler curve that follows the data points more closely than the red line.

    *   **Labels:** Each graph is labeled with the value of "M," which likely represents the degree of the polynomial fit.  The labels are positioned above the respective graphs.

**Description:**

This slide demonstrates the concept of overfitting in polynomial curve fitting.  The graphs show how increasing the degree of the polynomial (M) can lead to a model that fits the training data very closely but performs poorly on unseen data.  The green lines represent a simpler, less complex fit that likely generalizes better to unseen data.  The red lines represent increasingly complex fits that overfit the training data, resulting in a poor fit to unseen data.  The slide is likely to be followed by a discussion of how to choose an appropriate model complexity to avoid overfitting. The blue circles are the data points, and the red and green lines are the fitted polynomials of different degrees. The different degrees of the polynomials are represented by the values of M. The slide is likely to be followed by a discussion of how to choose an appropriate model complexity to avoid overfitting.
