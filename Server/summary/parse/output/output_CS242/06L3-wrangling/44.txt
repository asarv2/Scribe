# Slide 44

**Lets return to Grades**

We filled in our missing data, but that doesn't recalculate our grades
```
df_grades
```

A table is presented, displaying data read from a CSV file.  The table has columns for "Lastname", "Firstname", "ID", "Test1", "Test2", "Test3", "Test4", and "Final Grade".  Each row represents a student's record, including their name, ID, and test scores.  The data appears to be student grades.

```
To do a meaningful average
df_grades.Test1 = (df_grades.Test1/50) * 100
```

These lines provide context.  The text "To do a meaningful average" suggests that the slide is discussing how to calculate a meaningful average for the test scores.  The code snippet shows how to adjust the Test1 scores to be out of 100.

```
Now calculate the average
df_grades['Grade'] = df_grades.loc[:, ['Test1', 'Test2', 'Test3', 'Test4', 'Final']].mean(axis=1)
```

This line of code calculates the average of the test scores (Test1, Test2, Test3, Test4, and Final) for each student and stores the result in a new column named 'Grade'.

```
For simpler slides
df_final = df_grades.filter(['Lastname', 'Firstname', 'ID', 'Grade'])
```

This line of code filters the `df_grades` DataFrame to include only the specified columns ('Lastname', 'Firstname', 'ID', and 'Grade') and assigns the result to a new DataFrame called `df_final`.

```
But how do we do categorical grades and letter grades?
```

This line indicates that the slide will likely discuss how to convert numerical grades to letter grades (e.g., A, B, C).


---

**Description of the Slide:**

This slide revisits the student grade data, now with the goal of calculating a meaningful average grade and potentially converting it to letter grades.  The slide shows code snippets for calculating the average grade and filtering the DataFrame to include only relevant columns.  The text prompts the question of how to handle categorical grades, suggesting the next steps in the data analysis process.  The slide is part of a larger lecture on data cleaning and manipulation techniques, specifically focusing on student grade data.  The tables are crucial for demonstrating the data and the results of the calculations. The code snippets are essential for demonstrating the data analysis process.
