# Slide 6

**How to specify pattern to match**

There are also special escape characters for categorical groups of characters

`\d`  This matches any digit [0-9]
`\D`  This matches non-digit characters [^0-9]
`\s`  This matches whitespace character [ \t\n\r\f\v]
`\S`  This matches non-whitespace character [^ \t\n\r\f\v]
`\w`  This matches alphanumeric character [a-zA-Z0-9_]
`\W`  This matches any non-alphanumeric character [^a-zA-Z0-9]


---

**Description:**

This slide, part of a lecture on Intro to Data Science, continues the discussion of regular expressions (RegEx). It provides a table-like presentation of special escape characters used in RegEx to match specific categories of characters.  Each line shows a special escape character (`\d`, `\D`, `\s`, `\S`, `\w`, `\W`) followed by a description of what it matches.  The descriptions include the corresponding character sets in square brackets, which define the specific characters that the escape character will match.  For example, `\d` matches any digit (0-9), while `\D` matches any character that is *not* a digit.  This slide is crucial for understanding how to use RegEx to match specific categories of characters, such as digits, whitespace, or alphanumeric characters, in data processing tasks.  The format is clear and concise, making it easy to understand the different escape characters and their corresponding matches.  The slide builds upon the previous slides, which introduced the basic concepts of RegEx and how to specify patterns.
