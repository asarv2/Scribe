# Slide 19

```markdown
Beautiful Soup: Parse Paragraphs
Data Science from Scratch, Ch 9

# find all paragraph text without embedded links
paragraphs = soup.find_all('p')
for p in paragraphs:
    if not p.find('a'):  # as in <a href ...
        pText = p.get_text()
        print(pText)
Find Info For
Quick Links
Professors Xu, Bianchi, Tian and researchers at Purdue Computer Science
recently discovered a vulnerability that affects many IoT devices running
Bluetooth Low Energy (BLE). The research centers around the routine
reconnection process of BLE devices. The researchers conclude that their
discovery implies that this vulnerability can potentially affect more
than 1 billion BLE devices and 16,000 BLE apps.
```

**Description:**

This slide presents Python code and its intended output related to web scraping using the Beautiful Soup library.  The code snippet aims to extract paragraph text from a webpage, specifically excluding paragraphs that contain embedded links (`<a>` tags).  The code iterates through all paragraphs (`<p>`) found on the page.  If a paragraph does *not* contain an `<a>` tag (checked by `if not p.find('a')`), the paragraph's text content is extracted using `p.get_text()` and printed.  The output shows the extracted paragraph text, which in this case, is a description of a research finding about a vulnerability in Bluetooth Low Energy (BLE) devices.  The text is formatted as a paragraph, with the expected structure of a news article or research summary.  The presence of "Find Info For" and "Quick Links" suggests that the code is likely being used to parse a webpage from a university website, possibly Purdue University, and extract information about research findings.  The code is designed to extract the text content of paragraphs, excluding any links that might be present within those paragraphs.
