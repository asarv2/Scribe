# Slide 28

**Histograms (1D)**

Most common plot for univariate data
Split data range into equal-sized bins, count number
of data points that fall into each bin
Graphically shows:
* Center (location)
* Spread (scale)
* Skew
* Outliers
* Multiple modes

**(Figure Description):**

A histogram is displayed, showing the distribution of "Sepal Length" values.  The horizontal axis is labeled "Sepal Length" and ranges from approximately 4.0 to 8.0.  The vertical axis represents the count of data points falling within each bin.  The histogram consists of several rectangular bars, each representing a bin of equal width along the horizontal axis.  The height of each bar corresponds to the number of data points within that bin.  The histogram visually displays the frequency distribution of sepal lengths in the dataset.  The plot is positioned in the right-hand portion of the slide.  The title "Histogram of Sepal Length" is above the histogram.


**Context:**

This slide, part of a presentation on data visualization techniques in an Intro to Data Science course, introduces histograms as a way to visualize the distribution of a single variable (univariate data).  The histogram, specifically of sepal length, is used to illustrate how histograms can show the center, spread, skewness, outliers, and multiple modes of a dataset.  The slide is likely part of a larger discussion on exploratory data analysis (EDA) techniques.


**Description of what you see:**

The slide shows a histogram of sepal length values.  The x-axis represents the range of sepal lengths, and the y-axis represents the frequency (count) of data points within each bin.  The histogram visually displays the distribution of sepal lengths, showing the frequency of different sepal length values.  The histogram is a common way to visualize the distribution of a single variable in a dataset.  The title "Histogram of Sepal Length" above the plot indicates the variable being visualized.  The plot is positioned in the right-hand portion of the slide, with the descriptive text on the left.
