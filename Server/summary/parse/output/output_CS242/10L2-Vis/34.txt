# Slide 34

**Stacking the bars**

```python
categories = iris.category.unique()
c = ["#00A5FC","#007979","#008800","#215419"]
byCategory = {x:iris[iris.category==x] for x in categories}
swMean = [byCategory[y]['sepal-width'].mean() for y in byCategory.keys()]
slMean = [byCategory[y]['sepal-length'].mean() for y in byCategory.keys()]
pwMean = [byCategory[y]['petal-width'].mean() for y in byCategory.keys()]
plMean = [byCategory[y]['petal-length'].mean() for y in byCategory.keys()]
display(plMean)
[1.464, 4.26, 5.552]
plPlot = plt.bar(categories, plMean, color=c[0])
slPlot = plt.bar(categories, slMean, color=c[2], bottom=plMean)
plt.ylabel('Length')
plt.title('Combined lengths of petal and sepals')
# NOTE: to match the color order, botttom is last in the list
plt.legend((slPlot[0],plPlot[0]), ("Sepal Length", "Petal Length",))
plt.show()
```

**(Figure Description):**

A bar plot is displayed, showing the average sepal length and petal length for each of the three iris species (Iris-setosa, Iris-versicolor, and Iris-virginica).  The plot is a stacked bar chart, with the bars stacked vertically.  The x-axis represents the different iris species.  The y-axis represents the length values, ranging from approximately 0 to 12.  The bars are stacked in such a way that the height of each bar represents the sum of the average sepal length and petal length for that species.  The bars are colored differently, with the bottom portion of each bar representing the average petal length (in a shade of light blue) and the top portion representing the average sepal length (in a dark green).  A legend is present, clearly labeling the two components of the stacked bars: "Sepal Length" and "Petal Length."  The plot is positioned centrally on the slide.


**Context:**

This slide builds upon previous discussions of data visualization techniques, specifically histograms and bar plots.  The code calculates the average sepal length and petal length for each iris species and then creates a stacked bar chart to visually compare these averages.  The stacked bar chart allows for a direct comparison of the combined lengths of sepal and petal for each species.  The slide is likely part of a larger presentation on descriptive statistics and data visualization using the Iris dataset.


**Description of what you see:**

The slide shows a stacked bar chart and the Python code used to generate it.  The plot displays three stacked bars, each corresponding to a different iris species.  The height of each bar represents the combined average sepal length and petal length for that species.  The bottom portion of each bar represents the average petal length, and the top portion represents the average sepal length.  The code calculates the average values and then uses Matplotlib to create the stacked bar plot.  The plot is a visual representation of the combined average lengths for each iris species.  The code is clear and concise, demonstrating the steps involved in creating the stacked bar plot.  The legend makes it easy to distinguish between the two components of the stacked bars.
