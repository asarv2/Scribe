# Slide 62

**Visual Estimates**

Scale of elementary perceptual tasks, inspired by William Cleveland and Robert McGill

*   User performs one or more of the associated mental-visual tasks to extract values represented in the plot

*   To create a successful chart, construct it based on tasks as high in the hierarchy as possible

**(Figure Description):**

The slide displays a collection of small visualizations, arranged in a grid-like format.  These visualizations represent different ways to visually encode data, demonstrating the principles of visual perception and estimation.  The visualizations are categorized and labeled, indicating the type of visual encoding used.

* **Top Row, Left:**  A series of vertical bars, each with a different height.  The bars are positioned along a horizontal axis, and the height of each bar represents a value.  This is an example of encoding data using length.

* **Top Row, Middle:**  A series of lines with varying slopes.  The lines are positioned on a two-dimensional plane, and the slope of each line represents a value.  This is an example of encoding data using direction/slope.

* **Top Row, Right:**  A series of lines with varying angles.  The lines are positioned on a two-dimensional plane, and the angle of each line represents a value.  This is an example of encoding data using angle.

* **Second Row, Left:**  A series of circles with varying areas.  The circles are positioned on a two-dimensional plane, and the area of each circle represents a value.  This is an example of encoding data using area.

* **Second Row, Middle:**  A series of 3D shapes (cubes) with varying volumes.  The shapes are positioned on a two-dimensional plane, and the volume of each shape represents a value.  This is an example of encoding data using volume.

* **Second Row, Right:**  A series of colored rectangles with varying shades.  The shades of gray represent a range of values.  This is an example of encoding data using shading and saturation.

* **Third Row, Left:**  A series of colored rectangles with varying hues.  The hues represent a range of values.  This is an example of encoding data using color hue.


**Context:**

This slide, likely part of a data visualization lecture, introduces the concept of visual estimates.  It shows how different visual properties (length, angle, area, volume, shading, color) can be used to encode data values.  The examples illustrate the principles of visual perception and how effective visualizations can facilitate the extraction of information from data.  The slide is likely to follow a discussion of the importance of choosing appropriate visual encodings for effective data communication.


**Description of what you see:**

The slide shows a grid of small visualizations, each demonstrating a different way to encode data using visual properties.  The visualizations are labeled with the type of encoding used (length, direction/slope, angle, area, volume, shading, color hue).  The slide is designed to illustrate the principles of visual perception and how these principles can be applied to create effective data visualizations.  The visualizations are small and arranged in a grid, making it easy to compare the different encoding methods.  The slide is likely part of a larger discussion on data visualization techniques.


