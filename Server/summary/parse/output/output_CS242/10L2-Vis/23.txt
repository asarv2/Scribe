# Slide 23

**Is this limited to scatter plots?**

No! Consider many of the data sets in Anscombe look like lines. 
Lets consider a line plot
```python
x2sorted = data.sort_values(by=['x2'])
plt.plot(x2sorted['x2'],x2sorted['y2'])
```
We have to sort our values in the order we want the line to proceed. 
Unsorted out line will jump around.
```python
plt.plot(X.x2,Y.y2)
```

**(Figure Description):**

Two figures are displayed, positioned vertically.

* **Top Figure:** A line plot is shown, depicting a smooth, slightly curved line. The x-axis ranges from approximately 4 to 14, and the y-axis ranges from approximately 3 to 9. The line plot appears to represent a relationship between two variables, where the y-value increases as the x-value increases, but not in a perfectly linear fashion. The curve suggests a non-linear relationship.

* **Bottom Figure:** A line plot is shown, depicting a series of somewhat jagged lines. The x-axis ranges from approximately 4 to 14, and the y-axis ranges from approximately 3 to 9. The lines appear to represent a relationship between two variables, but the lines are not smooth and appear to jump around, suggesting that the data points are not sorted in the order needed to create a smooth line plot.


**Context:**

This slide follows a discussion of scatter plots and their limitations, particularly when dealing with high-density data.  It introduces the concept of line plots as an alternative visualization technique, demonstrating that sorting the data points by the x-values is crucial for creating a meaningful line plot.  The two plots side-by-side visually illustrate the difference between a sorted and an unsorted line plot, highlighting the importance of data ordering for line plots.  The slide is likely part of a larger discussion on data visualization techniques and how to choose the appropriate visualization method based on the characteristics of the data.


**Description of what you see:**

The slide shows two line plots.  The top plot shows a smooth, continuous line, suggesting a clear relationship between the variables.  The bottom plot shows a series of jagged lines, indicating that the data points are not ordered correctly, which results in a less meaningful visualization.  The slide is illustrating the importance of data ordering when creating line plots.  The plots are positioned vertically, one above the other, for comparison.  The x-axis and y-axis are labeled, but the specific variables are not shown.  The plots are a visual representation of the difference between a sorted and an unsorted line plot.
