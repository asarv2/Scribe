# Slide 18

**Figures and Subplots**

```python
import pandas as pd
import matplotlib.pyplot as plt
%matplotlib inline
X = data[['x1','x2','x3','x4']]
Y = data[['y1','y2','y3','y4']]
for i in range(0,4):
    fig, ax = plt.subplots()
    ax.scatter(X.iloc[:,i],Y.iloc[:,i], marker = mark[i], color=colours[i])
    ax.set_title("Set "+str(i+1))
    ax.set_xlim(0,20)
    ax.set_ylim(0,15)
fig, ax = plt.subplots(nrows=2, ncols=2, sharex=True, sharey=True)
for i in range(0,4):
    sp = ax[i//2, i%2]
    sp.scatter(X.iloc[:,i],Y.iloc[:,i], marker = mark[i], color=colours[i])
    sp.set_title("Set "+str(i+1))
fig.tight_layout()
```

**Figure Description:**

The figure displays a grid of four scatter plots, each representing a dataset (Set 1, Set 2, Set 3, and Set 4) from Anscombe's Quartet.  These plots are arranged in a 2x2 grid, with each subplot showing the relationship between the x and y values for a specific dataset.  The plots are positioned in the lower portion of the slide.  Above the grid of plots, there are four additional scatter plots, each in its own figure, also representing the same datasets.  These plots are positioned above the 2x2 grid.  The plots in the upper section are arranged in a way that allows for easy comparison of the four datasets.  The x-axis and y-axis are labeled, and each plot has a distinct set of data points.  The plots are colored differently, and each is labeled with a legend entry.  The legend makes it easy to distinguish between the different datasets.  The plots are arranged in a way that allows for easy comparison of the four datasets.  The x-axis and y-axis are labeled, and each plot has a distinct set of data points.  The plots are colored differently, and each is labeled with a legend entry.  The legend makes it easy to distinguish between the different datasets.


**Context:**

This slide builds upon previous slides demonstrating Anscombe's Quartet, emphasizing the importance of visualization in data analysis.  The code and figure show how to create multiple subplots within a single figure using `plt.subplots` to visualize the four datasets side-by-side for easier comparison.  The use of `sharex=True` and `sharey=True` ensures that the x and y axes have the same limits across all subplots, facilitating a direct comparison of the data distributions.


**Description of what you see:**

The slide shows a combination of Python code and its output.  The code uses Matplotlib to create a grid of scatter plots, each visualizing a dataset from Anscombe's Quartet.  The output is a figure with four scatter plots arranged in a 2x2 grid.  The plots are colored differently, and each is labeled with a legend entry.  The code is designed to illustrate the use of `plt.subplots` for creating multiple subplots within a single figure, which is a common technique for visualizing multiple datasets or different aspects of a single dataset.  The slide is a continuation of the previous slides' discussion of Anscombe's Quartet and the importance of visualization in data analysis.  The figure is a visual representation of the code, showing the scatter plots for each dataset, with each plot clearly labeled and colored.  The legend makes it easy to distinguish between the different datasets.  The arrangement of the plots in a grid format is crucial for comparison.
