# Slide 66

**Baselines and scales of plots**

Should you always include 0 in the axis as a baseline?

**(Figure Description):**

The slide displays two side-by-side bar charts.

* **Left Chart (Non-zero baseline):**  This bar chart shows a set of bars, with heights representing percentages.  The bars are not aligned to a zero baseline; the lowest value shown on the y-axis is approximately 40%.  The bars are a light teal color.  The x-axis labels are not fully visible, but there are likely labels for different categories or groups.  The y-axis ranges from approximately 40% to 50%.

* **Right Chart (Zero baseline):** This bar chart shows a similar set of bars, also representing percentages.  The bars are aligned to a zero baseline on the y-axis, which ranges from 0% to 50%.  The bars are a light teal color.  The x-axis labels are not fully visible, but there are likely labels for different categories or groups.


**Context:**

This slide, likely part of a data visualization lecture, is comparing the use of different baselines (zero vs. non-zero) on a bar chart.  The purpose is to illustrate how the choice of baseline can affect the perception of the data.  The question "Should you always include 0 in the axis as a baseline?" prompts the audience to consider the implications of this choice.  The two charts are presented side-by-side to allow for a direct comparison of how the data is perceived with different baselines.


**Description of what you see:**

The slide shows two bar charts.  The left chart has a non-zero baseline, starting at approximately 40%.  The right chart has a zero baseline, starting at 0%.  Both charts display similar data, but the way the data is presented visually differs significantly due to the baseline choice.  The slide is likely part of a larger discussion on data visualization best practices, specifically focusing on the importance of choosing appropriate scales and baselines for effective data communication.  The question at the bottom prompts the audience to consider the implications of this choice.  The charts are positioned side-by-side for direct comparison.  The bars are a light teal color, and the y-axis represents percentages.  The x-axis likely represents categories or groups.


