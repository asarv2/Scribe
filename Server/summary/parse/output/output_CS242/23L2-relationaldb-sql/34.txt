# Slide 34

# Join Examples

```
SQL JOINS
A
B
SELECT <select_list>
FROM TableA A
LEFT JOIN TableB B
ON A.Key = B.Key
```

```
A
B
SELECT <select_list>
FROM TableA A
INNER JOIN TableB B
ON A.Key = B.Key
```

```
A
B
SELECT <select_list>
FROM TableA A
FULL OUTER JOIN TableB B
ON A.Key = B.Key
```

```
A
B
SELECT <select_list>
FROM TableA A
RIGHT JOIN TableB B
ON A.Key = B.Key
```

```
A
B
SELECT <select_list>
FROM TableA A
RIGHT JOIN TableB B
ON A.Key = B.Key
WHERE A.Key IS NULL
```

```
A
B
SELECT <select_list>
FROM TableA A
FULL OUTER JOIN TableB B
ON A.Key = B.Key
WHERE A.Key IS NULL
OR B.Key IS NULL
```

---

**Description:**

This slide presents various SQL join types, visually represented by overlapping circles (Venn diagrams).  Each diagram shows two sets, labeled "A" and "B," with the overlapping region representing the common elements.  The slide provides the SQL syntax for each join type, including `LEFT JOIN`, `INNER JOIN`, `FULL OUTER JOIN`, and `RIGHT JOIN`.  The SQL syntax includes the `SELECT`, `FROM`, and `JOIN` clauses, along with the `ON` clause specifying the join condition (`A.Key = B.Key`).  The slide also shows variations of the `RIGHT JOIN` and `FULL OUTER JOIN` that include `WHERE` clauses to filter out rows where the join key is null in one or both tables.  The purpose of this slide is to visually and conceptually illustrate the different types of SQL joins and their corresponding SQL syntax.


**Description of the Figure:**

The figure consists of multiple Venn diagrams, each showing two overlapping circles labeled "A" and "B."  These diagrams visually represent the different types of SQL joins.  The overlapping region in each diagram corresponds to the rows that satisfy the join condition.  The diagrams are arranged in a grid-like fashion, with each diagram representing a different join type.  The diagrams are positioned centrally on the slide, and the SQL syntax for each join type is presented below the corresponding diagram.  The diagrams are crucial for visually demonstrating the concept of joins and how they combine data from multiple tables based on common columns.  The different colors and shading in the diagrams help to distinguish the rows included in each join type, making the concept of joins more accessible.  The diagrams are well-suited to the context of the lecture, as they visually demonstrate the different join types and their results.  The diagrams are positioned to help the viewer understand the relationship between the tables and the join conditions.


