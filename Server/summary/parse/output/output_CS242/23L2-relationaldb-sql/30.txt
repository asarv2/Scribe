# Slide 29

# Example Join

Consider joining two tables

* Person and Grades (used in FROM clause)
* on ID / PersonID (used in WHERE clause)

| ID | Last Name | First Name | RoleID |
|---|---|---|---|
| 1 | Bergstrom | Anthony | 1 |
| 2 | Purdue | Peter | 2 |
| 3 | Doe | John | 3 |
| 4 | Smith | Jane | 3 |

| StudentID | HW1 | HW2 |
|---|---|---|
| 3 | 35 | 42 |
| 4 | 37 | 41 |
| 17 | 31 | 36 |
| 82 | 40 | 44 |


---

**Description of the Slide:**

This slide, part of a lecture on Introduction to Data Science, presents an example of joining two tables, likely `Person` and `Grades`.  The title "Example Join" indicates the topic.  The text explains that the tables are joined based on a common column, likely `ID` or `PersonID`.  Two tables are shown, one titled "Person" and the other titled "Grades".  The "Person" table contains columns for `ID`, `Last Name`, `First Name`, and `RoleID`.  The "Grades" table contains columns for `StudentID`, `HW1`, and `HW2`.  The data in both tables is structured in rows and columns, with each row representing a record and each column representing an attribute.  The purpose of this slide is to illustrate a practical example of how to join two tables based on a common column, likely for the purpose of combining information from both tables.  The tables are formatted with clear column headers and data entries, making the relationships between the tables easily visible.  The layout is designed to visually represent the concept of a join and its application in a data science context.  The example is likely used to demonstrate how to perform joins in SQL or a similar database query language.


**Description of the Figure:**

There is no explicit figure in the provided text.  The slide contains a table and a table-like structure, but these are not figures in the traditional sense.  The tables are part of the textual content of the slide, not a separate image.  The tables are formatted to visually represent the data from two tables, which are likely to be joined in a query.  The tables are used to illustrate the example of joining two tables, which is the topic of the slide.
