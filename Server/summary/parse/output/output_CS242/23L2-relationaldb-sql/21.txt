# Slide 21

# Inserting Data

Insert data into the table:
```
cursor.execute("INSERT INTO role VALUES (1, 'Instructor')")
cursor.execute("INSERT INTO role VALUES (2, 'TA')")
cursor.execute("INSERT INTO role VALUES (3, 'Student')")
conn.commit()
```
Delete items from a table:
Note: if you don't call commit, you can undo with conn.rollback()
```
cursor.execute("DELETE FROM role WHERE id == 3")
conn.commit()
```

---

**Description of the Slide:**

This slide presents Python code snippets for inserting and deleting data into a SQLite database table.  The code uses the `sqlite3` library and demonstrates how to insert new rows into the `role` table with specific values for `id` and `name`.  The `conn.commit()` function is used to save the changes to the database.  The slide also includes a `DELETE` statement to remove a row from the `role` table where the `id` is 3.  The code is formatted to show the structure of Python commands, with SQL commands embedded within string literals.  The purpose of this slide is to provide a practical example of how to insert and delete data in a SQLite database using Python.  There are no figures on this slide.  The content is purely textual, focusing on the code necessary for data manipulation in a database.


