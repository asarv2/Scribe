# Slide 60

**Translation to MapReduce**

The components of the jobs naturally translate into Pig Latin

```
Load Users
Filter by Age
Load Pages
Join on Names
Group on url
Count clicks
Order by clicks
Take top 5
```

```
Users    = load 'users' as (name, age);
Filtered = filter Users by age >= 18 and age <= 25;
Pages    = load 'pages' as (user, url);
Joined   = join Filtered by name, Pages by user;
Grouped  = group Joined by url;
Summed   = foreach Grouped generate group, count(Joined) as clicks;
Sorted   = order Summed by clicks desc;
Top5     = limit Sorted 5;
store Top5 into 'top5sites'
```

---

**Description of the Figure:**

The figure is a flowchart diagram.  It visually represents the translation of a data processing pipeline from a high-level description (Pig Latin) to a lower-level implementation (MapReduce).  The diagram shows three distinct processing stages (Job 1, Job 2, Job 3) connected by arrows.  Each stage contains rectangular boxes representing specific operations.  For example, "Load Users" and "Filter by Age" are in Job 1, "Join on Names" and "Group on url" are in Job 2, and "Count clicks," "Order by clicks," and "Take top 5" are in Job 3.  The boxes are visually distinct, and the arrows connecting them represent the flow of data between the stages.  The diagram is centrally located on the slide and is a key visual aid for understanding the mapping between the high-level Pig Latin operations and the lower-level MapReduce jobs.  The purpose of the diagram is to illustrate how the Pig Latin script can be translated into a series of MapReduce jobs.


**Description of the Slide:**

This slide shows a visual representation of how a Pig Latin script translates into a series of MapReduce jobs.  The top portion of the slide shows a flowchart diagram, which visually represents the steps involved in the data processing pipeline.  The bottom portion of the slide shows the corresponding Pig Latin script, which is a higher-level description of the same data processing pipeline.  The slide is likely part of a lecture or tutorial on Pig Latin and MapReduce, demonstrating how a high-level data processing language can be translated into a lower-level distributed computing framework.  The flowchart and Pig Latin script are presented together to illustrate the translation process.  The context suggests that the lecture is transitioning from high-level data processing languages to lower-level distributed computing frameworks.




