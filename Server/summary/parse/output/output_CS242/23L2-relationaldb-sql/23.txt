# Slide 23

# Structure of an SQL Query

```
SELECT, FROM, WHERE are keywords
Other words are names of table/columns
Terminate query with semicolon
SELECT: Specifies attributes of interest (note comma separators)
FROM: Specifies table the query applies to
WHERE: Specifies criterion for rows of interest
• For each row, condition "points >= 500" is true or false based on the points value in that row
• Only rows where condition is satisfied appear in result
```

---

**Description of the Slide:**

This slide details the structure of an SQL query.  It's a textual slide, listing the keywords (`SELECT`, `FROM`, `WHERE`) and their roles in an SQL query.  The slide also explains the general structure of an SQL query, including the use of other words (which are typically table or column names), and the semicolon to terminate the query.  The slide also provides a description of the `SELECT`, `FROM`, and `WHERE` clauses, and how the `WHERE` clause filters rows based on a condition (in this case, `points >= 500`).  The slide is formatted with a bulleted list, making the components of an SQL query easy to understand.  There are no figures on this slide.  The content is purely textual, providing a conceptual overview of SQL query structure.  The slide is likely part of a larger discussion on SQL queries and how to construct them.  The context suggests that the lecture is focused on the practical application of SQL in a data science setting.
