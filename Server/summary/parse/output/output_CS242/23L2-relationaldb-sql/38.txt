# Slide 38

# Outer Join

Return all rows from right and left join
Pandas way
`df_person.merge(df_grades, how="outer", left_on="id", right_on="person_id")`

Not directly supported in SQLite

| ID | Last Name | First Name | RoleID | HW1 | HW2 |
|---|---|---|---|---|---|
| 1 | Bergstrom | Anthony | 1 |  NULL | NULL |
| 2 | Purdue | Peter | 2 |  NULL | NULL |
| 3 | Doe | John | 3 | 35 | 42 |
| 4 | Smith | Jane | 3 | 37 | 41 |
| 17 |  NULL | NULL | NULL | 31 | 36 |
| 82 |  NULL | NULL | NULL | 40 | 44 |


---

**Description:**

This slide discusses the outer join operation in the context of data manipulation, likely within a data science course.  It shows how to perform an outer join using the Pandas library in Python.  The slide title, "Outer Join," clearly indicates the topic.  The text explains that an outer join returns all rows from both input tables, combining matching rows and including rows from either table that don't have a match in the other.  The Pandas code example demonstrates how to perform an outer join using the `.merge()` method, specifying `how="outer"`.  The `left_on` and `right_on` arguments specify the columns to join on from the respective DataFrames.  The slide also notes that outer joins are not directly supported in SQLite.  A table is included, showing the result of an outer join, where all rows from both the "Person" and "Grades" tables are included, with `NULL` values for columns where there's no match.  The purpose of this slide is to illustrate the outer join operation and its implementation in Pandas, highlighting the difference between inner, left, and right joins.


**Description of the Figure:**

There is no separate figure in the slide.  The slide contains a table, but this is part of the textual content, not a separate image.  The table is formatted to visually represent the data from two tables, which are likely to be joined in a query.  The table is used to illustrate the example of an outer join, which is the topic of the slide.  The formatting of the table, with clear column headers and data entries, makes the relationships between the tables easily visible.  The layout is designed to visually represent the concept of an outer join and its application in a data science context.  The example is likely used to demonstrate how to perform outer joins in SQL or a similar database query language.  The `NULL` values in the table clearly indicate rows where there's no match in the other table.
