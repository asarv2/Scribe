# Slide 59

**In Pig Latin**

```
Users = load 'users' as (name, age);
Filtered = filter Users by age >= 18 and age <= 25;
Pages = load 'pages' as (user, url);
Joined = join Filtered by name, Pages by user;
Grouped = group Joined by url;
Summed = foreach Grouped generate group, count(Joined) as clicks;
Sorted = order Summed by clicks desc;
Top5 = limit Sorted 5;
store Top5 into 'top5sites'
```

---

**Description:**

This slide presents a Pig Latin script for a data analysis task.  The script defines a series of steps to process data from two input files, 'users' and 'pages', to identify the top 5 most visited web pages by users aged 18-25.  Each line represents a transformation step in the Pig Latin pipeline.  The `load` operation reads data from files.  The `filter` operation selects data based on a condition (age).  The `join` operation combines data from different files based on a common key (user name).  The `group` operation groups data based on a specified attribute (URL).  The `foreach` operation applies a function to each group (counting the number of joined records).  The `order` operation sorts the results based on a specified attribute (click count).  The `limit` operation selects the top 5 results.  The `store` operation saves the final result to a file named 'top5sites'.  The context suggests this is part of a lecture or tutorial on Pig Latin, a higher-level data processing language that simplifies the use of MapReduce.  The script is designed to efficiently process large datasets.


**Description of the Figure:**

There is no separate figure on this slide.  The slide is entirely text-based, containing a Pig Latin script.  The script is formatted to show the sequence of operations in a Pig Latin pipeline, which is a common way to represent the steps involved in a data analysis task.  The script is designed to efficiently process large datasets.  The context suggests that the script is designed to identify the top 5 most visited web pages by users aged 18-25.  The script is well-suited to the context of the lecture, as it visually demonstrates the sequence of operations required to solve the problem.




