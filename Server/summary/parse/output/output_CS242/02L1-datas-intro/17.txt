# Slide 17

**Early identification of heart failure**

*Choi et al. 2016*

Electronic health records of 32,784 patients

Deep learning to produce features for classification. Uses temporal information about procedures, medications, diagnoses

ML methods with new DL features can predict heart failure as much as nine months before doctors can now deliver the diagnosis

Although currently applied to heart failure, methods could be applied to any disease


---

**Visual Description:**

The slide presents a visual representation of a deep learning model, likely used for classifying heart failure.

* **Diagram (a):** A diagram of a neural network is shown.  It depicts an input layer, multiple hidden layers (labeled 1, 2, and 3), and an output layer.  The connections between the layers are represented by lines.  The image is oriented horizontally.  The purpose of this diagram is to illustrate the structure of the deep learning model used in the study.  The image is placed in the upper portion of the slide.

* **Diagram (b):** A horizontal timeline-like representation is shown.  It depicts a series of medical concepts (Cough, Fever, Chest X-ray, etc.) connected to a central, highlighted area labeled "Medical concept vectors."  The connections between the concepts and the vectors are represented by lines.  The image is oriented horizontally.  The purpose of this diagram is to illustrate how the deep learning model processes temporal information about medical procedures, medications, and diagnoses.  The image is placed below the neural network diagram.

* **Diagram (c):** A simple mathematical equation is shown, with multiple plus signs (+) and an equal sign (=).  The purpose of this diagram is to illustrate the combination of medical concept vectors to produce a final output.  The image is oriented horizontally.  The image is placed below the timeline-like representation.

* **Images:** Several images of faces are shown in the hidden layers of the neural network.  These images are grayscale and show different levels of detail.  The purpose of these images is to illustrate the process of feature extraction by the deep learning model.  The images are placed in the middle of the slide, above the timeline-like representation.

The overall design of the slide is to visually represent the deep learning model used to predict heart failure, highlighting the process of feature extraction and temporal information processing.  The images and diagrams are crucial for understanding the methodology and the application of deep learning to medical diagnosis.
