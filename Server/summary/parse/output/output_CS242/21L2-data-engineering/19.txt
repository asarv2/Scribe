# Slide 19

# Example: How to count words in a textfile

```
the wheels on the bus
go round and round
round and round
round and round
the wheels on the bus
go round and round
all through the town
```

```
mapper()
[('the', 1), ('wheels', 1), ('on', 1), ('the', 1), ('bus', 1)]
[('go', 1), ('round', 1), ('and', 1), ('round', 1)]
[('round', 1), ('and', 1), ('round', 1)]
[('round', 1), ('and', 1), ('round', 1)]
[('the', 1), ('wheels', 1), ('on', 1), ('the', 1), ('bus', 1)]
[('go', 1), ('round', 1), ('and', 1), ('round', 1)]
[('all', 1), ('through', 1), ('the', 1), ('town', 1)]
```

---

**Description of the slide:**

This slide, part of a lecture on Intro to Data Science, continues the example of using MapReduce to count words in a text file.  It shows the input text (a children's rhyme) and the output of the mapper function.

* **Content:** The slide displays a short text snippet, a children's rhyme about the wheels on a bus.  Below the text, the slide shows the output of a mapper function applied to that text.  This output is a list of key-value pairs, where each pair consists of a word (the key) and a count of 1 (the value).  This output demonstrates how the mapper function would process the input text, extracting each word and assigning a count of 1 to it.  The output is formatted as a list of lists, where each inner list represents a key-value pair.

* **Purpose:** The slide continues the example from the previous slide, illustrating the mapper function's output in more detail.  It shows how the mapper function transforms the input text into key-value pairs, which are then processed by the reducer function to produce the final word counts.

* **Visuals:** There are no figures or images on this slide.  The content is entirely text-based, focusing on the example text and the mapper function's output.  The output is formatted to clearly show the key-value pairs generated by the mapper function.  The example is designed to be simple and easy to understand, illustrating the basic concept of MapReduce.  The output is a list of lists, where each inner list represents the output of the mapper function for a single line of the input text.
