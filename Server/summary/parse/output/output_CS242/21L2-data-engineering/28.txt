# Slide 28

When is distributed processing needed?


---

**Description of the slide:**

This slide, likely part of a lecture on Intro to Data Science, presents a table comparing the performance of different distributed computing systems (e.g., GraphChi, Stratosphere, X-Stream, Spark, Giraph, GraphLab, GraphX) with single-threaded implementations.  The table shows the elapsed times for processing two datasets (Twitter and uk-2007-05) using various numbers of cores.

* **Table Structure:** The table is organized with columns for "scalable system," "cores," "twitter," and "uk-2007-05."  The "scalable system" column lists different distributed computing frameworks.  The "cores" column indicates the number of processing cores used.  The "twitter" and "uk-2007-05" columns show the elapsed times (in seconds or milliseconds) for processing the respective datasets.
* **Data Representation:** The table provides numerical data, showing how the elapsed time changes with the number of cores used in each system.  The values in the table represent the time taken to complete a specific task (likely a graph processing or similar operation) on the respective datasets.
* **Purpose:** The table is designed to illustrate when distributed processing becomes necessary.  The comparison between single-threaded and multi-core/distributed systems shows the performance gains (or lack thereof) that can be achieved by distributing the workload.  The different elapsed times for different systems and core counts highlight the varying efficiencies of different distributed computing frameworks.  The data suggests that for very large datasets or complex computations, distributed processing is often necessary to achieve reasonable performance.


**Visual Representation:**

The slide contains a table with rows representing different distributed computing systems (e.g., GraphChi, Stratosphere) and columns representing the number of cores used and the corresponding processing times for two datasets (Twitter and uk-2007-05).  The table is formatted to clearly show the relationship between the number of cores, the system used, and the processing time.  The table is designed to be easily readable and to quickly convey the performance characteristics of the different systems.  The table is a key visual element, providing quantitative data to support the discussion of when distributed processing is necessary.


