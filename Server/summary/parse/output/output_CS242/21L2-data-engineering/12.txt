# Slide 12

# Example

**Example:** sum charges for each credit card

```
procedure MAPCREDITCARDS(input)
  while not input.done() do
    record ← input.next()
    card ← record.card
    amount ← record.amount
    Emit(card, amount)
  end while
end procedure

procedure REDUCE CREDITCARDS(key, values)
  total ← 0
  card ← key
  while not values.done() do
    amount ← values.next()
    total ← total + amount
  end while
  Emit(card, total)
end procedure
```

**Key:** Card number
**Value:** Charge on card
**Key:** Card number
**Output:** Sum of charges


---

**Description of the slide:**

This slide provides a concrete example of how MapReduce works, using the task of summing credit card charges.  The example is presented as pseudocode, showing the `MAPCREDITCARDS` and `REDUCECREDITCARDS` procedures.  The pseudocode clearly defines the input (credit card transactions), the intermediate steps (mapping each transaction to a card number and amount), and the final output (the sum of charges for each card).  The slide also explicitly labels the keys and values in the MapReduce process, and the final output.  There are no figures on this slide.  The example is presented in a way that is easy to understand and follow, highlighting the core concepts of the MapReduce model.
