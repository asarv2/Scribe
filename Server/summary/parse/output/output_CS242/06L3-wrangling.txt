# Slide 1

Introduction to Data Science
Purdue CS24200
Tony Bergstrom
Slides adapted from Jen Neville, CS 24200


---

**Description of the Slide:**

This is the title slide for a lecture on Introduction to Data Science.  The background is a dark gray.  The title "Introduction to Data Science" is large and prominent, followed by the course information (Purdue CS24200 and the instructor's name, Tony Bergstrom).  At the very bottom, a credit line indicates that the slides were adapted from another source (Jen Neville, CS 24200).  The text is a light gray color, making it easily readable against the dark background.  There are no figures or images on this slide.  The slide's purpose is to introduce the course topic and provide basic course information.


# Slide 2

# Data Organization


---

**Description of the Slide:**

This slide, a plain gray background, has the single, bold, centered title "Data Organization" in a dark gray font.  It's a simple slide, likely serving as a heading for the next section of the lecture, which will cover different methods of organizing data in a data science context.  There are no figures or other elements on the slide.




# Slide 3

A data set of real estate

Pretend we are interested in doing some analysis of real estate.
We want to get a sense of what is available.
We find a csv dataset from zillow for some “local” houses

# A new data file: Zillow Example
data_zillow = pd.read_csv('zillow.csv')
data_zillow

Does it look ok? Is something off?
data_zillow.columns
Index(['Index', ' "Living Space (sq ft)”',
       ' "Beds"', ' "Baths"', ' "Zip"',
       ' "Year"', ' "List Price ($)"'],
      dtype='object')


---

**Description of the Slide:**

This slide presents a data set of real estate, likely from a CSV file named "zillow.csv".  The data is displayed in a table format.  The table's columns are labeled with the following headers: "Index", "Living Space (sq ft)", "Beds", "Baths", "Zip", "Year", and "List Price ($)".  The table contains numerical data for various houses, including their living space, number of bedrooms and bathrooms, zip code, year built, and list price.  The slide also includes code snippets demonstrating how to load the data into a Python data frame using the pandas library.  The code shows how to read the CSV file and then display the column names of the loaded data frame.  The purpose of this slide is to introduce a sample dataset and demonstrate how to load and inspect it using Python.  The table is a key part of the slide, showing the data itself.  The code snippets are crucial for demonstrating the data analysis process.  The question "Does it look ok? Is something off?" suggests that the slide is intended to prompt the audience to consider the quality and potential issues with the data.


# Slide 4

**Fixing Index Names**

If we want to access the Zip code column, we have to type
`data_zillow[' "Zip"']`
That's just annoying

`data_zillow.columns = ['INDEX', 'LIVING SPACE (SQ FT)', 'BEDS', 'BATHS', 'ZIP', 'YEAR', 'LIST PRICE ($)']`

`display(data_zillow)`

`data_zillow["ZIP"]`

```
INDEX LIVING SPACE (SQ FT) BEDS BATHS ZIP YEAR LIST PRICE ($)
0       0                 2222     3   3.5 32312 1981      250000
1       1                 1628     3   2.0 32308 2009      185000
2       2                 3824     5   4.0 32312 1954      399000
3       3                 1137     3   2.0 32309 1993      150000
4       4                 3560     6   4.0 32309 1973      315000
5       5                 2893     4   3.0 32312 1994      699000
6       6                 3631     4   3.0 32309 1996      649000
7       7                 2483     4   3.0 32312 2016      399000
```

---

**Description of the Slide:**

This slide, part of a course on Introduction to Data Science, focuses on correcting the names of columns in a dataset.  The previous slide introduced a dataset (`data_zillow`) likely from a CSV file, and this slide addresses the issue of awkward column names.  The slide shows how to rename the columns to more standard and usable names.  The code `data_zillow.columns = ['INDEX', 'LIVING SPACE (SQ FT)', 'BEDS', 'BATHS', 'ZIP', 'YEAR', 'LIST PRICE ($)']` is crucial for making the data usable.  The slide also demonstrates how to access the "ZIP" column using the corrected column name.  The slide includes a table displaying the data after the column names have been corrected.  The table shows the index, living space, beds, baths, zip code, year, and list price for several houses.  The table is formatted to show the corrected column names.  The code snippets are essential for demonstrating the process of data manipulation and access.  The slide's purpose is to show how to fix the column names in the dataset and access specific columns.  The table is a key part of the slide, showing the data after the column names have been corrected.  The code snippets are crucial for demonstrating the data analysis process.


# Slide 5

**Sorting a DataFrame**

To sort rows in `data_zillow` by `LIVING SPACE` in ascending order

`data_zillow.sort_values('LIVING SPACE (SQ FT)', ascending=True)`

```
INDEX	LIVING SPACE (SQ FT)	BEDS	BATHS	ZIP	YEAR	LIST PRICE ($)
13	1128	2	1.0	32303	1955	89000
14	1137	3	2.0	32309	1993	150000
17	1158	3	2.0	32303	1993	155000
14	1381	3	2.0	32301	2006	143000
1	1628	3	2.0	32308	2009	185000
9	1997	3	3.0	32311	2006	295000
10	2097	4	3.0	32311	2016	290000
…	…	…	…	…	…	…
```

This does not change the order of `data_zillow`, if you want to save it assign it to a variable


---

**Description of the Slide:**

This slide demonstrates how to sort a DataFrame in a Python data science context.  The code `data_zillow.sort_values('LIVING SPACE (SQ FT)', ascending=True)` is used to sort the `data_zillow` DataFrame by the "LIVING SPACE (SQ FT)" column in ascending order.  The output shows the sorted data, with the rows ordered from smallest to largest living space.  The output is a table, displaying the `INDEX`, `LIVING SPACE (SQ FT)`, `BEDS`, `BATHS`, `ZIP`, `YEAR`, and `LIST PRICE ($)`.  The `...` indicates that the table continues beyond the displayed rows.  The slide is part of a larger lecture on data manipulation and analysis, likely using the pandas library in Python.  The purpose of this slide is to show how to sort data based on a specific column.  The table is a key part of the slide, showing the sorted data.  The code snippet is crucial for demonstrating the sorting process.  The final sentence emphasizes that the original `data_zillow` DataFrame is not modified; a new sorted DataFrame needs to be assigned to a variable to save the result.


# Slide 6

**Breaking ties**

You can sort by multiple columns, if you need to break ties systematically
`bedsYear = data_zillow.sort_values(by=['BEDS', 'YEAR'])`
`bedsYear`

```
INDEX	LIVING SPACE (SQ FT)	BEDS	BATHS	ZIP	YEAR	LIST PRICE ($)
13	14	1128	2	1.0	32303	1955	89000
0	1	2222	3	3.5	32312	1981	250000
16	17	2533	3	2.0	32310	1991	365000
3	4	1137	3	2.0	32309	1993	150000
17	18	1158	3	2.0	32303	1993	155000
9	10	1997	3	3.0	32311	2006	295000
14	15	1381	3	2.0	32301	2006	143000
…	…	…	…	…	…	…	…
```

---

**Description of the Slide:**

This slide, part of a data science lecture, demonstrates how to sort a DataFrame (`data_zillow`) by multiple columns.  The code `bedsYear = data_zillow.sort_values(by=['BEDS', 'YEAR'])` sorts the DataFrame first by the 'BEDS' column and then by the 'YEAR' column.  This is useful for breaking ties that might occur when sorting by a single column.  The output is a table showing the sorted data.  The table's columns are `INDEX`, `LIVING SPACE (SQ FT)`, `BEDS`, `BATHS`, `ZIP`, `YEAR`, and `LIST PRICE ($)`.  The `...` indicates that the table continues beyond the displayed rows.  The slide is part of a larger lecture on data manipulation and analysis, likely using the pandas library in Python.  The purpose of this slide is to show how to sort data based on multiple columns, breaking ties systematically.  The table is a key part of the slide, showing the sorted data.  The code snippet is crucial for demonstrating the sorting process.  The `bedsYear` variable stores the sorted DataFrame.


# Slide 7

**Sort by Index**

You can sort by the row index or column index as well

`data_zillow.sort_index(axis=0, ascending=False)`

```
INDEX	LIVING SPACE (SQ FT)	BEDS	BATHS	ZIP	YEAR	LIST PRICE ($)
19	4010	5	3.0	32309	2002	549900
18	2497	4	4.0	32309	1990	289000
17	1158	3	2.0	32303	1993	155000
16	2533	3	2.0	32310	1991	365000
…	…	…	…	…	…	…
```

`data_zillow.sort_index(axis=1)`

```
BATHS	BEDS	INDEX	LIST PRICE ($)	LIVING SPACE (SQ FT)	YEAR	ZIP
0	3.5	3	1	250000	2222	1981	32312
1	2.0	3	2	185000	1628	2009	32308
…	…	…	…	…	…	…	…
```

---

**Description of the Slide:**

This slide demonstrates how to sort a DataFrame (`data_zillow`) by its row index (axis=0) and column index (axis=1) in a data science context.  The code `data_zillow.sort_index(axis=0, ascending=False)` sorts the DataFrame by the row index in descending order.  The output shows the sorted data, with the rows ordered from the highest index to the lowest.  The columns are `INDEX`, `LIVING SPACE (SQ FT)`, `BEDS`, `BATHS`, `ZIP`, `YEAR`, and `LIST PRICE ($)`.  The `...` indicates that the table continues beyond the displayed rows.  The second code example, `data_zillow.sort_index(axis=1)`, sorts the DataFrame by the column index in ascending order.  The output shows the sorted data, with the columns ordered alphabetically.  The columns are `BATHS`, `BEDS`, `INDEX`, `LIST PRICE ($)`, `LIVING SPACE (SQ FT)`, `YEAR`, and `ZIP`.  The `...` indicates that the table continues beyond the displayed rows.  The slide is part of a larger lecture on data manipulation and analysis, likely using the pandas library in Python.  The purpose of this slide is to show how to sort data based on the index.  The tables are key parts of the slide, showing the sorted data.  The code snippets are crucial for demonstrating the sorting process.




# Slide 8

**Summary Statistics**

If you have your data in a DataFrame you also have access to a number of summary methods

`sum()`
`mean()`
`median()`
`std()`
`max()`
`min()`
`describe()`


---

**Description of the Slide:**

This slide, part of a lecture on Introduction to Data Science, lists various summary statistics functions available when working with data stored in a pandas DataFrame.  The slide is entirely text-based, presenting a bulleted list of functions.  The functions are all common statistical operations, such as calculating the sum, mean, median, standard deviation, maximum, and minimum values of a column, and a `describe()` function that likely provides a comprehensive summary of the data.  The purpose of this slide is to inform the audience about the tools available for summarizing data within a DataFrame.  There are no figures or images on this slide.




# Slide 9

**Summary**

You can call these methods on a column
`data_zillow["LIST PRICE ($)"].max()`
799900

You can call these methods on the dataframe
`data_zillow.describe().round(2)`

| INDEX | LIVING SPACE (SQ FT) | BEDS | BATHS | ZIP | YEAR | LIST PRICE ($) |
|---|---|---|---|---|---|---|
| count | 20.00 | 20.00 | 20.00 | 20.00 | 20.00 | 20.00 |
| mean | 10.50 | 2645.65 | 3.85 | 32308.90 | 1992.65 | 383440.00 |
| std | 5.92 | 1111.42 | 0.99 | 3.55 | 18.53 | 205756.67 |
| min | 1.00 | 1128.00 | 2.00 | 32301.00 | 1954.00 | 89000.00 |
| 25% | 5.75 | 1904.75 | 3.00 | 32308.75 | 1987.75 | 233750.00 |
| 50% | 10.50 | 2490.00 | 4.00 | 32309.50 | 1995.00 | 340000.00 |
| 75% | 15.25 | 3577.75 | 4.25 | 32312.00 | 2006.00 | 554675.00 |
| max | 20.00 | 4892.00 | 6.00 | 32312.00 | 2016.00 | 799900.00 |


---

**Description of the Slide:**

This slide, part of a data science lecture, presents a summary of the data from the `data_zillow` DataFrame.  The top portion of the slide shows code snippets for calculating the maximum value of the "LIST PRICE ($)" column and using the `.describe()` method to get summary statistics for the entire DataFrame.  The bottom portion of the slide displays a table containing the summary statistics.  The table shows various descriptive statistics for each column in the DataFrame, including `count`, `mean`, `std` (standard deviation), `min`, `25%`, `50%`, `75%`, and `max`.  The table is formatted with column headers and rows of numerical data.  The purpose of this slide is to demonstrate how to calculate summary statistics and to show the results of those calculations for the `data_zillow` dataset.  The table is a key part of the slide, providing a concise overview of the data's characteristics.  The code snippets are crucial for demonstrating the data analysis process.




# Slide 10

**Data Wrangling**


---

**Description of the Slide:**

This slide, a plain gray background, has the single, bold, centered title "Data Wrangling" in a dark gray font.  It's a simple slide, likely serving as a heading for the next section of the lecture, which will cover techniques for cleaning, transforming, and preparing data for analysis in a data science context.  There are no figures or other elements on the slide.


**What you see:**

A plain gray background with the title "Data Wrangling" centered in a dark gray, sans-serif font.  The title is large enough to be prominent but not overly large.  The slide is entirely text-based.


# Slide 11

**Data Wrangling**

Loosely refers to the tasks needed to preprocess data, including:

*   Augmentation (e.g., adding features/rows)
*   Subsetting (e.g., filtering and selecting)
*   Cleaning (e.g., find missing values/errors to correct)
*   Aggregating (e.g., grouping by values and summarizing)
*   Transforming (e.g., scaling attribute values)


---

**Description of the Slide:**

This slide, titled "Data Wrangling," is a simple, introductory slide in a data science lecture.  It defines data wrangling as the tasks needed to prepare data for analysis.  The slide lists several key tasks involved in data wrangling, including augmentation (adding features or rows), subsetting (filtering and selecting), cleaning (finding and correcting missing values or errors), aggregating (grouping and summarizing), and transforming (scaling attribute values).  The slide is entirely text-based, using bullet points to list the different tasks.  The purpose of the slide is to introduce the concept of data wrangling and the various activities it encompasses.  There are no figures or images on this slide.


# Slide 12

**Augmenting data**


---

**Description of the Slide:**

This slide, a plain, light gray background, has the single, bold, centered title "Augmenting data" in a dark gray font.  It's a simple slide, likely serving as a heading for the next section of the lecture, which will cover techniques for adding new data or features to an existing dataset in a data science context.  There are no figures or other elements on this slide.


**What you see:**

A plain, light gray background with the title "Augmenting data" centered in a dark gray, sans-serif font.  The title is large enough to be prominent but not overly large.  The slide is entirely text-based.


# Slide 13

**Augmenting: Adding Columns**

# add a column to a data frame with a constant value
`data_zillow['SOLD'] = 'NO'`

```
INDEX	LIVING SPACE (SQ FT)	BEDS	BATHS	ZIP	YEAR	LIST PRICE ($)	SOLD
0	0	2222	3	3.5	32312	1981	250000	NO
1	1	1628	3	2.0	32308	2009	185000	NO
2	2	3824	5	4.0	32312	1954	399000	NO
3	3	1137	3	2.0	32309	1993	150000	NO
4	4	3560	6	4.0	32309	1973	315000	NO
...	...	...	...	...	...	...	...	...
```

# add a column with a list of values for each item
`data_zillow['SOLD'] = ['NO', 'NO', 'NO', 'NO', 'YES', 'NO', 'YES', 'NO', 'NO', 'YES', 'NO', 'NO', 'NO', 'NO', 'NO', 'NO', 'NO', 'NO', 'NO', 'NO']`

```
INDEX	LIVING SPACE (SQ FT)	BEDS	BATHS	ZIP	YEAR	LIST PRICE ($)	SOLD
0	0	2222	3	3.5	32312	1981	250000	NO
1	1	1628	3	2.0	32308	2009	185000	NO
2	2	3824	5	4.0	32312	1954	399000	NO
3	3	1137	3	2.0	32309	1993	150000	NO
4	4	3560	6	4.0	32309	1973	315000	YES
...	...	...	...	...	...	...	...	...
```


---

**Description of the Slide:**

This slide demonstrates how to augment a DataFrame by adding a new column.  The code snippets show two methods:

1.  Adding a column with a constant value: `data_zillow['SOLD'] = 'NO'`. This adds a new column named 'SOLD' and populates it with the string 'NO' for every row.
2.  Adding a column with a list of values: `data_zillow['SOLD'] = ['NO', 'NO', 'NO', 'NO', 'YES', ...]`. This adds a new column named 'SOLD' and populates it with a list of values, one for each row.  The list contains strings 'NO' and 'YES'.

The slide includes two tables, each showing a portion of the `data_zillow` DataFrame after the addition of the 'SOLD' column.  The first table shows the column populated with the constant value 'NO'. The second table shows the column populated with a list of values, including both 'NO' and 'YES'.  The tables are formatted to show the `INDEX`, `LIVING SPACE (SQ FT)`, `BEDS`, `BATHS`, `ZIP`, `YEAR`, `LIST PRICE ($)`, and `SOLD` columns.  The `...` indicates that the tables continue beyond the displayed rows.  The purpose of this slide is to illustrate how to add new columns to a DataFrame, either with a constant value or a list of values.  The code snippets are crucial for demonstrating the augmentation process. The tables are essential for showing the effect of the code on the data.


# Slide 14

**Augmenting: Adding Calculated Columns**

# add a new column called "PRICE/SQ FT ($)"
# price per square feet = list price / living space
data_zillow['PRICE/SQ FT ($)'] = data_zillow['LIST PRICE ($)'] / data_zillow['LIVING SPACE (SQ FT)']

# add a new column called AGE
# get current year (don't use 2021!)
import datetime
now = datetime.datetime.now()
current_year = now.year
# the house age in a new column AGE
data_zillow['AGE'] = current_year - data_zillow['YEAR']
data_zillow

```
INDEX	LIVING SPACE (SQ FT)	BEDS	BATHS	ZIP	YEAR	LIST PRICE ($)	SOLD	PRICE/SQ FT ($)	AGE
0	2222	3	3.5	32312	1981	250000	NO	112.0	40
1	1628	3	2.0	32308	2009	185000	NO	113.0	12
2	3824	5	4.0	32312	1954	399000	NO	104.0	67
3	1137	3	2.0	32309	1993	150000	NO	131.0	28
4	3560	6	4.0	32309	1973	315000	YES	88.0	48
...	...	...	...	...	...	...	...	...	...
```

---

**Description of the Slide:**

This slide demonstrates how to add calculated columns to a DataFrame, specifically `data_zillow`.  The code calculates the price per square foot and the age of the houses.

*   **`PRICE/SQ FT ($) = list price / living space`**: This line calculates the price per square foot by dividing the `LIST PRICE ($) ` by the `LIVING SPACE (SQ FT)` for each row. The result is stored in a new column named `PRICE/SQ FT ($)`.
*   **`AGE = current year - YEAR`**: This line calculates the age of each house by subtracting the `YEAR` from the current year. The result is stored in a new column named `AGE`.
*   **`import datetime`**: This line imports the `datetime` module, which is necessary for getting the current date and time.
*   **`now = datetime.datetime.now()`**: This line gets the current date and time.
*   **`current_year = now.year`**: This line extracts the current year from the `now` object.
*   **`data_zillow`**: This line displays the updated DataFrame, which now includes the newly calculated `PRICE/SQ FT ($) ` and `AGE` columns.

The slide includes a table showing a portion of the updated `data_zillow` DataFrame.  The table displays the `INDEX`, `LIVING SPACE (SQ FT)`, `BEDS`, `BATHS`, `ZIP`, `YEAR`, `LIST PRICE ($)`, `SOLD`, `PRICE/SQ FT ($)`, and `AGE` columns.  The `...` indicates that the table continues beyond the displayed rows.  The purpose of this slide is to show how to add calculated columns to a DataFrame, which is a common data wrangling task. The code snippets are crucial for demonstrating the calculation and addition of new columns. The table is essential for showing the effect of the code on the data.




# Slide 15

**Augmenting: Adding a Row**

# insert a new row using `loc`
# create the new entry
`newEntry = [21, 2800, 4, 3.5, '32356', 1988, 400000, 'NO', 142, 32]`
`data_zillow.loc[20] = newEntry`
`data_zillow.tail()`

```
INDEX	LIVING SPACE (SQ FT)	BEDS	BATHS	ZIP	YEAR	LIST PRICE ($)	SOLD	PRICE/SQ FT ($)	AGE
16	17	2533	3	2.0	32310	1991	365000	NO	144	29
17	18	1158	3	2.0	32303	1993	155000	NO	133	27
18	19	2497	4	4.0	32309	1990	289000	NO	115	30
19	20	4010	5	3.0	32309	2002	549900	NO	137	18
20	21	2800	4	3.5	32356	1988	400000	NO	142	32
```

---

**Description of the Slide:**

This slide demonstrates how to add a new row to a DataFrame (`data_zillow`) using the `.loc` method.  The code first defines a list `newEntry` containing the values for the new row.  Crucially, this list includes the index (21), living space, beds, baths, zip code, year, list price, sold status, price per square foot, and age.  Then, it uses `data_zillow.loc[20] = newEntry` to insert this new row into the DataFrame at index 20.  Finally, `data_zillow.tail()` displays the last few rows of the updated DataFrame, showing the newly added row.  The table shows the `INDEX`, `LIVING SPACE (SQ FT)`, `BEDS`, `BATHS`, `ZIP`, `YEAR`, `LIST PRICE ($)`, `SOLD`, `PRICE/SQ FT ($)`, and `AGE` columns.  The `...` indicates that the table continues beyond the displayed rows.  The purpose of this slide is to show how to add a new row to a DataFrame using the `.loc` method.  The code snippets are crucial for demonstrating the row insertion process. The table is essential for showing the effect of the code on the data, specifically the addition of the new row at index 20.  The new row's data is clearly visible in the table.


# Slide 16

**Augmenting: Adding a Row**

# Insert data using a Series
newData = [22, 4500, 5, 4.5, '47906', 2010, 550000, 'NO', 122, 10]
newEntry = pd.Series(newData, index=list(data_zillow.columns))
# append the new entry to the dataFrame data_zillow
data_zillow = data_zillow.append(newEntry, ignore_index=True)
data_zillow.tail()

```
INDEX	LIVING SPACE (SQ FT)	BEDS	BATHS	ZIP	YEAR	LIST PRICE ($)	SOLD	PRICE/SQ FT ($)	AGE
17	18	1158	3	2.0	32303	1993	155000	NO	133	27
18	19	2497	4	4.0	32309	1990	289000	NO	115	30
19	20	4010	5	3.0	32309	2002	549900	NO	137	18
20	21	2800	4	3.5	32356	1988	400000	NO	142	32
21	22	4500	5	4.5	47906	2010	550000	NO	122	10
```

---

**Description of the Slide:**

This slide demonstrates how to add a new row to a DataFrame (`data_zillow`) using the `.append()` method.  The code first defines a list `newData` containing the values for the new row.  This list includes the index (22), living space, beds, baths, zip code, year, list price, sold status, price per square foot, and age.  Then, it creates a pandas Series `newEntry` from this data, using the column names of the DataFrame as the index.  Finally, it appends this new row to the DataFrame using `data_zillow.append(newEntry, ignore_index=True)`.  The `ignore_index=True` argument is crucial; it resets the index of the DataFrame to sequential integers, preventing issues with duplicate indices.  The `data_zillow.tail()` command displays the last five rows of the updated DataFrame, showing the newly added row at the end.  The table shows the `INDEX`, `LIVING SPACE (SQ FT)`, `BEDS`, `BATHS`, `ZIP`, `YEAR`, `LIST PRICE ($)`, `SOLD`, `PRICE/SQ FT ($)`, and `AGE` columns.  The purpose of this slide is to show how to add a new row to a DataFrame.  The code snippets are crucial for demonstrating the row insertion process. The table is essential for showing the effect of the code on the data, specifically the addition of the new row.




# Slide 17

**Augmenting: UNaugmenting (deleting)**

Be careful about deleting data. It can cause incompatibilities between data sources.
Rather than deleting, consider duplicating a filtered portion of the data frame.

But if you must:

```
del data_zillow['INDEX']
```

```
LIVING SPACE (SQ FT)	BEDS	BATHS	ZIP	YEAR	LIST PRICE ($)	SOLD	PRICE/SQ FT ($)	AGE
0	2222	3	3.5	32312	1981	250000	NO	112	39
…	…	…	…	…	…	…	…	…
```

---

**Description of the Slide:**

This slide, part of a data science lecture, discusses the importance of avoiding deleting data and instead suggests duplicating filtered portions of a DataFrame.  It emphasizes that deleting data can lead to inconsistencies between different data sources.  The slide then shows an example of deleting a column named 'INDEX' from a DataFrame called `data_zillow`.  The code `del data_zillow['INDEX']` is shown, indicating the deletion of the 'INDEX' column.  The slide also includes a table showing a portion of the DataFrame after the deletion.  The table shows the `LIVING SPACE (SQ FT)`, `BEDS`, `BATHS`, `ZIP`, `YEAR`, `LIST PRICE ($)`, `SOLD`, `PRICE/SQ FT ($)`, and `AGE` columns.  The `...` indicates that the table continues beyond the displayed rows.  The purpose of this slide is to highlight the potential issues with deleting data and to demonstrate how to delete a column from a DataFrame.  The table is a key part of the slide, showing the data after the column deletion.  The code snippet is crucial for demonstrating the deletion process.




# Slide 18

**Subsetting Data**


---

**Description of the Slide:**

This slide, a plain, light gray background, has the single, bold, centered title "Subsetting Data" in a dark gray font.  It's a simple slide, likely serving as a heading for the next section of the lecture, which will cover techniques for selecting specific portions of a dataset in a data science context.  There are no figures or other elements on this slide.


**What you see:**

A plain, light gray background with the title "Subsetting Data" centered in a dark gray, sans-serif font.  The title is large enough to be prominent but not overly large.  The slide is entirely text-based.


# Slide 19

**Filtering**

# select only rows of interest
`data_zillow[data_zillow.BEDS == 3]`

| INDEX | LIVING SPACE (SQ FT) | BEDS | BATHS | ZIP | YEAR | LIST PRICE ($) | SOLD | PRICE/SQ FT ($) | AGE |
|---|---|---|---|---|---|---|---|---|---|
| 0 | 2222 | 3 | 3.5 | 32312 | 1981 | 250000 | NO | 112 | 39 |
| 1 | 1628 | 3 | 2.0 | 32308 | 2009 | 185000 | NO | 113 | 11 |
| 3 | 1137 | 3 | 2.0 | 32309 | 1993 | 150000 | NO | 131 | 27 |
| 9 | 1997 | 3 | 3.0 | 32311 | 2006 | 295000 | YES | 147 | 14 |
| 14 | 1381 | 3 | 2.0 | 32301 | 2006 | 143000 | NO | 103 | 14 |
| 16 | 2533 | 3 | 2.0 | 32310 | 1991 | 365000 | NO | 144 | 29 |
| 17 | 1158 | 3 | 2.0 | 32303 | 1993 | 155000 | NO | 133 | 27 |


# Combine boolean expressions for more complicated filters
# Note: you need the parenthesis
`data_zillow[(data_zillow.BEDS == 3) & (data_zillow.YEAR > 2000)]`

| INDEX | LIVING SPACE (SQ FT) | BEDS | BATHS | ZIP | YEAR | LIST PRICE ($) | SOLD | PRICE/SQ FT ($) | AGE |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 1628 | 3 | 2.0 | 32308 | 2009 | 185000 | NO | 113 | 11 |
| 9 | 1997 | 3 | 3.0 | 32311 | 2006 | 295000 | YES | 147 | 14 |
| 14 | 1381 | 3 | 2.0 | 32301 | 2006 | 143000 | NO | 103 | 14 |


---

**Description of the Slide:**

This slide demonstrates filtering a DataFrame (`data_zillow`) based on specific conditions.  The first part shows how to select rows where the 'BEDS' column is equal to 3.  The output is a table displaying the filtered data, including columns like `INDEX`, `LIVING SPACE (SQ FT)`, `BEDS`, `BATHS`, `ZIP`, `YEAR`, `LIST PRICE ($)`, `SOLD`, `PRICE/SQ FT ($)`, and `AGE`.  The second part shows how to combine multiple conditions using boolean expressions.  The code `data_zillow[(data_zillow.BEDS == 3) & (data_zillow.YEAR > 2000)]` filters the DataFrame to include only rows where 'BEDS' is 3 *and* 'YEAR' is greater than 2000.  The output is another table displaying the filtered data, matching the format of the first table.  The tables are crucial for showing the results of the filtering operations.  The code snippets are essential for demonstrating the filtering process.  The slide is part of a larger lecture on data manipulation and analysis, likely using the pandas library in Python.


# Slide 20

**Filtering to columns**

# select a subset of columns by name
data_zillow[['ZIP','YEAR']]

```
ZIP	YEAR
0	32312	1981
1	32308	2009
2	32312	1954
3	32309	1993
4	32309	1973
5	32312	1994
6	32309	1996
7	32312	2016
8	32312	2002
9	32311	2006
...	...	...
```

# another way to select columns by name
data_zillow.filter(items=['ZIP', 'YEAR'])

# filter() also allows you to filter rows
# you can specify labels with partial strings
# see documentation


---

**Description of the Slide:**

This slide demonstrates two ways to select specific columns from a DataFrame, `data_zillow`, in a data science context.  The first method uses the double bracket notation `data_zillow[['ZIP','YEAR']]` to select the 'ZIP' and 'YEAR' columns.  The output shows a table with these two columns.  The second method uses the `.filter()` method `data_zillow.filter(items=['ZIP', 'YEAR'])` to achieve the same result.  The output of this method is also a table with only the 'ZIP' and 'YEAR' columns.  The `...` indicates that the table continues beyond the displayed rows.  The slide also includes comments about filtering rows and using partial strings for labels, suggesting that these topics will be covered in subsequent slides or sections of the lecture.  The purpose of this slide is to show different ways to select specific columns from a DataFrame.  The tables are crucial for demonstrating the selection of columns.  The comments are important for understanding the broader context of the lecture.




# Slide 21

**Sampling to Filter Rows**

# get a random sample of ten rows
`data_zillow.sample(10)`

| INDEX | LIVING SPACE (SQ FT) | BEDS | BATHS | ZIP | YEAR | LIST PRICE ($) | SOLD | PRICE/SQ FT ($) | AGE |
|---|---|---|---|---|---|---|---|---|---|
| 6 | 7 | 3631 | 4 | 3.0 | 32309 | 1996 | 649000 | YES | 178 | 24 |
| 11 | 12 | 3200 | 5 | 4.0 | 32312 | 1964 | 465000 | NO | 145 | 56 |
| 21 | 22 | 4500 | 5 | 4.5 | 47906 | 2010 | 550000 | NO | 122 | 10 |
| 0 | 1 | 2222 | 3 | 3.5 | 32312 | 1981 | 250000 | NO | 112 | 39 |
| 2 | 3 | 3824 | 5 | 4.0 | 32312 | 1954 | 399000 | NO | 104 | 66 |
| 8 | 9 | 2400 | 4 | 4.0 | 32312 | 2002 | 613000 | NO | 255 | 18 |
| 13 | 14 | 1128 | 2 | 1.0 | 32303 | 1955 | 89000 | NO | 78 | 65 |
| 19 | 20 | 4010 | 5 | 3.0 | 32309 | 2002 | 549900 | NO | 137 | 18 |
| 4 | 5 | 3560 | 6 | 4.0 | 32309 | 1973 | 315000 | YES | 88 | 47 |
| 7 | 8 | 2483 | 4 | 3.0 | 32312 | 2016 | 399000 | NO | 160 | 4 |


---

**Description of the Slide:**

This slide, part of a data science lecture, demonstrates how to randomly sample rows from a DataFrame (`data_zillow`).  The code `data_zillow.sample(10)` is used to select a random sample of 10 rows.  The output is a table displaying the sampled data.  The table shows the `INDEX`, `LIVING SPACE (SQ FT)`, `BEDS`, `BATHS`, `ZIP`, `YEAR`, `LIST PRICE ($)`, `SOLD`, `PRICE/SQ FT ($)`, and `AGE` columns.  Each row represents a randomly selected house from the dataset.  The purpose of this slide is to illustrate how to obtain a random subset of data from a larger dataset.  The table is a key part of the slide, showing the sampled data.  The code snippet is crucial for demonstrating the sampling process.  The output is a random sample of 10 rows from the `data_zillow` DataFrame.  The rows are selected randomly, and the order of the rows in the output is not necessarily the same as the order in the original DataFrame.


# Slide 22

**Cleaning Data**


---

**Description of the Slide:**

This slide, part of a lecture on Introduction to Data Science, has a single, bold, centered title: "Cleaning Data".  It's a simple slide, likely serving as a heading for the next section of the lecture, which will cover techniques for cleaning and preparing data for analysis.  There are no figures or other elements on this slide.


**What you see:**

A plain, light gray background with the title "Cleaning Data" centered in a dark gray, sans-serif font.  The title is large enough to be prominent but not overly large.  The slide is entirely text-based.


# Slide 23

**Employee Information**

You are looking at a list of employees...

```
df_employee = pd.read_csv('employees.csv')
df_employee
df_employee.shape
(20, 4)
```

...and you notice the data has duplicates

| First Name | ID | Last Login Time | Team |
|---|---|---|---|
| Douglas | 785 | 5/1/20 12:42 | Marketing |
| Jerry | 222 | 5/2/20 5:50 | Finance |
| Christina | 543 | 5/2/20 17:19 | Engineering |
| Thomas | 234 | 5/1/20 6:53 | Legal |
| Maria | 102 | 5/3/20 11:17 | Finance |
| Jerry | 222 | 5/1/20 17:00 | Finance |
| Larry | 366 | 5/1/20 16:47 | Client Services |
| Maria | 478 | 5/2/20 11:17 | Finance |
| Dennis | 189 | 5/2/20 1:35 | Legal |
| Ruby | 331 | 5/1/20 16:20 | Product |
| Benjamin | 420 | 5/1/20 22:06 | Legal |
| Frances | 682 | 5/2/20 6:51 | Business Development |
| Terry | 777 | 5/1/20 18:30 | Client Services |
| Benjamin | 420 | 5/1/20 22:06 | Legal |
| Christina | 543 | 5/2/20 13:19 | Engineering |
| Jerry | 222 | 5/1/20 13:00 | Finance |
| Douglas | 785 | 5/1/20 12:42 | Marketing |
| Ruby | 331 | 5/1/20 16:20 | Product |
| John | 800 | 5/1/20 12:00 | Business Development |
| Benjamin | 420 | 5/1/20 22:06 | Legal |


---

**Description of the Slide:**

This slide presents a table of employee information, likely from a CSV file named 'employees.csv'.  The table has four columns: "First Name", "ID", "Last Login Time", and "Team".  The data appears to be a list of employees, with their respective IDs, login times, and teams.  The text at the top of the slide indicates that the data has been loaded into a pandas DataFrame called `df_employee`, and its shape is (20, 4), meaning it has 20 rows and 4 columns.  The final line, "...and you notice the data has duplicates", suggests that the instructor is highlighting the presence of duplicate entries in the data, which is a common issue in data analysis.  The table is formatted in a standard tabular format, with each row representing an employee and each column representing an attribute of the employee.  The data in the table is likely used as an example dataset for the subsequent discussion of data cleaning and manipulation techniques.


# Slide 24

**Identifying Duplicates**

You don't want to spend time comparing elements to find those duplicates

`df_employee.duplicated()`

This returns a boolean Series to identify duplicates

There are options for which elements you identify

`df_employee.duplicated(keep=<option>)`

# 'first': all but first element are marked True
# 'last': all but last element are marked True
# False: all elements are marked True
# first is the default

0     False
1     False
2     False
3     False
4     False
5     False
6     False
7     False
8     False
9     False
10    False
11    False
12    False
13     True
14    False
15    False
16     True
17     True
18    False
19     True
dtype: bool


---

**Description of the Slide:**

This slide, part of a data science lecture, discusses identifying duplicate rows in a DataFrame (`df_employee`).  The code `df_employee.duplicated()` is shown, which returns a boolean Series indicating whether each row is a duplicate.  The output of this code is a column of boolean values (True or False).  The values in the output correspond to the rows in the `df_employee` DataFrame.  A value of True indicates that the corresponding row is a duplicate, while False indicates that the row is unique.  The slide also shows how to control which duplicate is kept using the `keep` parameter in the `duplicated()` method.  The options are 'first', 'last', or False.  The default is 'first'.  The purpose of this slide is to demonstrate how to identify duplicate rows in a DataFrame and how to control which duplicate is kept.  The output is a crucial part of the slide, showing the boolean Series that identifies duplicates.  The comments explain the different options for the `keep` parameter.


```markdown
# Slide 25

**Identifying Duplicates**

You don't want to spend time comparing elements to find those duplicates

`df_employee.duplicated()`

This returns a boolean Series to identify duplicates

There are options for which elements you identify

`df_employee.duplicated(keep=<option>)`

# 'first': all but first element are marked True
# 'last': all but last element are marked True
# False: all elements are marked True
# first is the default

0     False
1     False
2     False
3     False
4     False
5     False
6     False
7     False
8     False
9     False
10    False
11    False
12    False
13     True
14    False
15     True
16     True
17     True
18    False
19     True
dtype: bool


```

**Description of the Slide:**

This slide, part of a data science lecture, continues the discussion of identifying duplicate rows in a DataFrame.  It builds on the previous slide's concept of using the `duplicated()` method to identify duplicates.  The slide shows a table with the output of `df_employee.duplicated()`.  The table displays a boolean Series, where each row corresponds to a row in the `df_employee` DataFrame.  A value of `True` indicates that the corresponding row is a duplicate, and `False` indicates that the row is unique.  The slide also explains the different options for the `keep` parameter in the `duplicated()` method, which allows you to control which duplicate is considered the "keep" row.  The options are 'first', 'last', or False.  The default is 'first'.  The purpose of this slide is to demonstrate how to identify duplicate rows and control which duplicate is kept.  The table is crucial for showing the boolean Series output of the `duplicated()` method.  The comments explain the different options for the `keep` parameter.  The slide is part of a larger lecture on data cleaning and manipulation techniques.


# Slide 26

**Using the booleans as a selector**

You can use a boolean Series to select elements from the data frame
`df_employee.loc[df_employee.duplicated()]`

This may also be a custom method applied to the data frame, as long as it returns the correct number of boolean values.

|  | First Name | ID | Last Login Time | Team |
|---|---|---|---|---|
| 13 | Benjamin | 420 | 5/1/20 22:06 | Legal |
| 16 | Douglas | 785 | 5/1/20 12:42 | Marketing |
| 17 | Ruby | 331 | 5/1/20 16:20 | Product |
| 19 | Benjamin | 420 | 5/1/20 22:06 | Legal |


---

**Description of the Slide:**

This slide builds on the previous one, demonstrating how to use the boolean Series returned by `df_employee.duplicated()` to select duplicate rows from the `df_employee` DataFrame.  The code `df_employee.loc[df_employee.duplicated()]` is shown, selecting rows where the `duplicated()` method returns `True`.  The output is a table showing the duplicate rows.  The table has columns for "First Name", "ID", "Last Login Time", and "Team".  Each row in the table represents a duplicate entry from the original `df_employee` DataFrame.  The slide emphasizes that this method can be adapted to use any custom method that returns a boolean Series of the correct length.  The purpose of this slide is to show how to use boolean indexing to select specific rows based on a condition (in this case, duplicate rows).  The table is crucial for demonstrating the result of the boolean indexing operation.  The comments explain the broader context of using boolean Series for selection.


# Slide 27

**Removing duplicates**

You could use this as a selection
`df_employee.loc[df_employee.duplicated() == False]`
Or use `drop_duplicates`, with the same keep variable
`df_employee.drop_duplicates(keep='first')`

Just remember, if you want to keep it, you have to save it
`dfe_nodups = df_employee.drop_duplicates()`

|  | First Name | ID | Last Login Time | Team |
|---|---|---|---|---|
| 0 | Douglas | 785 | 5/1/20 12:42 | Marketing |
| 1 | Jerry | 222 | 5/2/20 5:50 | Finance |
| 2 | Christina | 543 | 5/2/20 17:19 | Engineering |
| 3 | Thomas | 234 | 5/1/20 6:53 | Legal |
| 4 | Maria | 102 | 5/3/20 11:17 | Finance |
| 5 | Jerry | 222 | 5/1/20 17:00 | Finance |
| 6 | Larry | 366 | 5/1/20 16:47 | Client Services |
| 7 | Maria | 478 | 5/2/20 11:17 | Finance |
| 8 | Dennis | 189 | 5/2/20 1:35 | Legal |
| 9 | Ruby | 331 | 5/1/20 16:20 | Product |
| 10 | Benjamin | 420 | 5/1/20 22:06 | Legal |
| 11 | Frances | 682 | 5/2/20 6:51 | Business Development |
| 12 | Terry | 777 | 5/1/20 18:30 | Client Services |
| 14 | Christina | 543 | 5/2/20 13:19 | Engineering |
| 15 | Jerry | 222 | 5/1/20 13:00 | Finance |
| 17 | Ruby | 331 | 5/1/20 16:20 | Product |
| 18 | John | 800 | 5/1/20 12:00 | Business Development |


---

**Description of the Slide:**

This slide, part of a data science lecture, focuses on removing duplicate rows from a DataFrame called `df_employee`.  The slide presents two methods for achieving this:

1.  **`df_employee.loc[df_employee.duplicated() == False]`**: This method uses boolean indexing to select only the rows where the `duplicated()` method returns `False`, effectively keeping only the unique rows.
2.  **`df_employee.drop_duplicates(keep='first')`**: This method directly removes duplicate rows, keeping only the first occurrence of each duplicate.  The `keep='first'` argument specifies that the first occurrence of a duplicate should be retained.
3.  **`dfe_nodups = df_employee.drop_duplicates()`**: This method removes all duplicate rows and assigns the result to a new variable `dfe_nodups`.

The slide includes a table displaying the unique rows after applying the `drop_duplicates` method.  The table shows the "First Name", "ID", "Last Login Time", and "Team" columns.  The purpose of this slide is to demonstrate how to remove duplicate rows from a DataFrame using different methods and to emphasize the importance of saving the result if you want to keep the unique data.  The table is crucial for showing the result of the duplicate removal operation.


# Slide 28

```markdown
Replacing Values
When you read in a new data set, look over it for unusual values
df_weather = pd.read_csv('weather_march2020.csv')
df_weather
Date	Highest Temperature (Deg F)	Lowest Temperature	Weather
0	3/6/20	38	24	Cloudy
1	3/7/20	50	19	Sunny
2	3/8/20	-999	-999	None
3	3/9/20	-999	50	Rain
4	3/10/20	57	-999	Cloudy
5	3/11/20	45	37	Cloudy
6	3/12/20	-1111	41	None
7	3/13/20	54	-999	Sunny
8	3/14/20	-999	34	Snow
9	3/17/20	53	40	Cloudy
It could be typos, special coded values, a different way of noting missing data, etc.
```

---

**Description of the Slide:**

This slide presents a table of weather data, likely from a CSV file named 'weather_march2020.csv'.  The table has four columns: "Date", "Highest Temperature (Deg F)", "Lowest Temperature", and "Weather".  Each row represents a day's weather data, including the date, the highest temperature, the lowest temperature, and the type of weather.  The data is displayed in a tabular format, with rows and columns clearly defined.  The text above the table, "Replacing Values", suggests that the slide is part of a section on data cleaning, specifically dealing with unusual values in the dataset.  The text "When you read in a new data set, look over it for unusual values" indicates that the slide is intended to highlight the importance of inspecting data for anomalies.  The presence of the value "-999" in several temperature columns suggests that this value might be a placeholder for missing data or an error in the original data.  The purpose of the slide is to introduce a sample dataset and highlight the need to check for unusual values, which is a crucial step in data cleaning.




# Slide 29

**Replacing Values**

We could edit the data frame for each location and replace values specifically
… that’s tedious, particularly if we have a large dataset.
Python and Pandas offer some useful tools to do this systematically
# NumPy is the package for scientific computing with Python.
```python
import numpy as np
df_weather.replace(-999, np.NaN)
```
Date	Highest Temperature (Deg F)	Lowest Temperature	Weather
0	3/6/20	38	24	Cloudy
1	3/7/20	50	19	Sunny
2	3/8/20	NaN	NaN	None
3	3/9/20	NaN	50	Rain
4	3/10/20	57	NaN	Cloudy
5	3/11/20	45	37	Cloudy
6	3/12/20	-1111	41	None
7	3/13/20	54	NaN	Sunny
8	3/14/20	NaN	34	Snow
9	3/17/20	53	40	Cloudy


---

**Description of the Slide:**

This slide demonstrates how to replace specific values in a DataFrame, `df_weather`, likely containing weather data.  The code `df_weather.replace(-999, np.NaN)` is shown, replacing all occurrences of -999 with NaN (Not a Number), which is often used to represent missing or invalid data.  The slide includes a table displaying the `df_weather` DataFrame *after* the replacement.  The table shows the "Date", "Highest Temperature (Deg F)", "Lowest Temperature", and "Weather" columns.  The presence of NaN values in the temperature columns indicates that the code successfully replaced the -999 values.  The purpose of this slide is to show how to systematically replace values in a DataFrame using the `replace()` method, which is a common data cleaning technique.  The table is crucial for demonstrating the effect of the replacement operation on the data.  The code snippet is essential for demonstrating the replacement process.  The context suggests that this slide follows a discussion on data cleaning and the need to handle unusual values in datasets.


# Slide 30

**More Replacing**

Of course, why use a single value, when you can use a list...

```
df_weather.replace([-999, -1111, 'None'], np.NaN)
```

Or for custom values, use a dictionary

```
df_weather.replace({-999:np.NaN, -1111:np.NaN, 'None':'N/A'})
```

```
Date	Highest Temperature (Deg F)	Lowest Temperature	Weather
0	3/6/20	38	24	Cloudy
1	3/7/20	50	19	Sunny
2	3/8/20	NaN	NaN	N/A
3	3/9/20	NaN	50	Rain
4	3/10/20	57	NaN	Cloudy
5	3/11/20	45	37	Cloudy
6	3/12/20	NaN	41	N/A
7	3/13/20	54	NaN	Sunny
8	3/14/20	NaN	34	Snow
9	3/17/20	53	40	Cloudy
```

---

**Description of the Slide:**

This slide continues the discussion of replacing values in a DataFrame, specifically the `df_weather` DataFrame, which likely contains weather data.  The slide shows how to replace multiple values simultaneously using a list in the `replace()` method.  The code `df_weather.replace([-999, -1111, 'None'], np.NaN)` replaces the values -999, -1111, and 'None' with NaN (Not a Number).  The slide also demonstrates how to use a dictionary for more complex replacements, as shown in the second code example.  The dictionary `{-999:np.NaN, -1111:np.NaN, 'None':'N/A'}` replaces -999 and -1111 with NaN and 'None' with 'N/A'.  The slide includes a table displaying the `df_weather` DataFrame after the replacements.  The table shows the "Date", "Highest Temperature (Deg F)", "Lowest Temperature", and "Weather" columns.  The presence of NaN values in the temperature columns and 'N/A' in the Weather column indicates that the replacements were successful.  The purpose of this slide is to show how to replace multiple values systematically using lists and dictionaries within the `replace()` method.  The table is crucial for demonstrating the effect of the replacement operations on the data.  The code snippets are essential for demonstrating the replacement process.  The context suggests that this slide follows a discussion on data cleaning and the need to handle unusual values in datasets.


# Slide 31

**One final replacement challenge**

Consider this smaller, but more complicated data frame

```
df_new
```

```
Temperature	Wind Speed	Weather
0	0	3	Snow
1	-999	0	0
2	45	5	Sunny
3	None	-999	Cloudy
```

Along with the obvious -999, we also want to replace 0 in the Weather column…

… but 0 is a valid value in Temperature and Wind Speed

So don’t use just a dictionary, use a dictionary of features and values!

```
df_new.replace({'Temperature': [-999, 'None'], 'Wind Speed': -999, 'Weather': 0}, np.NaN)
```

```
Temperature	Wind Speed	Weather
0	0.0	3.0	Snow
1	NaN	0.0	NaN
2	45.0	5.0	Sunny
3	NaN	NaN	Cloudy
```


---

**Description of the Slide:**

This slide presents a data cleaning challenge using a smaller DataFrame, `df_new`, with weather data.  The table shows the "Temperature", "Wind Speed", and "Weather" columns.  The data includes the values -999 and 0, which need to be replaced with NaN.  The slide emphasizes that 0 is a valid value for temperature and wind speed, so a simple replacement of 0 with NaN won't work.  The code `df_new.replace({'Temperature': [-999, 'None'], 'Wind Speed': -999, 'Weather': 0}, np.NaN)` is shown, demonstrating how to use a dictionary to specify the values to replace and the columns to replace them in.  The output table shows the DataFrame after the replacements, with the values -999, 'None', and 0 replaced with NaN.  The purpose of this slide is to illustrate a more complex data replacement scenario, requiring a dictionary to target specific columns and values.  The tables are crucial for demonstrating the input and output of the replacement operation. The code snippet is essential for demonstrating the replacement process.  The context suggests that this slide is part of a larger lecture on data cleaning and manipulation techniques.


# Slide 32

```markdown
So much replacing
With a complicated data set, you may find the need to do a lot of data frame manipulation
Rather than something like
df_weather_clean = df_weather.replace(...)
df_weather_cleaner = df_weather_clean.replace(...)
dfw_clean = df_weather_cleaner.replace(...)
dfw_final = dfw_clean.repace(...)
dfw_newFinal = ...
Consider using the inplace parameter to update the current data frame without having to re-assign
df_weather.replace({-999:np.NaN, -1111:np.NaN, 'None':'N/A'}, inplace=True)
```

---

**Description of the Slide:**

This slide discusses the potential for extensive data frame manipulation when dealing with complex datasets.  It suggests an alternative approach using the `inplace=True` parameter within the `replace()` method to avoid repeatedly re-assigning variables.  The slide shows several example variable assignments, illustrating the typical repetitive nature of data cleaning tasks.  The final line, `df_weather.replace({-999:np.NaN, -1111:np.NaN, 'None':'N/A'}, inplace=True)`, emphasizes the use of `inplace=True` to modify the DataFrame directly, avoiding the need to create new variables at each step.  There are no figures on this slide.  The text is entirely text-based, focusing on the concept of data manipulation and efficiency.


# Slide 33

**Missing Data**

---

**Description of the Slide:**

This slide, a plain gray background, has the single, bold, centered title "Missing Data" in a dark gray font.  It's a simple slide, likely serving as a heading for the next section of the lecture, which will cover techniques for handling missing data in a data science context.  There are no figures or other elements on this slide.


**What you see:**

A plain, light gray background with the title "Missing Data" centered in a dark gray, sans-serif font.  The title is large enough to be prominent but not overly large.  The slide is entirely text-based.


```markdown
# Slide 34

**Missing Grades**

```
data2 = pd.read_csv("grades.csv")
data2
```

A table is presented, displaying data read from a CSV file named "grades.csv".  The table has columns for "Lastname", "Firstname", "ID", "Test1", "Test2", "Test3", "Test4", and "Final Grade".  Each row represents a student's record, including their name, ID, and test scores.  The data appears to be student grades.

```
NaN can be good in some cases,
it's mostly ignored.
Here, we want a value
```

These lines provide context.  The text "NaN can be good in some cases, it's mostly ignored" suggests that the slide is discussing how to handle missing values (NaN) in the data.  The line "Here, we want a value" implies that the subsequent content will address strategies for dealing with missing data, potentially by replacing or imputing values.


---

**Description of the Slide:**

This slide introduces a dataset of student grades, likely from a CSV file.  The data is presented in a table format, showing student names, IDs, and test scores.  The presence of "NaN" values in the table suggests that some data is missing.  The text below the table indicates that the slide will discuss how to handle missing values in the dataset.  The slide is part of a larger lecture on data cleaning and manipulation techniques.
```
```


# Slide 35

**Finding Missing Grades**

```
data2 = pd.read_csv("grades.csv")
data2
```

A table is presented, displaying data read from a CSV file named "grades.csv".  The table has columns for "Lastname", "Firstname", "ID", "Test1", "Test2", "Test3", "Test4", and "Final Grade".  Each row represents a student's record, including their name, ID, and test scores.  The data appears to be student grades.

```
NaN can be good in some cases,
it's mostly ignored.
Here, we want a value
```

These lines provide context.  The text "NaN can be good in some cases, it's mostly ignored" suggests that the slide is discussing how to handle missing values (NaN) in the data.  The line "Here, we want a value" implies that the subsequent content will address strategies for dealing with missing data, potentially by replacing or imputing values.

```
Check for null, sum them toconvert True to 1
df_grades.isnull().sum()
Use isnull to filter your data frame
df_grades[df_grades.Test2.isnull()]
```

These lines contain code snippets that will be used to find and filter rows with missing values in the "Test2" column of a DataFrame called `df_grades`.


```
Lastname Firstname
ID Test1 Test2 Test3 Test4 Final Grade
8 Airpump Andrew airpump100 49 NaN 90.0 100.0 83 B
9 Backus Jim backj 48 NaN 97.0 96.0 97 B
10 Carnivore Art carn400 44 NaN 80.0 60.0 40 D+
```

A smaller table is presented, showing a subset of the data from the larger table.  This subset focuses on rows where the "Test2" column has a missing value (NaN).  The table shows the "Lastname", "Firstname", "ID", "Test1", "Test2", "Test3", "Test4", and "Final Grade" columns for those rows.


---

**Description of the Slide:**

This slide introduces a student grade dataset (`grades.csv`) and demonstrates how to identify and filter rows with missing values (NaN) in a Pandas DataFrame.  The slide shows how to use the `.isnull().sum()` method to count missing values in each column and the `.isnull()` method to filter rows based on missing values in a specific column.  The example focuses on the "Test2" column, highlighting the rows where this column contains missing values.  The slide is part of a larger lecture on data cleaning and manipulation techniques, specifically addressing the handling of missing data in a student grade dataset.  The tables are crucial for demonstrating the data and the results of the filtering operations. The code snippets are essential for demonstrating the data analysis process.


# Slide 36

**Filling Missing Values**

How is this missing data different than replacing data?
Ideally, you're able to find the data, and update it directly

# `df_grades` entries change
`df_grades.iloc[8, 4] = 50.0`
`df_grades.iloc[9, 4] = 35.0`
`df_grades.iloc[10, 4] = 40.0`
`df_grades.iloc[6, 5] = 70.0`
`df_grades.iloc[14, 6] = 65.0`

Can't we just drop a zero in and be done?
Yes. There's even a dedicated method to do so:
`df_grades.fillna(0)`

For this data set, if you don't have the grades a 0 would be appropriate. That's not true in all situations.

```
Lastname	Firstname	ID	Test1	Test2	Test3	Test4	Final Grade
8	Airpump	Andrew	airpump100	49	NaN	90.0	100.0	83
9	Backus	Jim	backj	48	NaN	97.0	96.0	97
10	Carnivore	Art	carn400	44	NaN	80.0	60.0	40
```


---

**Description of the Slide:**

This slide continues the discussion of handling missing data in a student grade dataset.  It focuses on filling in missing numerical values (NaN) in the "Test2" column of the `df_grades` DataFrame.  The slide shows several lines of code that demonstrate how to update specific cells in the DataFrame with new values.  The code uses `iloc` to target specific rows and columns and assign new values.  The slide also introduces the concept of filling missing values with a default value (0) using the `fillna()` method.  The example shows a small portion of the DataFrame, highlighting the rows where the "Test2" column has missing values.  The purpose of this slide is to demonstrate how to fill in missing numerical values in a DataFrame using the `iloc` method and the `fillna()` method.  The code snippets are crucial for demonstrating the data manipulation process. The table is essential for showing the effect of the code on the data, specifically the filling of missing values.  The context suggests that this slide is part of a larger lecture on data cleaning and manipulation techniques.


# Slide 37

```markdown
# Dealing with Missing Values

This slide presents weather data from West Lafayette, spanning from March 6th to March 17th, 2020.  The data is loaded into a Pandas DataFrame called `df_weather`.

```
df_weather = pd.read_csv('weather_march2020.csv')
df_weather
```

A table follows, displaying the weather data.  The table has columns for "Date", "Highest Temperature (Deg F)", "Lowest Temperature (Deg F)", and "Weather".  Each row represents a day's weather record.  The data includes numerical values for temperature and categorical values for weather conditions.  Noticeable in the table are several instances of `NaN` values, indicating missing data points for temperature.

```
# ... (code to handle missing values)
```

The text below the table suggests that the slide will discuss strategies for handling these missing values.  The context implies that the following content will address methods for replacing or imputing the missing temperature data.


---

**Description of the Slide:**

This slide introduces a weather dataset, `weather_march2020.csv`, and displays the data loaded into a Pandas DataFrame.  The table shows the weather data for each day, including the date, highest temperature, lowest temperature, and weather condition.  The presence of NaN values in the temperature columns highlights the need for data cleaning and handling missing values.  The slide is part of a larger lecture on data cleaning and manipulation techniques, specifically addressing the handling of missing data in a weather dataset.  The table is crucial for demonstrating the data and the presence of missing values. The code snippets are essential for demonstrating the data analysis process.


# Slide 38

**Timestamps and Re-indexing**

Check the 'Date' column for type
```python
type(df_weather.Date[0])
```
str
Convert this to a timestamp when reading in the data to gain some sorting and indexing advantages
```python
df_weather = pd.read_csv('weather_march2020.csv',
             parse_dates=['Date'])
```
```python
type(df_weather.Date[0])
```
pandas._libs.tlibs.timestamps.Timestamp
And now re-index
```python
df_weather.set_index(Date, inplace=True)
df_weather
```

| Date       | Highest Temperature (Deg F) | Lowest Temperature (Deg F) | Weather |
|------------|-----------------------------|----------------------------|---------|
| 2020-03-06 | 38.0                        | 24.0                       | Cloudy  |
| 2020-03-07 | 50.0                        | 19.0                       | Sunny   |
| 2020-03-08 | NaN                         | NaN                        | NaN     |
| 2020-03-09 | NaN                         | 50.0                       | Rain    |
| 2020-03-10 | 57.0                        | NaN                        | Cloudy  |
| 2020-03-11 | 45.0                        | 37.0                       | Cloudy  |
| 2020-03-12 | NaN                         | 41.0                       | NaN     |
| 2020-03-13 | 54.0                        | NaN                        | Sunny   |
| 2020-03-14 | NaN                         | 34.0                       | Snow    |
| 2020-03-17 | 63.0                        | 45.0                       | Cloudy  |


---

**Description of the Slide:**

This slide demonstrates how to convert a 'Date' column in a Pandas DataFrame (likely `df_weather`) to a datetime format (timestamp) and then set the 'Date' column as the index.  The code snippets show how to read a CSV file ('weather_march2020.csv') into a DataFrame, parsing the 'Date' column as dates, and then setting the 'Date' column as the index of the DataFrame.  The output is a table showing the DataFrame after the conversion and re-indexing.  The table has columns for 'Date', 'Highest Temperature (Deg F)', 'Lowest Temperature (Deg F)', and 'Weather'.  The 'Date' column now contains datetime objects, which is a crucial step for time-series analysis.  The purpose of this slide is to show how to work with date/time data in a Pandas DataFrame.  The code snippets are essential for demonstrating the process. The table is essential for showing the effect of the code on the data, specifically the conversion of the 'Date' column to a datetime format and the re-indexing.  The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on time-series data.




# Slide 39

**An easy solution**

What happens if we just add 0's for missing data?

```python
df_weather.fillna({'Highest Temperature (Deg F)': 0,
                   'Lowest Temperature (Deg F)': 0,
                   'Weather': 'N/A'})
```

| Date       | Highest Temperature (Deg F) | Lowest Temperature (Deg F) | Weather |
|------------|-----------------------------|----------------------------|---------|
| 2020-03-06 | 38.0                        | 24.0                       | Cloudy  |
| 2020-03-07 | 50.0                        | 19.0                       | Sunny   |
| 2020-03-08 | 0.0                         | 0.0                        | N/A     |
| 2020-03-09 | 0.0                         | 50.0                       | Rain    |
| 2020-03-10 | 57.0                        | 0.0                        | Cloudy  |
| 2020-03-11 | 45.0                        | 37.0                       | Cloudy  |
| 2020-03-12 | 0.0                         | 41.0                       | N/A     |
| 2020-03-13 | 54.0                        | 0.0                        | Sunny   |
| 2020-03-14 | 0.0                         | 34.0                       | Snow    |
| 2020-03-17 | 63.0                        | 45.0                       | Cloudy  |


---

**Description of the Slide:**

This slide demonstrates a simple solution for handling missing values in a weather dataset.  The code `df_weather.fillna({'Highest Temperature (Deg F)': 0, 'Lowest Temperature (Deg F)': 0, 'Weather': 'N/A'})` fills all missing values in the specified columns with 0 for numerical columns and 'N/A' for the 'Weather' column.  The output is a table showing the DataFrame after the `fillna()` operation.  The table displays the "Date", "Highest Temperature (Deg F)", "Lowest Temperature (Deg F)", and "Weather" columns.  Each row represents a day's weather record.  The presence of 0's in the temperature columns and 'N/A' in the 'Weather' column indicates that the missing values have been replaced.  The purpose of this slide is to illustrate a straightforward method for filling missing values with default values.  The table is crucial for demonstrating the effect of the `fillna()` operation on the data. The code snippet is essential for demonstrating the data manipulation process. The context suggests that this slide follows a discussion on handling missing data in a weather dataset.


# Slide 40

**More complexity**

0 is a bad default value in this case because 0 is an arbitrary temperature.
Analyzing trends across days, the 0 will have an notable impact.
Smooth over missing data using `ffill` (forward fill) to push previous values forward into the empty spots
```python
df_weather.fillna(method='ffill')
```
Or also, `bfill`
```python
df_weather.fillna(method='bfill')
```

**Additional useful points:**
`axis=1` to `ffill` or `bfill` on the row
`limit=1` to only allow it to push forward one row/col

| Date       | Highest Temperature (Deg F) | Lowest Temperature (Deg F) | Weather |
|------------|-----------------------------|----------------------------|---------|
| 2020-03-06 | 38.0                        | 24.0                       | Cloudy  |
| 2020-03-07 | 50.0                        | 19.0                       | Sunny   |
| 2020-03-08 | 50.0                        | 19.0                       | Sunny   |
| 2020-03-09 | 50.0                        | 50.0                       | Rain    |
| 2020-03-10 | 57.0                        | 50.0                       | Cloudy  |
| 2020-03-11 | 45.0                        | 37.0                       | Cloudy  |
| 2020-03-12 | 45.0                        | 41.0                       | Cloudy  |
| 2020-03-13 | 54.0                        | 41.0                       | Sunny   |
| 2020-03-14 | 54.0                        | 34.0                       | Snow    |
| 2020-03-17 | 63.0                        | 45.0                       | Cloudy  |


| Date       | Highest Temperature (Deg F) | Lowest Temperature (Deg F) | Weather |
|------------|-----------------------------|----------------------------|---------|
| 2020-03-06 | 38.0                        | 24.0                       | Cloudy  |
| 2020-03-07 | 50.0                        | 19.0                       | Sunny   |
| 2020-03-08 | 57.0                        | 50.0                       | Rain    |
| 2020-03-09 | 57.0                        | 50.0                       | Rain    |
| 2020-03-10 | 57.0                        | 37.0                       | Cloudy  |
| 2020-03-11 | 45.0                        | 37.0                       | Cloudy  |
| 2020-03-12 | 54.0                        | 41.0                       | Sunny   |
| 2020-03-13 | 54.0                        | 34.0                       | Sunny   |
| 2020-03-14 | 63.0                        | 34.0                       | Snow    |
| 2020-03-17 | 63.0                        | 45.0                       | Cloudy  |


---

**Description of the Slide:**

This slide discusses the complexities of filling missing values in a time-series dataset (weather data).  It highlights that using a simple value like 0 to fill missing temperatures can introduce inaccuracies in trend analysis.  The slide presents two methods for handling missing data: `ffill` (forward fill) and `bfill` (backward fill).  It also introduces the concept of `axis=1` and `limit=1` for more controlled filling.  Two tables are shown, one using `ffill` and the other using `bfill`, demonstrating how these methods propagate values forward or backward.  The purpose is to illustrate the impact of different imputation methods on the data and the importance of considering the context of the data when handling missing values.  The code snippets are essential for demonstrating the data manipulation process. The tables are essential for showing the effect of the code on the data, specifically the filling of missing values. The context suggests that this slide is part of a larger lecture on data cleaning and manipulation techniques, specifically focusing on time-series data.




# Slide 41

**For even better estimates**

If your data is generally related from one element to the next, consider `interpolate`
Records with values are end points and missing values are filled in via linear methods
```python
df_weather.interpolate()  # # each record is equidistant
df_weather.interpolate(method='time')  # # spaced by time index
```

```
Highest Temperature (Deg F)	Lowest Temperature (Deg F)	Weather
Date
2020-03-06	38.0	24.0	Cloudy
2020-03-07	50.0	19.0	Sunny
2020-03-08	52.333333	34.5	NaN
2020-03-09	54.666667	50.0	Rain
2020-03-10	57.0	43.5	Cloudy
2020-03-11	45.0	37.0	Cloudy
2020-03-12	49.5	41.0	NaN
2020-03-13	54.0	37.5	Sunny
2020-03-14	56.25	34.0	Snow
2020-03-17	63.0	45.0	Cloudy
```

```
Highest Temperature (Deg F)	Lowest Temperature (Deg F)	Weather
Date
2020-03-06	38.0	24.0	Cloudy
2020-03-07	50.0	19.0	Sunny
2020-03-08	52.333333	34.5	NaN
2020-03-09	54.666667	50.0	Rain
2020-03-10	57.0	43.5	Cloudy
2020-03-11	45.0	37.0	Cloudy
2020-03-12	49.5	41.0	NaN
2020-03-13	54.0	37.5	Sunny
2020-03-14	58.5	34.0	Snow
2020-03-17	63.0	45.0	Cloudy
```


---

**Description of the Slide:**

This slide presents a method for interpolating missing values in a time-series dataset, specifically a weather dataset.  The code `df_weather.interpolate()` and `df_weather.interpolate(method='time')` are shown.  The first method assumes equidistant data points, while the second method uses the time index for interpolation.  The output shows two tables, each representing the interpolated DataFrame.  The tables display the "Date", "Highest Temperature (Deg F)", "Lowest Temperature (Deg F)", and "Weather" columns.  The interpolated values fill in the missing temperature data points based on the surrounding data points.  The purpose of this slide is to demonstrate how to use interpolation to estimate missing values in a time-series dataset, particularly when the data points are not equidistant.  The tables are crucial for demonstrating the effect of the interpolation on the data. The code snippets are essential for demonstrating the interpolation process. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on time-series data and handling missing values.




# Slide 42

**Other Strategies**

Every data set will have its own nuances, do what makes sense for your data and analysis.
That may involve writing a custom function to fill in data
It could be that no value makes sense and you have to drop the entire record
`df_weather.dropna()`
For this data set, that leaves very little to work with

| Date       | Highest Temperature (Deg F) | Lowest Temperature (Deg F) | Weather |
|------------|-----------------------------|----------------------------|---------|
| 2020-03-06 | 38.0                        | 24.0                       | Cloudy  |
| 2020-03-07 | 50.0                        | 19.0                       | Sunny   |
| 2020-03-11 | 45.0                        | 37.0                       | Cloudy  |
| 2020-03-17 | 63.0                        | 45.0                       | Cloudy  |


---

**Description of the Slide:**

This slide discusses alternative strategies for handling missing data in a weather dataset.  The text emphasizes that every dataset has unique characteristics, and the best approach to handling missing data depends on the specific dataset and the goals of the analysis.  The slide suggests that in some cases, dropping rows with missing values (`df_weather.dropna()`) might be the appropriate strategy.  A table is presented, showing a subset of the original weather data, with only the rows that have complete data.  The table displays the "Date", "Highest Temperature (Deg F)", "Lowest Temperature (Deg F)", and "Weather" columns.  The purpose of this slide is to introduce the concept of alternative strategies for handling missing data, beyond the methods discussed in previous slides.  The table is a key part of the slide, showing the data that would remain if the `dropna()` method were used.  The context suggests that this slide is part of a larger lecture on data cleaning and manipulation techniques, specifically focusing on handling missing values in time-series data.


# Slide 43

**Transformation**


---

**Description of the Slide:**

This slide, a plain, light gray background, has the single, bold, centered title "Transformation" in a dark gray font.  It's a simple slide, likely serving as a heading for the next section of the lecture, which will cover techniques for changing the format or structure of data in a data science context.  There are no figures or other elements on this slide.


**What you see:**

A plain, light gray background with the title "Transformation" centered in a dark gray, sans-serif font.  The title is large enough to be prominent but not overly large.  The slide is entirely text-based.


# Slide 44

**Lets return to Grades**

We filled in our missing data, but that doesn't recalculate our grades
```
df_grades
```

A table is presented, displaying data read from a CSV file.  The table has columns for "Lastname", "Firstname", "ID", "Test1", "Test2", "Test3", "Test4", and "Final Grade".  Each row represents a student's record, including their name, ID, and test scores.  The data appears to be student grades.

```
To do a meaningful average
df_grades.Test1 = (df_grades.Test1/50) * 100
```

These lines provide context.  The text "To do a meaningful average" suggests that the slide is discussing how to calculate a meaningful average for the test scores.  The code snippet shows how to adjust the Test1 scores to be out of 100.

```
Now calculate the average
df_grades['Grade'] = df_grades.loc[:, ['Test1', 'Test2', 'Test3', 'Test4', 'Final']].mean(axis=1)
```

This line of code calculates the average of the test scores (Test1, Test2, Test3, Test4, and Final) for each student and stores the result in a new column named 'Grade'.

```
For simpler slides
df_final = df_grades.filter(['Lastname', 'Firstname', 'ID', 'Grade'])
```

This line of code filters the `df_grades` DataFrame to include only the specified columns ('Lastname', 'Firstname', 'ID', and 'Grade') and assigns the result to a new DataFrame called `df_final`.

```
But how do we do categorical grades and letter grades?
```

This line indicates that the slide will likely discuss how to convert numerical grades to letter grades (e.g., A, B, C).


---

**Description of the Slide:**

This slide revisits the student grade data, now with the goal of calculating a meaningful average grade and potentially converting it to letter grades.  The slide shows code snippets for calculating the average grade and filtering the DataFrame to include only relevant columns.  The text prompts the question of how to handle categorical grades, suggesting the next steps in the data analysis process.  The slide is part of a larger lecture on data cleaning and manipulation techniques, specifically focusing on student grade data.  The tables are crucial for demonstrating the data and the results of the calculations. The code snippets are essential for demonstrating the data analysis process.


# Slide 45

**Lambda functions**

Lambda functions are functions without names

```
lambda x: x > 0
```

The term `lambda` creates a temporary function

`x` is the parameter name

The code following the `:` is the code body

It is often discarded after use


---

**Description of the Slide:**

This slide introduces the concept of lambda functions in a data science context.  It defines lambda functions as functions without names, and provides a simple example: `lambda x: x > 0`.  The slide explains that `lambda` creates a temporary function, `x` is the parameter, and the code following the colon (`:`) is the function's body.  The slide also notes that lambda functions are often used for short, simple operations and are discarded after use.  The slide is entirely text-based, with no figures or tables.  The purpose is to introduce a fundamental programming concept relevant to data science.


# Slide 46

**Mapping Lambda Functions**

We could round down, and later replace scores with letter grades
```
df_final.Grade.map(lambda x: (x // 10) * 10)
```
.. But I might want to something different
```
avgGrade = df_final.Grade.mean()
stdGrade = df_final.Grade.std()
print(avgGrade, stdGrade)
67.2125 20.978524733641308
```
It seems we might want to adjust our final grades.

```
0     90.0
1     90.0
2     60.0
3     50.0
4     70.0
5     80.0
6     40.0
7     40.0
8     80.0
9     80.0
10    60.0
11    40.0
12    70.0
13    90.0
14    30.0
15    30.0
Name: Grade, dtype: float64
```

---

**Description of the Slide:**

This slide demonstrates the use of lambda functions for data transformation, specifically on a 'Grade' column within a DataFrame (`df_final`).  The code `df_final.Grade.map(lambda x: (x // 10) * 10)` applies a lambda function to round down grades to the nearest 10.  The slide then calculates the average (`avgGrade`) and standard deviation (`stdGrade`) of the 'Grade' column and prints these values.  Finally, a table displays the original 'Grade' column values, which are floating-point numbers.  The purpose of the slide is to show how to use lambda functions for simple transformations and then calculate summary statistics on the transformed data.  The table is essential for demonstrating the input data. The code snippets are crucial for demonstrating the data analysis process. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on grade data.




# Slide 47

**Normalizing a feature**

So lets normalize those scores

```
df_final['Normalized'] = df_final.Grade.map(lambda x: (x - avgGrade)/stdGrade)
df_final.sort_values(['Normalized'])
```

First, lets make the average 77

```
df_final.Grade += 10
```

The deviation looks ok. Lets set some boundaries


---

**Description of the Slide:**

This slide describes the process of normalizing a feature, specifically the 'Grade' column in a DataFrame (`df_final`).  The code snippet `df_final['Normalized'] = df_final.Grade.map(lambda x: (x - avgGrade)/stdGrade)` normalizes the 'Grade' column by subtracting the average grade (`avgGrade`) from each value and then dividing by the standard deviation (`stdGrade`).  The result is stored in a new column named 'Normalized'.  The subsequent line `df_final.sort_values(['Normalized'])` sorts the DataFrame by the 'Normalized' column.  The line `df_final.Grade += 10` adds 10 to each value in the 'Grade' column.  The final sentence, "The deviation looks ok. Lets set some boundaries", suggests that the slide is part of a larger discussion about data analysis and preparing data for further analysis or modeling.  The slide is entirely text-based, with no figures or tables.  The purpose is to explain the normalization process and the steps involved.




# Slide 48

**Binning Data**

Binning data cuts an otherwise continuous numerical feature into a discrete category
First set cut boundaries, then cut your data
```
bins = [-1, 59, 69, 89, 110]
cats = pd.cut(df_final.Grade, bins)
print(cats)
print(pd.value_counts(cats))
```
Now we have categorical data, but the names could use work

```
0      (89, 110]
1      (89, 110]
2       (69, 89]
3       (-1, 59]
4       (69, 89]
5       (69, 89]
6       (-1, 59]
7       (-1, 59]
8       (69, 89]
9       (69, 89]
10      (59, 69]
11      (-1, 59]
12      (69, 89]
13     (89, 110]
14      (-1, 59]
15      (-1, 59]
Name: Grade, dtype: category
Categories (4, interval[int64]): [(-1, 59] < (59, 69] < (69, 89] < (89, 110]]
```

```
(89, 110]    7
(-1, 59]     5
(69, 89]     3
(59, 69]     1
Name: Grade, dtype: int64
```

---

**Description of the Slide:**

This slide demonstrates binning data, a technique used to transform a continuous numerical feature (like a student's grade) into a categorical feature.  The code `bins = [-1, 59, 69, 89, 110]` defines the boundaries for the bins.  `cats = pd.cut(df_final.Grade, bins)` applies the binning to the 'Grade' column of the `df_final` DataFrame.  The output shows the resulting categorical data (`cats`) and the counts of values in each bin (`pd.value_counts(cats)`).  The output includes a Series of categories and a Series of counts for each category.  The purpose of this slide is to show how to bin a continuous variable into discrete categories using Pandas.  The code snippets are essential for demonstrating the binning process. The output is crucial for understanding the distribution of the data after binning. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on transforming data.




# Slide 49

**Naming Categories**

Use a separate list for category names

```
# bins = [-1, 59, 69, 89, 110]
bin_names = ['Fail', 'Pass', 'Good', 'Excellent']
df_final['Category'] = pd.cut(df_final.Grade, bins, labels=bin_names)
df_final
```

A table is presented, displaying the `df_final` DataFrame.  The table has columns for "Lastname", "Firstname", "ID", "Grade", "Normalized", and "Category".  Each row represents a student's record, including their name, ID, calculated grade, normalized grade, and categorized grade.  The "Category" column is a new categorical column, likely representing a letter grade (e.g., Fail, Pass, Good, Excellent) based on the numerical grade in the "Grade" column.  The data appears to be student grades.

```
# Get the numbers grades of each bin by bin name
cats = pd.cut(df_final.Grade, bins, labels=bin_names)
pd.value_counts(cats)
```

This code snippet calculates the counts of students in each category (Fail, Pass, Good, Excellent).  The output is a Series showing the counts for each category.

```
Excellent    7
Fail         6
Pass         2
Good         1
Name: Grade, dtype: int64
```

This output shows the counts of students in each category.  For example, there are 7 students with an "Excellent" grade.


---

**Description of the Slide:**

This slide demonstrates how to categorize numerical student grades into categorical bins (e.g., Fail, Pass, Good, Excellent).  The code defines bins and category names, then uses `pd.cut` to assign categories to the 'Grade' column in the `df_final` DataFrame.  The output shows the updated DataFrame with the new 'Category' column and a summary of the counts for each category.  The slide is part of a larger lecture on data manipulation and analysis, specifically focusing on transforming numerical data into categorical data.  The table is crucial for demonstrating the categorized data. The code snippets are essential for demonstrating the data analysis process. The output is a Series showing the counts of students in each category.


# Slide 50

**Just to complete the story**

Re-bin the grades and add a 'Final Grade' column as letter grades

```
bins = [0, 59, 69, 79, 89, 100, 110]
letter_grades = ['F', 'D', 'C', 'B', 'A', 'A+']
df_final['Final Grade'] = pd.cut(df_final.Grade, bins, labels=letter_grades)
df_final
```

A table is presented, displaying the `df_final` DataFrame.  The table has columns for "Lastnam", "Firstname", "ID", "Grade", "Normalized", "Category", and "Final Grade".  Each row represents a student's record, including their name, ID, calculated grade, normalized grade, categorized grade, and the final letter grade.  The "Final Grade" column is a new categorical column, representing a letter grade (e.g., F, D, C, B, A, A+) based on the numerical grade in the "Grade" column.  The data appears to be student grades.

---

**Description of the Slide:**

This slide completes the process of transforming student grades by re-binning them into letter grades.  The code snippet `df_final['Final Grade'] = pd.cut(df_final.Grade, bins, labels=letter_grades)` adds a new column named 'Final Grade' to the `df_final` DataFrame.  This column contains letter grades corresponding to the numerical grades in the 'Grade' column, using the predefined `bins` and `letter_grades` lists.  The output is a table showing the updated `df_final` DataFrame, which now includes the 'Final Grade' column.  The purpose of this slide is to demonstrate the final step in transforming the numerical grades into letter grades.  The table is crucial for demonstrating the final result of the data transformation. The code snippet is essential for demonstrating the data analysis process. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on transforming numerical data into categorical data.




# Slide 51

**Aggregating Data**


---

**Description of the Slide:**

This slide, a plain, light gray background, has the single, bold, centered title "Aggregating Data" in a dark gray font.  It's a simple slide, likely serving as a heading for the next section of the lecture, which will cover techniques for grouping and summarizing data in a data science context.  There are no figures or other elements on this slide.


**What you see:**

A plain, light gray background with the title "Aggregating Data" centered in a dark gray, sans-serif font.  The title is large enough to be prominent but not overly large.  The slide is entirely text-based.


# Slide 52

**The idea of Split/Apply/Combine**

```
Split
Key Data
A	0
B	5
C	10
A	5
B	10
C	15
A	10
B	15
C	20
```

```
Apply / Combine
Sum
A	0
A	5
A	10
A	15
B	5
B	10
B	15
B	30
C	10
C	15
C	20
C	45
```

A diagram is presented, illustrating the Split/Apply/Combine paradigm.  The diagram shows three stages: Split, Apply, and Combine.  The Split stage involves dividing the data into groups based on a key.  The Apply stage involves applying a function (in this case, Sum) to each group.  The Combine stage involves combining the results from the Apply stage.  The diagram visually represents how data is divided, processed, and then aggregated.  The key data (A, B, C) are shown in the left column, and the sums are shown in the right column.  Arrows connect the key data to the corresponding sums, visually representing the application of the function.  The diagram is centrally located on the slide and is oriented horizontally.  The purpose of the diagram is to illustrate the concept of the Split/Apply/Combine paradigm, a common approach in data analysis for processing data in groups.


**Description of the Slide:**

This slide presents a visual representation of the Split-Apply-Combine paradigm, a common approach in data analysis.  The diagram shows how data is split into groups based on a key, then a function (in this case, "Sum") is applied to each group, and finally the results are combined.  The diagram uses arrows to visually connect the key data to the corresponding sums, making the process clear.  The slide is part of a lecture on data analysis techniques, specifically focusing on aggregation and data manipulation.  The diagram is the central element of the slide, and its purpose is to illustrate the concept of the Split-Apply-Combine paradigm.


# Slide 53

```markdown
Group By
DataFrame.groupby(by=None, axis=0, level=None, as_index=True,
    sort=True, group_keys=True, squeeze=<>, observed=False,
    dropna=True)
• by: mapping, function, label, or list of labels - determines which records to group
• as_index: For aggregated output, use the group label as the index
• Returns: A groupby object
Iterating over the groupbby object returns a tuple
(<Grouped Value>, <DataFrame filtered by Grouped Value>)
```

---

**Description of the Slide:**

This slide presents the documentation for the `groupby()` method in Pandas.  It shows the parameters and return value of the method, which is used for grouping data in a DataFrame.  The text describes the `by` parameter as determining which records to group, and the `as_index` parameter as controlling whether the group labels become the index of the resulting object.  The `Returns` section explains that `groupby()` returns a `groupby` object, which, when iterated over, yields tuples containing the grouped value and the corresponding filtered DataFrame.  The slide is entirely text-based, containing no figures or tables.  The purpose is to provide a reference for using the `groupby()` method in data analysis.




# Slide 54

**Simple grouping**

Look at our stock dataset

```
df_stock = pd.read_csv('stocks.csv')
df_stock = pd.read_csv('stocks.csv', parse_dates=['Date'])
```

Use `groupby` to split your DataFrame systematically

```
grouped_stock = df_stock.groupby('Symbol')
for stock, stock_df in grouped_stock:
    print(stock)
    display(stock_df)
```

Or pull a specific group as needed:

```
grouped_stock.get_group('GOOG')
```

A series of tables are presented, each representing a portion of the `df_stock` DataFrame grouped by the 'Symbol' column.  The tables are arranged in a way that visually demonstrates the grouping.  For example, one table might show all rows where the 'Symbol' is 'AAPL', another where it's 'GOOG', and another where it's 'MSFT'.  Each table shows the 'Date', 'Close', 'Volume', and 'Symbol' columns.  The tables are arranged vertically, with each table representing a different group.  The purpose of these tables is to illustrate how the `groupby` method divides the data into distinct groups based on the 'Symbol' column.  The code snippets are essential for demonstrating the grouping process.


---

**Description of the Slide:**

This slide demonstrates how to group a DataFrame (`df_stock`) by a specific column ('Symbol') using the `groupby()` method in Pandas.  The code shows how to read a CSV file into a DataFrame and then group the data.  The output consists of multiple tables, each showing the data for a particular stock symbol (e.g., AAPL, GOOG, MSFT).  The tables are arranged vertically, with each table representing a different group.  The purpose of the slide is to illustrate the `groupby()` method and how it partitions a DataFrame into smaller DataFrames based on a specified column.  The code snippets are essential for demonstrating the grouping process. The tables are essential for showing the effect of the grouping on the data. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on grouping and aggregating data.


# Slide 55

**Applying Summary Functions**

Once you have grouped your data, summary functions produce an aggregated DataFrame.  Using `max`, you get the max in each column, with the `Symbol` as the index.  `grouped_stock.max()`

```
Symbol
Close      Volume
AAPL  455.61  50000000
GOOG  1500.10 1995400
MSFT  216.35 32656800
```

Whereas using `mean`, you only get the columns where a mean makes sense. `grouped_stock.mean()`

```
Symbol
Close      Volume
AAPL  446.770000  4.316667e+07
GOOG  1489.400000  1.850500e+06
MSFT  213.923333  2.976833e+07
```

These all return new DataFrames, and can be accessed as you can with any other DataFrame.


---

**Description of the Slide:**

This slide demonstrates how to apply summary functions (like `max` and `mean`) to grouped data in a Pandas DataFrame.  The example uses a stock data set (`df_stock`) grouped by `Symbol`.  The `grouped_stock.max()` function returns a new DataFrame containing the maximum values for each column within each group (stock symbol).  Similarly, `grouped_stock.mean()` returns a new DataFrame with the mean values for numeric columns within each group.  The output shows the resulting DataFrames, clearly displaying the maximum and mean values for the 'Close' and 'Volume' columns for each stock symbol.  The slide emphasizes that these summary functions return new DataFrames, which can be further manipulated or analyzed.  The code snippets are essential for demonstrating the aggregation process. The tables are crucial for showing the results of the summary functions applied to the grouped data. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on grouping and aggregating data.




# Slide 56

**Combining Data**

This slide is a title slide, likely introducing the next section of the lecture on data manipulation and analysis.  It's a simple slide with a single, bold, centered title: "Combining Data".  There are no figures or tables on this slide.


**Description of the Slide:**

The slide is a plain, light gray background with the title "Combining Data" centered in a dark gray, sans-serif font.  The title is large enough to be prominent but not overly large.  The slide is entirely text-based and serves as a heading for the next section of the lecture.


# Slide 57

**Simply Concatenating**

The most basic combination is concatenation
* Similar to, but not quite as simple as, String concatenation
* Take all the elements and append them - but where?
Are you adding records?
Are you adding features?


---

**Description of the Slide:**

This slide introduces the concept of concatenating data, likely in a data science context.  It's a title slide for a section on combining data.  There are no figures or tables.  The text prompts the user to consider whether they are adding rows (records) or columns (features) when concatenating data.  The purpose is to introduce the topic of concatenation and to encourage the user to think about the context of the operation.  The gray boxes are likely visual aids to help the user understand the different ways data can be combined.  The boxes are positioned below the questions "Are you adding records?" and "Are you adding features?".  The boxes are simple, filled shapes, and their purpose is to visually represent the different types of concatenation operations.  The boxes are not complex and do not contain any text.  The slide is entirely text-based, with the text organized in a way that encourages the user to consider the context of the operation.


# Slide 58

**Description of the Slide:**

This slide provides the documentation for the `pandas.concat()` function.  It lists the parameters of the function, including `objs`, `axis`, and `ignore_index`.  The `objs` parameter is a sequence of Series or DataFrames to be concatenated.  The `axis` parameter specifies the axis along which to concatenate (0 for rows, 1 for columns).  The `ignore_index` parameter is a boolean value; if `True`, the concatenation results in a new index for the combined data.  The slide is entirely text-based, containing no figures or tables.  The purpose is to provide a reference for using the `pandas.concat()` function in data analysis.


**What you see:**

The slide displays the documentation for the `pandas.concat()` function, including its parameters and their descriptions.  The text is formatted in a way that clearly outlines the function's arguments and their intended use.  There are no images or tables on the slide.  The text is presented in a way that is typical of a slide containing function documentation.


# Slide 59

**Adding Records**

Once again, we'll be using simple examples

```
display(df_A)
display(df_B)
```

Two tables are shown, labeled `df_A` and `df_B`.  Each table has three columns: "Name", "HW", and "Quiz".  Each row represents a student's scores.  The tables are side-by-side, and the data in each table is likely a set of student scores for homework, quizzes, and labs.  The purpose of these tables is to provide example data for demonstrating how to combine dataframes.

```
Combine directly
pd.concat([df_A, df_B])
```

This line of code shows how to combine `df_A` and `df_B` using the `pd.concat()` function.  The result of this concatenation is a new DataFrame that contains all the rows from both `df_A` and `df_B`.  The order of the rows will be the same as the order in the original DataFrames.

```
Be mindful of the duplicated index values, we can fix them
pd.concat([df_A, df_B], ignore_index=True)
```

This line of code shows how to combine `df_A` and `df_B` using the `pd.concat()` function, but with the `ignore_index=True` argument.  This argument will reset the index of the combined DataFrame to sequential integers, avoiding issues with duplicate indices.  The result of this concatenation is a new DataFrame that contains all the rows from both `df_A` and `df_B`, but with a new index.


---

**Description of the Slide:**

This slide demonstrates how to combine two DataFrames (`df_A` and `df_B`) using the `pd.concat()` function.  It shows two ways to combine the DataFrames: one that preserves the original indices and one that resets the indices.  The slide uses example data in tables to illustrate the concept of concatenating DataFrames.  The code snippets are essential for demonstrating the concatenation process. The tables are essential for showing the input data and the expected output of the concatenation. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on combining data from different sources.




# Slide 60

**Adding Records**

You may not want to lose the source of your data.  Or the name of the source might be data in itself.

```
df_A['Group'] = 'A'
df_B['Group'] = 'B'
df_All = pd.concat([df_A, df_B])
```

Two tables are shown, `df_A` and `df_B`.  Each table has columns for "Name", "HW", "Quiz", and "Lab".  Each row represents a student's scores.  `df_A` contains student scores labeled as group 'A', and `df_B` contains student scores labeled as group 'B'.  The tables are side-by-side, and the data in each table is likely a set of student scores for homework, quizzes, and labs.  The purpose of these tables is to provide example data for demonstrating how to combine DataFrames.

```
We can filter out one subset by using loc function
df_All.loc[df_All.Group == 'A', :]
```

This line of code filters the `df_All` DataFrame to include only the rows where the 'Group' column is equal to 'A'.  The result is a new DataFrame containing only the rows from group 'A'.

```
df_All.loc[df_All.Group == 'B', :]
```

This line of code filters the `df_All` DataFrame to include only the rows where the 'Group' column is equal to 'B'.  The result is a new DataFrame containing only the rows from group 'B'.


---

**Description of the Slide:**

This slide demonstrates how to combine two DataFrames (`df_A` and `df_B`) into a single DataFrame (`df_All`) using `pd.concat()`.  It also shows how to filter the combined DataFrame to select only rows belonging to a specific group ('A' or 'B') using the `.loc` method.  The slide uses example data in tables to illustrate the concepts of concatenation and filtering.  The code snippets are essential for demonstrating the data manipulation process. The tables are essential for showing the input data and the expected output of the concatenation and filtering operations. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on combining and filtering data.  The tables are side-by-side, and the data in each table is likely a set of student scores for homework, quizzes, and labs.  The purpose of these tables is to provide example data for demonstrating how to combine dataframes.




# Slide 61

**Adding Features**

Sometimes we might have data divided in different files.
Perhaps a main gradebook and a list of scores passed back by the grader.

Use `axis=1` for side-by-side.

```
df_grades = pd.concat([df_no_exam, df_exam_1], axis=1)
```

What happens with a randomized, shorter exam 2 list?

```
pd.concat([df_grades, df_exam_2], axis=1)
```

This is not what we want...

```
df_no_exam
```

```
ST_Name	HW	Quiz	Lab
0	John	90	70	100
1	Olivia	95	93	97
2	Joshua	79	76	95
```

```
df_exam_1
```

```
ST_Name	Exam1
0	John	85
1	Olivia	98
2	Joshua	63
```

```
df_exam_2
```

```
ST_Name	Exam2
0	John	85
1	Olivia	92
2	Joshua	63
```

```
df_grades
```

```
ST_Name	HW	Quiz	Lab	ST_Name	Exam1
0	John	90	70	100	John	85
1	Olivia	95	93	97	Olivia	98
2	Joshua	79	76	95	Joshua	63
```


---

**Description of the Slide:**

This slide demonstrates the concept of adding features to a dataset, specifically combining data from different files.  It shows how `pd.concat()` can be used to combine DataFrames, but highlights that simply concatenating DataFrames side-by-side might not be the desired outcome.  The example uses three DataFrames (`df_no_exam`, `df_exam_1`, and `df_exam_2`) representing different parts of a student's grade data.  The slide shows the individual DataFrames and then the result of concatenating `df_no_exam` and `df_exam_1` along the columns (`axis=1`).  The example illustrates that simply concatenating the DataFrames side-by-side doesn't always produce the desired result, as the data might not be aligned correctly.  The slide is part of a lecture on data manipulation and analysis, specifically focusing on combining data from different sources.  The code snippets are essential for demonstrating the data manipulation process. The tables are crucial for showing the input data and the expected output of the concatenation operation.  The tables are formatted to show the columns and rows of the DataFrames.




# Slide 62

**Description of the Slide:**

This slide discusses how to combine DataFrames with different structures, specifically when adding new features (columns) to an existing DataFrame.  The example uses three DataFrames (`df_grades`, `df_exam_1`, and `df_exam_2`) representing different parts of student grade data.  The slide shows the structure of each DataFrame, highlighting that they have the same columns but different indexes.  The key point is that concatenating these DataFrames along the columns (`axis=1`) directly will result in misaligned data, as shown in the example output of `df_grades`.  The slide emphasizes that careful consideration of the structure and indexes of the DataFrames is crucial when combining them to avoid errors.  The code snippets demonstrate the concatenation process, and the tables illustrate the structure of the DataFrames and the potential issues with direct concatenation.  The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on combining data from different sources and the importance of data alignment.


**What you see:**

The slide presents several tables and code snippets.  The tables (`df_grades`, `df_exam_1`, `df_exam_2`) show student names, homework scores, quiz scores, lab scores, and exam scores.  The tables are formatted to show the columns and rows of the DataFrames.  The code snippets demonstrate how to concatenate the DataFrames using `pd.concat()`, highlighting the potential for misalignment when `axis=1` is used without careful consideration of the indexes.  The slide's overall purpose is to illustrate the importance of data alignment when combining DataFrames, particularly when adding new features.  The gray arrows in the diagram visually represent the concatenation process.  The diagram shows how `df_grades` and `df_exam_2` are combined, but the result is not correctly aligned.  This is a key point of the slide, illustrating the potential for errors when combining DataFrames with different indexes.


# Slide 63

**Cleaning up**

We have our combined data!
... but it's messy...

Apply duplicated to the columns
```
mask = ~df_grades.columns.duplicated()
mask
array([ True,  True,  True,  True, False,  True, False,  True])
```

Use it to filter duplicate columns
```
df_grades = df_grades.loc[:, mask]
```

```
ST_Name  HW  Quiz  Lab  Exam1  Exam2
0      John  90    70  100     85    NaN
1    Olivia  95    93   97     98   92.0
2    Joshua  79    76   95     63   79.0
```

---

**Description of the Slide:**

This slide addresses the issue of duplicate columns in a DataFrame (`df_grades`).  The code `mask = ~df_grades.columns.duplicated()` creates a boolean mask that identifies columns that are not duplicates.  The `~` operator inverts the boolean values, so `True` indicates a unique column.  The code `df_grades = df_grades.loc[:, mask]` filters the DataFrame to keep only the unique columns, effectively removing the duplicate "ST_Name" columns.  The resulting DataFrame is shown in a table, displaying the "ST_Name", "HW", "Quiz", "Lab", "Exam1", and "Exam2" columns.  The purpose of this slide is to demonstrate how to remove duplicate columns from a DataFrame using boolean indexing.  The code snippets are essential for demonstrating the data cleaning process. The table is crucial for showing the result of the column filtering operation. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on cleaning and preparing data for further analysis.




# Slide 64

**Description of the Slide:**

This slide is a title slide, introducing the topic of merging data.  It's a plain, light gray background with the single, bold, centered title "Merging Data" in a dark gray font.  There are no figures, tables, or code snippets on the slide.  The slide serves as a heading for the next section of the lecture, which will likely cover techniques for combining data from different sources based on shared attributes.


**What you see:**

A plain, light gray background with the title "Merging Data" centered in a dark gray, sans-serif font.  The title is large enough to be prominent but not overly large.  The slide is entirely text-based and serves as a heading for the next section of the lecture.


# Slide 65

**Pandas Merge**

```
pd.merge(left, right, how='inner', on=None, left_on=None, right_on=None,
         left_index=False, right_index=False, sort=True,
         suffixes=('_x', '_y'), copy=True, indicator=False,
         validate=None)
```

*   `left` / `right`: The data frame, their position side by side - affects the `how`.
*   `how`: one of 'left', 'right', 'inner', 'outer'
*   `on`: label or list identifying columns / index to be joined upon - often referred to as a key


---

**Description of the Slide:**

This slide presents the documentation for the `pd.merge()` function in Pandas.  It shows the function's parameters, including `left`, `right`, `how`, `on`, `left_on`, `right_on`, `left_index`, `right_index`, `sort`, `suffixes`, `copy`, `indicator`, and `validate`.  The descriptions of the parameters explain their roles in merging two DataFrames.  The slide is entirely text-based, containing no figures or tables.  The purpose is to provide a reference for using the `pd.merge()` function in data analysis.  The formatting is typical of a slide presenting function documentation.




# Slide 66

**The types of Joins**

A diagram is presented, illustrating the different types of joins (merges) in data manipulation.  The diagram shows four distinct join types: Inner, Outer, Left, and Right.  Each join type is represented by a set of overlapping boxes, with different colors highlighting the matching and non-matching data.

*   **Inner Join (Intersection):**  The overlapping area (in gray) represents the common elements between Data A and Data B.  Only rows with matching keys are included in the result.

*   **Outer Join (Union):**  All rows from both Data A and Data B are included in the result.  Matching rows are shown in the overlapping area (in gray), while rows without matches in the other dataset are shown in separate boxes (in blue and red).

*   **Left Join (Left-sided Union):**  All rows from Data A are included in the result.  Matching rows are shown in the overlapping area (in gray), and rows from Data B without a match in Data A are not shown.

*   **Right Join (Right-sided Union):**  All rows from Data B are included in the result.  Matching rows are shown in the overlapping area (in gray), and rows from Data A without a match in Data B are not shown.

The diagram is centrally located on the slide and is oriented horizontally.  The purpose of the diagram is to visually represent the different types of joins and how they affect the resulting combined dataset.  The colors (gray, blue, red) help distinguish the different parts of the data in each join type.


---

**Description of the Slide:**

The slide visually depicts the four main types of joins (merges) used in data manipulation, specifically in the context of combining data from two different datasets (Data A and Data B).  The diagram uses overlapping boxes to illustrate how rows from each dataset are combined based on matching keys.  The different colors (gray, blue, red) highlight the different parts of the data in each join type.  The slide is part of a lecture on data analysis techniques, specifically focusing on merging and combining data.  The diagram is the central element of the slide, and its purpose is to visually represent the different join types and how they affect the resulting combined dataset.  The context suggests that the slide is intended to help the audience understand the different ways to combine data based on shared attributes.


# Slide 67

**Inner Joins - the default merge**

The default merge is an inner merge

```
df_join = pd.merge(df_student, df_gpa)
```

```
df_student
```

```
SID	Name	Email
0	2343	Jeanne Garza	garza2@purdue.edu
1	8990	Amy Benson	bensona@purdue.edu
2	17652	Howard Nichols	nichols45@purdue.edu
3	7900	Gregg Brock	brockg@purdue.edu
4	8762	Noah Murphy	murphy231@purdue.edu
5	23432	Daniel Jennings	jennings8@purdue.edu
6	5483	Ricky Flemings	flemingsr@purdue.edu
```

```
df_gpa
```

```
SID	GPA
0	2343	3.8
1	45343	2.6
2	23432	4.0
3	8762	3.1
4	10022	3.5
```

```
df_join
```

```
SID	Name	Email	GPA
0	2343	Jeanne Garza	garza2@purdue.edu	3.8
1	8762	Noah Murphy	murphy231@purdue.edu	3.1
2	23432	Daniel Jennings	jennings8@purdue.edu	4.0
```

If no columns to merge on are specified, merge identifies columns with matching names.

The above example is the same as

```
df_join = pd.merge(df_student, df_gpa, how='inner', on='SID')
```


---

**Description of the Slide:**

This slide demonstrates an inner join (merge) operation in Pandas.  It shows how to combine two DataFrames, `df_student` and `df_gpa`, based on a shared column, 'SID'.  The `pd.merge()` function is used, and by default, it performs an inner join.  The `df_student` DataFrame contains student information (SID, Name, Email), and `df_gpa` contains student GPAs (SID, GPA).  The resulting `df_join` DataFrame contains only the rows where the 'SID' values match in both input DataFrames.  The slide clearly illustrates the inner join operation, showing the input DataFrames and the output DataFrame.  The code snippets are essential for demonstrating the merge process. The tables are crucial for showing the input and output data. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on merging data from different sources.




# Slide 67

**The Left Join**

The left join extends the left DataFrame to include features from the right DataFrame.

```
df_join = pd.merge(df_student, df_gpa, how='left')
```

Three tables are displayed: `df_student`, `df_gpa`, and `df_join`.

*   **`df_student`**: This table has columns for "SID", "Name", and "Email".  Each row represents a student's ID, name, and email address.

*   **`df_gpa`**: This table has columns for "SID" and "GPA".  Each row represents a student's ID and their corresponding GPA.

*   **`df_join`**: This table is the result of the left join.  It contains all the columns from `df_student` and the "GPA" column from `df_gpa`.  Rows from `df_student` that have a matching "SID" in `df_gpa` will have the corresponding GPA.  Rows from `df_student` that do *not* have a matching "SID" in `df_gpa` will have a NaN value in the "GPA" column.  The order of the rows in `df_join` is the same as in `df_student`.

A directed arrow is shown connecting `df_student` to `df_gpa` and then to `df_join`.  This arrow visually represents the merging process.


---

**Description of the Slide:**

This slide demonstrates a left join operation in Pandas, using the `pd.merge()` function.  It shows how to combine two DataFrames (`df_student` and `df_gpa`) based on a shared key ("SID").  The result (`df_join`) includes all rows from the left DataFrame (`df_student`) and the matching columns from the right DataFrame (`df_gpa`).  Rows in `df_student` without a match in `df_gpa` will have NaN values for the columns from `df_gpa`.  The slide uses a visual representation (the arrow) to illustrate the direction of the join.  The tables are crucial for demonstrating the input data and the result of the left join. The code snippet is essential for demonstrating the data manipulation process. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on merging and combining data from different sources.




# Slide 69

**Description of the Slide:**

This slide demonstrates a right join operation in Pandas, using the `pd.merge()` function.  It shows how to combine two DataFrames (`df_student` and `df_gpa`) based on a shared key ("SID").  The result (`df_join`) includes all rows from the right DataFrame (`df_gpa`) and the matching columns from the left DataFrame (`df_student`).  Rows in `df_gpa` without a match in `df_student` will have NaN values for the columns from `df_student`.  The slide uses three tables (`df_student`, `df_gpa`, and `df_join`) to illustrate the input data and the result of the right join.  The tables show the "SID", "Name", "Email", and "GPA" columns.  The purpose of this slide is to demonstrate the right join operation and its effect on the combined DataFrame.  The code snippet is essential for demonstrating the data manipulation process. The tables are crucial for showing the input data and the result of the right join operation. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on merging and combining data from different sources.


**What you see:**

The slide displays three tables (`df_student`, `df_gpa`, and `df_join`).  Each table has columns for "SID", "Name", "Email", and "GPA".  The `df_student` table contains student information, `df_gpa` contains student GPA information, and `df_join` is the result of the right join.  The `df_join` table includes all rows from `df_gpa`, and if a match is found in `df_student`, the corresponding data is included.  If no match is found, the columns from `df_student` will have NaN values.  A directed arrow is shown connecting `df_student` to `df_gpa` and then to `df_join`, visually representing the merging process.  The slide's overall purpose is to illustrate the right join operation and its effect on the combined DataFrame.




# Slide 70

**Outer Joins retain all the data**

An outer join keeps all parts of your data, and fills in the rest with NaN.

```
df_join = pd.merge(df_student, df_gpa, how='outer')
```

Three tables are displayed: `df_student`, `df_gpa`, and `df_join`.

*   **`df_student`**: This table has columns for "SID", "Name", and "Email".  Each row represents a student's ID, name, and email address.

*   **`df_gpa`**: This table has columns for "SID" and "GPA".  Each row represents a student's ID and their corresponding GPA.

*   **`df_join`**: This table is the result of the outer join.  It contains all the columns from both `df_student` and `df_gpa`.  Rows that have a matching "SID" in both DataFrames will have the corresponding GPA.  Rows from either `df_student` or `df_gpa` that do *not* have a matching "SID" in the other DataFrame will have NaN values for the columns from the other DataFrame.  The order of the rows in `df_join` is determined by the order of the rows in `df_student` followed by the rows in `df_gpa` that don't have a match.

A directed arrow is shown connecting `df_student` to `df_gpa` and then to `df_join`.  This arrow visually represents the merging process.  A second arrow is shown connecting `df_gpa` to `df_join`.  This arrow visually represents the merging process.


---

**Description of the Slide:**

This slide demonstrates an outer join (merge) operation in Pandas, using the `pd.merge()` function.  It shows how to combine two DataFrames (`df_student` and `df_gpa`) based on a shared key ("SID").  The result (`df_join`) includes all rows from both input DataFrames.  Rows without a match in the other DataFrame will have NaN values for the columns from the other DataFrame.  The slide uses three tables (`df_student`, `df_gpa`, and `df_join`) to illustrate the input data and the result of the outer join.  The tables show the "SID", "Name", "Email", and "GPA" columns.  The purpose of this slide is to demonstrate the outer join operation and its effect on the combined DataFrame.  The code snippet is essential for demonstrating the data manipulation process. The tables are crucial for showing the input data and the result of the outer join operation. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on merging and combining data from different sources.  The arrows visually represent the merging process.


# Slide 71

**Mismatched Identifiers**

Column names don't always match.  This is especially true when combining data sets from different sources.  In these cases, you have to specify which columns to join on.

```
df_all = pd.merge(df_student, df_gpa, left_on='Student_ID', right_on='SID')
df_all.drop('SID', axis=1)
```

Three tables are shown: `df_student`, `df_gpa`, and `df_all`.

*   **`df_student`**: This table has columns for "Student ID", "Name", and "Email".  Each row represents a student's ID, name, and email address.

*   **`df_gpa`**: This table has columns for "SID" and "GPA".  Each row represents a student's ID and their corresponding GPA.

*   **`df_all`**: This table is the result of merging `df_student` and `df_gpa` based on the 'Student_ID' column from `df_student` and the 'SID' column from `df_gpa`.  The 'SID' column is dropped from the merged DataFrame.  The resulting table shows the combined data, with matching 'Student ID' values appearing in both tables.


---

**Description of the Slide:**

This slide demonstrates the importance of specifying join columns when merging DataFrames with potentially mismatched column names.  The example uses two DataFrames, `df_student` and `df_gpa`, with different column names for the student ID ("Student_ID" vs. "SID").  The code `pd.merge(df_student, df_gpa, left_on='Student_ID', right_on='SID')` explicitly specifies the columns to join on, ensuring that the correct rows are matched.  The `df_all.drop('SID', axis=1)` line removes the 'SID' column from the merged DataFrame, as it's a duplicate column.  The slide uses tables to illustrate the input DataFrames and the output DataFrame, highlighting the matching process and the removal of the duplicate column.  The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on merging data from different sources.  The tables are crucial for demonstrating the input and output data, and the code snippets are essential for demonstrating the merging process.  The slide emphasizes the need to be careful when merging data from different sources, especially when column names might not match.


# Slide 72

**Description of the Slide:**

This slide demonstrates how to merge DataFrames when the index of one DataFrame needs to be used as a join key.  The code `df_all = pd.merge(df_student, df_gpa, left_index=True, right_on='SID')` merges `df_student` and `df_gpa` using the index of `df_student` (implicitly) and the 'SID' column of `df_gpa`.  The `left_index=True` parameter tells Pandas to use the index of the left DataFrame (`df_student`) as the join key.  The `right_on='SID'` parameter tells Pandas to use the 'SID' column of the right DataFrame (`df_gpa`) as the join key.  The slide includes three tables: `df_student`, `df_gpa`, and `df_all`.  `df_student` has columns "SID", "Name", and "Email"; `df_gpa` has columns "SID" and "GPA"; and `df_all` shows the merged result, containing columns from both DataFrames.  The purpose of the slide is to illustrate how to merge DataFrames when the index of one DataFrame is used as the join key.  The tables are crucial for demonstrating the input and output data, and the code snippet is essential for demonstrating the merging process. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on merging data from different sources.


**What you see:**

The slide displays three tables (`df_student`, `df_gpa`, and `df_all`).  Each table has columns for "SID", "Name", "Email", and "GPA".  The `df_student` table contains student information, `df_gpa` contains student GPA information, and `df_all` is the result of the merge.  The `df_all` table shows the combined data, with the index from `df_student` and the 'GPA' from `df_gpa` where a match is found.  There's a visual representation (arrows) connecting `df_student` and `df_gpa` to `df_all`, illustrating the merging process.  The slide's overall purpose is to illustrate merging DataFrames using the index of one DataFrame as the join key.




# Slide 73

**Merging on Index**

In some cases, your data frame's index might match a column in another data frame.  For this, we'll need to specify what to use in each.

```
df_all = pd.merge(df_student, df_gpa, left_index=True, right_index=True)
```

Three tables are shown: `df_student`, `df_gpa`, and `df_all`.

*   **`df_student`**: This table has columns for "SID", "Name", and "Email".  Each row represents a student's ID, name, and email address.  The index of this DataFrame is likely a sequential integer, starting from 0.

*   **`df_gpa`**: This table has columns for "SID" and "GPA".  Each row represents a student's ID and their corresponding GPA.  The index of this DataFrame is likely a sequential integer, starting from 0.

*   **`df_all`**: This table is the result of merging `df_student` and `df_gpa` using their indices as the join key.  The resulting table shows the combined data, with matching index values appearing in both tables.  The combined table includes columns from both input DataFrames.


---

**Description of the Slide:**

This slide demonstrates merging DataFrames based on their indices.  The code `df_all = pd.merge(df_student, df_gpa, left_index=True, right_index=True)` merges `df_student` and `df_gpa` using their indices as the join key.  The `left_index=True` and `right_index=True` parameters tell Pandas to use the index of each DataFrame as the join key.  The resulting `df_all` DataFrame contains the combined data, with rows corresponding to matching indices from both input DataFrames.  The slide uses tables to illustrate the input DataFrames and the output DataFrame, highlighting the matching process based on indices.  The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on merging data from different sources.  The tables are crucial for demonstrating the input and output data, and the code snippet is essential for demonstrating the merging process.  The slide emphasizes the use of indices as join keys in merging operations.




# Slide 74

**Description of the Slide:**

This slide discusses the potential issue of merging DataFrames with the same column names but different meanings.  It shows two DataFrames, `df_cs101` and `df_cs200`, both containing a "SID" and "Name" column, but the "Score" column in each DataFrame represents different things.  The slide highlights that simply merging these DataFrames using `pd.merge(df_cs101, df_cs200)` would likely produce incorrect results because Pandas would incorrectly join on the common columns.  The slide emphasizes the importance of specifying the correct columns to merge on when the column names are the same but the data they represent is different.  The slide shows examples of how to correctly merge the DataFrames using the `on` parameter to specify the 'SID' column as the join key.  The tables are crucial for demonstrating the input data and the potential issues with incorrect merging. The code snippets are essential for demonstrating the correct merging process. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on merging data from different sources and the importance of data alignment.


**What you see:**

The slide presents a title "Same name, different features".  Below the title, there's text explaining that DataFrames might have the same column names but different meanings.  The slide shows two tables, `df_cs101` and `df_cs200`, each with columns "SID", "Name", and "Score".  The data in the "Score" column is different in each table.  There are arrows connecting the tables to a third table, which shows the result of a merge operation.  The arrows visually represent the merging process.  The slide also includes code examples demonstrating how to correctly merge the DataFrames using the `on` parameter to specify the 'SID' column as the join key.  The code examples show how to use suffixes to distinguish the columns after the merge.  The overall purpose of the slide is to illustrate the importance of specifying the correct columns to merge on when merging DataFrames with potentially mismatched column meanings.




# Slide 75

**Relationships**

One to One
Many to One
One to Many

A diagram is presented, illustrating different relationships between dataframes.  The diagram uses colored rectangles (black, light-blue, and maroon) to represent DataFrames (df_A and df_B).  The rectangles are vertically stacked, with lines connecting them.  The lines represent the relationships between the DataFrames.

*   **One-to-One:** A single rectangle (black) is connected to a single other rectangle (dark gray) by a single line.  This represents a one-to-one relationship, where each row in one DataFrame corresponds to exactly one row in the other.

*   **Many-to-One:** Multiple rectangles (light-blue) are connected to a single rectangle (dark gray) by multiple lines.  This represents a many-to-one relationship, where multiple rows in one DataFrame correspond to a single row in the other.

*   **One-to-Many:** A single rectangle (maroon) is connected to multiple rectangles (dark gray) by multiple lines.  This represents a one-to-many relationship, where a single row in one DataFrame corresponds to multiple rows in the other.

The diagram is positioned centrally on the slide and is oriented vertically.  The purpose of the diagram is to visually represent the different types of relationships between DataFrames, which are important considerations when merging or combining data.  The colors help distinguish the different types of relationships.


---

**Description of the Slide:**

This slide visually depicts the different types of relationships that can exist between two or more DataFrames.  The diagram uses rectangles and lines to represent the relationships, making it easy to understand the one-to-one, many-to-one, and one-to-many relationships.  The slide is part of a lecture on data manipulation and analysis, specifically focusing on merging and combining data from different sources.  The diagram is the central element of the slide, and its purpose is to visually represent the different relationship types and how they affect the resulting combined dataset.  The context suggests that the slide is intended to help the audience understand the different ways data can be related and how these relationships affect the merging process.




# Slide 76

**One to One**

Most things we have shown so far, have been one to one.
It is a fair straightforward relationship.
Define the keys/identifiers in each data frame, and pair them.
`df_join = pd.merge(df_1, df_2)`

Since there are no duplicate SID values, the result is a simple pairing.

|       | SID | Grade_1 | Grade_2 |
|-------|-----|----------|----------|
|   0   | 122 | 90       | 100      |
|   1   | 345 | 78       | 88       |
|   2   | 678 | 84       | 88       |
|   3   | 290 | 95       | 82       |


---

**Description of the Slide:**

This slide demonstrates a one-to-one relationship between two DataFrames (`df_1` and `df_2`) and how to merge them using Pandas.  The example shows a simple pairing of data based on a common key, "SID".  The slide presents a table showing the merged DataFrame (`df_join`) with columns "SID", "Grade_1", and "Grade_2".  Each row in the table represents a pairing of data from `df_1` and `df_2` based on matching "SID" values.  The purpose of the slide is to illustrate a straightforward one-to-one merge operation, where each row in one DataFrame corresponds to exactly one row in the other.  The table is crucial for demonstrating the result of the merge. The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on merging data from different sources.




# Slide 77

**One-to-Many Relationships**

Consider the relationship of instructors to courses. One instructor teaches many courses. We have a DataFrame for instructors (`df_1`) and a separate one for courses (`df_2`). A course has a key, `InstructorID`, which allows us to reference the instructor data in `df_1`.  When we merge, pandas combines the instructor with every record sharing the same key.

```
pd.merge(df_1, df_2)
```

The following tables are shown:

**df_1 (Instructors):**

| InstructorID | Name |
|---|---|
| 111 | Jack |
| 589 | Cindy |
| 764 | Gary |


**df_2 (Courses):**

| InstructorID | Course |
|---|---|
| 111 | Math200 |
| 111 | CS200 |
| 111 | CS255 |
| 589 | CS101 |
| 764 | ChE101 |


**Result of pd.merge(df_1, df_2):**

| InstructorID | Name | Course |
|---|---|---|
| 111 | Jack | Math200 |
| 111 | Jack | CS200 |
| 111 | Jack | CS255 |
| 589 | Cindy | CS101 |
| 764 | Gary | ChE101 |


---

**Description of the Slide:**

This slide demonstrates a one-to-many relationship between instructors and courses.  It shows how a merge operation, using `pd.merge(df_1, df_2)`, combines the instructor information with the course information based on the common `InstructorID` column.  The example clearly illustrates that one instructor can teach multiple courses, resulting in multiple rows in the merged DataFrame for each instructor.  The tables (`df_1`, `df_2`, and the result) are crucial for demonstrating the one-to-many relationship and the outcome of the merge operation.  The context suggests that this slide is part of a larger lecture on data manipulation and analysis, specifically focusing on merging data from different sources with various relationships.




# Slide 78

**Description of the Slide:**

This slide discusses "Many-to-Many" relationships between DataFrames.  It shows two tables (`df_1` and `df_2`) and a diagram illustrating the relationship.  The tables represent a many-to-many relationship, where multiple rows in one DataFrame can correspond to multiple rows in another.  The example likely involves student IDs (`SID`) and courses, where a student can take multiple courses and a course can have multiple students.  The slide highlights that the direct result of merging such DataFrames is a Cartesian product, which might not be the desired outcome.  The text emphasizes that modeling a many-to-many relationship often requires a different approach, such as creating intermediate tables or using a different database design.  The diagram visually represents the many-to-many relationship between the two DataFrames.  The purpose of the slide is to illustrate the concept of a many-to-many relationship and its implications for data merging.


**What you see:**

The slide has a title "Many to Many combinations".  Below the title, there's text explaining that data frames can be in a many-to-many relationship.  A bullet point states that there are repeated keys in both data frames.  The text also states that the result is a Cartesian product, and that each element in `df_1` is paired with each element in `df_2` by key.  The text further notes that this might not be the desired outcome and asks how to know if the elements should be paired.  The slide also includes two tables (`df_1` and `df_2`) and a diagram.  `df_1` has a column "SID" and "course", and `df_2` has a column "SID" and "Award".  The diagram shows arrows connecting the tables, visually representing the many-to-many relationship.  The diagram also shows a table with the combined data, which would be a Cartesian product of the two tables.  The slide is part of a lecture on data manipulation and analysis, specifically focusing on merging data from different sources with various relationships.




# Slide 79

**Data munging outside of python/pandas**

---

**Description of the Slide:**

This slide is a title slide, likely introducing a section of the Intro to Data Science lecture that will cover data manipulation techniques that extend beyond the capabilities of Python's Pandas library.  The title, "Data munging outside of python/pandas," clearly indicates the topic.  There are no figures or tables on this slide.  The slide is entirely text-based and serves as a heading for the next section of the lecture.


**What you see:**

A plain, light gray background with the title "Data munging outside of python/pandas" centered in a dark gray, sans-serif font.  The title is large enough to be prominent but not overly large.  The slide is entirely text-based and serves as a heading for the next section of the lecture.


# Slide 80

**Command Line processing and Shell scripting**

Some basic data science tasks are easily completed with command line functions/tools

Note: that there are different types of shell (bash, csh, ksh, zsh, ...)
bash shell is most common (default shell major linux distributions, and previously on OSX)
Shell scripts allow us to program commands in chains and have the system execute them as a scripted event


---

**Description of the Slide:**

This slide introduces the topic of command-line processing and shell scripting in the context of data science.  It states that some basic data science tasks can be efficiently handled using command-line tools and shell scripts.  The slide also notes the existence of different shell types (bash, csh, ksh, zsh) and highlights the prevalence of the bash shell.  The text emphasizes that shell scripts allow users to chain commands together for automated execution.  The slide is entirely text-based, with no figures or tables.  The purpose is to introduce the concept of using command-line tools and scripting for data science tasks.


# Slide 81

**Useful commands**

```
wc -l file1.dat          # counts number of lines in a file
wc -w file1.dat          # counts number of words in a file
head -100 file1.dat      # output first 100 lines of file
tail file1.dat           # output last ten lines of file
sort file1.dat           # sort the contents of a file (default is alphabetical)
cat file1.dat file2.dat >newfile.dat     # concatenates input into new file
sort file1.dat | uniq       # sort the file and remove any duplicate lines  
sort file1.dat | uniq -c    # remove duplicates, output counts for each occur. 
```

Piping redirects output of previous command to input of next command


---

**Description of the Slide:**

This slide provides a list of useful command-line commands, likely for working with text files.  The commands are presented in a list format, with each command followed by a brief description (in comments).  The commands include:

*   `wc -l file1.dat`: Counts the number of lines in the file `file1.dat`.
*   `wc -w file1.dat`: Counts the number of words in the file `file1.dat`.
*   `head -100 file1.dat`: Outputs the first 100 lines of the file `file1.dat`.
*   `tail file1.dat`: Outputs the last ten lines of the file `file1.dat`.
*   `sort file1.dat`: Sorts the contents of the file `file1.dat` (alphabetically by default).
*   `cat file1.dat file2.dat >newfile.dat`: Concatenates the contents of `file1.dat` and `file2.dat` into a new file named `newfile.dat`.
*   `sort file1.dat | uniq`: Sorts `file1.dat` and then removes any duplicate lines.
*   `sort file1.dat | uniq -c`: Sorts `file1.dat`, removes duplicate lines, and outputs the count of each unique line.

The slide also explains the concept of "piping," where the output of one command is used as the input for another.  The purpose of this slide is to provide a reference for common command-line utilities that can be used for data manipulation tasks.  The slide is entirely text-based, with no figures or tables.




# Slide 82

**Unix Tools**

Unix tools excel at manipulating strings as data, often using regular expressions

`grep`: filters its input against a pattern

`sed`: applies transformation rules to each line

`awk`: manipulates tabular text files (e.g., CSV)


---

**Description of the Slide:**

This slide introduces Unix tools commonly used in data science for string manipulation, often employing regular expressions.  The slide lists three key tools: `grep`, `sed`, and `awk`.  `grep` is used for filtering input based on patterns, `sed` for applying transformations to each line, and `awk` for manipulating tabular text files (like CSV).  The slide is entirely text-based, with no figures or tables.  The purpose is to introduce these command-line tools as alternatives to or complements for Python-based data manipulation.


# Slide 83

**`grep`**

`grep` searches plain text files for lines that match a pattern.

The command below prints each line of the file which contains a match for the pattern:

```
grep pattern file
```

If you use `grep -v` instead, it prints each line of the file which *does not* contain a match for the pattern.

By default, `grep` uses regular expressions, e.g., the following finds all processes by user `bgstm`:

```
ps aux | grep "^bgstm"
```


---

**Description of the Slide:**

This slide describes the `grep` command-line utility, commonly used in Unix-like systems for searching text files.  The slide explains that `grep` searches for lines matching a specified pattern within a file.  It also shows how to use the `-v` option to find lines that *do not* match the pattern.  The example demonstrates using `grep` with a regular expression (`"^bgstm"`) to find processes owned by the user `bgstm`.  The slide is entirely text-based, with no figures or tables.  The purpose is to introduce the `grep` command and its usage in data science tasks involving text file processing.  The example shows how `grep` can be used in conjunction with other commands (like `ps aux`) to filter and extract specific information.


# Slide 84

**`grep` Examples**

```
grep "this" *.txt
grep ^root /etc/passwd
grep "http.*html" index.html
grep -i "linux" index.html  
# case insensitive
grep -A <N> "string" FILENAME  #print N lines after (-A) or before (-B) match
grep -c "pattern" filename     #count number of lines that match
http://www.grymoire.com/Unix/Grep.html
```

---

**Description of the Slide:**

This slide provides examples of using the `grep` command-line utility.  It shows various `grep` commands, each demonstrating different search patterns and options.  The examples include:

*   `grep "this" *.txt`: Searching for the string "this" in all files ending with ".txt" in the current directory.
*   `grep ^root /etc/passwd`: Searching for lines starting with "root" in the `/etc/passwd` file.  The `^` is a regular expression anchor for the beginning of a line.
*   `grep "http.*html" index.html`: Searching for lines containing "http" followed by any characters (`.`) and ending with ".html" in the `index.html` file.  The `*` is a regular expression quantifier, meaning "zero or more occurrences of the preceding character".
*   `grep -i "linux" index.html`: Searching for the string "linux" in `index.html`, ignoring case (the `-i` option).
*   `grep -A <N> "string" FILENAME`: Printing `N` lines *after* the lines containing the string "string" in the file `FILENAME`.  The `<N>` is a placeholder for a numerical value.
*   `grep -c "pattern" filename`: Counting the number of lines in `filename` that match the pattern.

The slide also includes a URL (`http://www.grymoire.com/Unix/Grep.html`) which is a helpful resource for learning more about the `grep` command.  The slide is entirely text-based, with no figures or tables.  The purpose is to provide examples of how to use `grep` for searching and filtering text files.




# Slide 85

**`sed`**

`sed` is a stream editor that can perform basic transformations on an input stream.
It reads line-by-line, conditionally applying a sequence of operations to each line and outputting the result.
By default, `sed` uses regular expression syntax.
Most `sed` programs consist a single `sed` command: substitute.  E.g., to substitute commas with tabs, use:

```
$ sed 's/,/\t/g' <in >out
```

---

**Description of the Slide:**

This slide introduces the `sed` command-line utility, describing it as a stream editor for performing transformations on text.  It explains that `sed` operates line by line, applying operations conditionally and outputting the result.  The default syntax uses regular expressions.  The example shows a simple substitution command: `sed 's/,/\t/g' <in >out`.  This command substitutes all commas (`,`) with tabs (`\t`) in the input file (`<in`) and redirects the output to a new file (`>out`).  The slide is entirely text-based, with no figures or tables.  The purpose is to introduce the `sed` command and its basic usage.


# Slide 86

**`sed` Examples**

```
sed 's/test/another test/' filename
sed 's/test//g' filename        # g=global (every occurrence on the line)
sed '/regexp/d' filename        # d=delete lines
sed '/^$/d' filename
sed -n '/Jones/p' filename      # p=print
sed G filename                  # double space a file
http://www.grymoire.com/Unix/Sed.html
```

---

**Description of the Slide:**

This slide provides examples of using the `sed` command-line utility.  It shows various `sed` commands, each demonstrating different text transformations.  The examples include:

*   `sed 's/test/another test/' filename`: Substitutes the string "test" with "another test" in each line of the file `filename`.
*   `sed 's/test//g' filename`: Globally substitutes the string "test" with nothing (removes all occurrences of "test") in each line of the file `filename`.
*   `sed '/regexp/d' filename`: Deletes lines containing the regular expression `regexp` in the file `filename`.
*   `sed '/^$/d' filename`: Deletes empty lines from the file `filename`.  The `^$` is a regular expression matching an empty line.
*   `sed -n '/Jones/p' filename`: Prints only the lines containing the string "Jones" in the file `filename`. The `-n` option suppresses the default printing of all lines.
*   `sed G filename`: Appends a newline and a blank line to each line of the file `filename`.

The slide also includes a URL (`http://www.grymoire.com/Unix/Sed.html`) which is a helpful resource for learning more about the `sed` command.  The slide is entirely text-based, with no figures or tables.  The purpose is to provide examples of how to use `sed` for text transformations.




# Slide 87

**`awk`**

`awk` provides a more traditional programming language for text processing than `sed`.
The major difference between `awk` and `sed` is that `awk` is record-oriented rather than line-oriented.
Each line of the input to `awk` is treated like a delimited record. The command line parameter `-F` sets the field delimiter.
The essential organization of an `awk` program has the form:

`pattern { action }`


---

**Description of the Slide:**

This slide introduces the `awk` command-line utility, highlighting its record-oriented approach to text processing, contrasting it with the line-oriented nature of `sed`.  It explains that `awk` treats each input line as a delimited record, and the `-F` parameter sets the field delimiter.  The general structure of an `awk` program is presented as `pattern { action }`.  The slide is entirely text-based, with no figures or tables.  The purpose is to introduce the `awk` command and its fundamental structure.


# Slide 88

**Description of the Slide:**

This slide introduces the `awk` command-line utility, focusing on its structure and use in data manipulation.  It describes the basic form of an `awk` command as `$ awk options program file`.  An example is given, `awk '{print $1}' myfile`, which prints the first field (`$1`) of each record in the file `myfile`.  The slide explains that `$0` represents the entire line, `$1` the first field, and `$n` the nth field.  Crucially, the slide emphasizes the ability to combine `awk` with other Unix tools like `grep` and `sed` using pipes.  An example is provided: `ps aux | grep "^bgstm" | awk '{print $2 + $3}'`, demonstrating how to chain commands to filter and process data.  The slide is entirely text-based, with no figures or tables.  The purpose is to introduce the `awk` command and its potential for complex data manipulation.


**What you see:**

The slide contains text explaining the `awk` command-line utility.  The text describes the basic structure of an `awk` command, including the use of `$1`, `$2`, etc., to access specific fields within a line.  It also shows how `awk` can be used in conjunction with other commands (like `grep`) to perform more complex data manipulations.  The example `ps aux | grep "^bgstm" | awk '{print $2 + $3}'` demonstrates how to chain commands together to filter and process data.  The slide is entirely text-based, with no figures or tables.  The text is formatted to clearly present the information about `awk` and its usage.


# Slide 89

**Description of the Slide:**

This slide provides examples of `awk` commands for manipulating data in text files, likely CSV files.  It shows three examples:

1.  **Adding a line number:** `awk '{print NR "," $0}' filename`.  This command adds a sequential line number (`NR`) to each line of the input file (`filename`), separated by a comma.  `NR` is a built-in variable in `awk` representing the record number.

2.  **Counting fields:** `awk -F "," '{print NF}' filename`.  This command, using a comma (`,`) as the field separator (`-F`), counts the number of fields (`NF`) in each line of the input file.  `NF` is a built-in variable in `awk` representing the number of fields in a record.

3.  **Formatting output:** `awk -F ":" '{ print "username: " $1, "uid:" $3 }' /etc/passwd`.  This command, using a colon (`:`) as the field separator (`-F`), extracts the username (`$1`) and user ID (`$3`) from the `/etc/passwd` file and formats the output as "username: username, uid: uid".  `$1` and `$3` are field variables in `awk`.

The slide also includes a URL (`http://www.grymoire.com/Unix/Awk.html`) for further reference on `awk` usage.  The slide is entirely text-based, with no figures or tables.  The purpose is to provide practical examples of `awk` commands for data manipulation.


**What you see:**

The slide contains a list of `awk` commands, each with a brief explanation.  The commands demonstrate how to use `awk` to process text files, including adding line numbers, counting fields, and formatting output.  The examples are presented in a clear and concise manner, making it easy to understand the purpose and usage of each command.  The URL is provided for additional resources.  The slide is entirely text-based, with no images or tables.
