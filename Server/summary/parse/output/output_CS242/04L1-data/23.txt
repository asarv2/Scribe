# 23

# Accessing Data (continued)

```python
import pandas as pd
data_oscar = pd.read_csv("oscar_age_female.csv")
```

# Accessing rows by index

```python
data_oscar.iloc[0]  # first row
```

```
Index     1
Year     1928
Age        22
Name    Janet Gaynor
Movie    Seventh Heaven,Street Angel and Sunrise: A Son...
Name: 0, dtype: object
```

# Accessing multiple rows

```python
data_oscar.iloc[[0, 2, 4]]  # rows 0, 2, and 4
```

```
    Index  Year  Age            Name               Movie
0       1  1928   22      Janet Gaynor  Seventh Heaven,Street Angel and Sunrise: A Son...
2       3  1930   28     Norma Shearer           The Divorcee
4       5  1932   32        Helen Hayes  The Sin of Madelon Claudet
```


---

**Description:**

This slide continues the demonstration of accessing data from a Pandas DataFrame, focusing on accessing rows by index.

* **Import Statement:** The slide starts with the Python code to import the Pandas library (`pd`) and read data from a CSV file ("oscar_age_female.csv") into a DataFrame (`data_oscar`).

* **Accessing a Single Row by Index:** The slide shows how to access the first row (`data_oscar.iloc[0]`).  The `iloc` method is used for integer-based indexing.  The output is a Pandas Series containing the values from the first row of the DataFrame.

* **Accessing Multiple Rows by Index:** The slide shows how to access multiple rows (rows 0, 2, and 4) using a list of indices (`data_oscar.iloc[[0, 2, 4]]`).  The output is a Pandas DataFrame containing only the specified rows.

* **Code Formatting:** The Python code is presented within code blocks, using backticks (````python`) to indicate code.  The code is formatted to be easily readable, with proper indentation and variable names.  The code is a complete Python script, which is a common way to present code examples in a data science lecture.

* **Output:** The slide shows the output of the Python code, which are Pandas Series and DataFrames.  The output is formatted to show the data in a tabular format, making it easy to see the values in the specified rows.  The output is a visual representation of the data accessed from the DataFrame.  The output shows the data from the specified rows, including the index, year, age, name, and movie.


**Overall:** The slide is part of a larger lecture on data science, specifically focusing on the Pandas library.  The slide demonstrates how to access data from a Pandas DataFrame, specifically accessing rows by index using the `iloc` method.  The code examples are complete Python scripts, which are a common way to present code examples in a data science lecture.  The code is likely intended to illustrate the accessing of data from a DataFrame, which is a fundamental operation in data analysis using Pandas.  The output is a visual representation of the data accessed from the DataFrame.
