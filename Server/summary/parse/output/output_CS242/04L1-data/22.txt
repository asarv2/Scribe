# 22

# Accessing Data

```python
import pandas as pd
data_oscar = pd.read_csv("oscar_age_female.csv")
```

# Accessing columns by name

```python
data_oscar['Age']  # Similar to dictionary references
data_oscar.Age    # Similar to an object field
```

```
0     22
1     37
2     28
3     63
4     32
...
```

# Accessing multiple columns

```python
data_oscar[['Year', 'Name']]
```

```
   Year                  Name
0  1928          Janet Gaynor
1  1929         Mary Pickford
2  1930        Norma Shearer
3  1931       Marie Dressler
4  1932          Helen Hayes
...
```


---

**Description:**

This slide demonstrates how to access data from a Pandas DataFrame, specifically accessing columns by name and multiple columns.

* **Import Statement:** The slide shows the Python code to import the Pandas library, aliased as `pd`, and read data from a CSV file named "oscar_age_female.csv" into a DataFrame named `data_oscar`.

* **Accessing Columns by Name:** The slide shows two ways to access a single column ('Age') by name:
    * `data_oscar['Age']`:  This uses bracket notation, similar to accessing a dictionary key.
    * `data_oscar.Age`: This uses dot notation, similar to accessing an attribute of an object.
    Both methods return a Pandas Series containing the values in the 'Age' column.  The output shows the first few values of the 'Age' column.

* **Accessing Multiple Columns:** The slide shows how to access multiple columns ('Year' and 'Name') using a list of column names within double brackets: `data_oscar[['Year', 'Name']]`.  This returns a DataFrame containing only the specified columns.  The output shows the first few rows of the 'Year' and 'Name' columns.

* **Code Formatting:** The Python code is presented within code blocks, using backticks (````python`) to indicate code.  The code is formatted to be easily readable, with proper indentation and variable names.  The code is a complete Python script, which is a common way to present code examples in a data science lecture.

* **Output:** The slide shows the output of the Python code, which are Pandas Series and DataFrames.  The output is formatted to show the data in a tabular format, making it easy to see the values in the specified columns.  The output is a visual representation of the data accessed from the DataFrame.


**Overall:** The slide is part of a larger lecture on data science, specifically focusing on the Pandas library.  The slide demonstrates how to access data from a Pandas DataFrame, specifically accessing columns by name and multiple columns.  The code examples are complete Python scripts, which are a common way to present code examples in a data science lecture.  The code is likely intended to illustrate the accessing of data from a DataFrame, which is a fundamental operation in data analysis using Pandas.
