# Slide 25

**Example: Compute Centroids**

<img src="image.png" alt="Scatter Plot with Centroids">


---

**Description of the Figure:**

A scatter plot is displayed, showing a large number of data points (small circles) scattered across a white background.  The data points are colored in three different colors:  reddish-brown, light gray/dark gray, and light blue.  These colors likely represent different clusters.  Superimposed on the scatter plot are three larger, differently colored stars.  These stars are positioned within the scatter plot, near the center of the clusters of data points.  The red star is near the cluster of reddish-brown points, the light blue star is near the cluster of light blue points, and the dark gray star is near the cluster of dark gray points.  The stars represent the centroids (or means) of the respective clusters.  The plot is two-dimensional, with no axes or labels visible.  The x and y axes are not labeled, and the scale is not specified.  The purpose of the scatter plot with the centroids is to illustrate a step in the K-means clustering algorithm, specifically the computation of centroids after the initial random assignment of data points to clusters.  The centroids are calculated as the mean of the data points within each cluster.


**Description of the Slide:**

This slide, titled "Example: Compute Centroids," displays a scatter plot with superimposed centroids.  The scatter plot shows data points colored by cluster, and the centroids (stars) are positioned near the center of each cluster.  This visual representation illustrates a step in the K-means clustering algorithm, where the algorithm calculates the mean of the data points within each cluster to determine the cluster centroid.  The slide is part of a lecture on clustering algorithms, specifically demonstrating the K-means algorithm.  The different colors of the data points and the centroids help to visually represent the clusters and their centers.
