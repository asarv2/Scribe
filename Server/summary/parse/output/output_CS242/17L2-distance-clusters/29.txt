# Slide 29

**Example: Re-compute Centroids**

<img src="image.png" alt="Scatter Plot with Updated Centroids">


---

**Description of the Figure:**

The slide displays a scatter plot.  Numerous small, colored circles are scattered across the plot area.  There are three distinct clusters of points, each colored differently: light blue, dark gray, and reddish-brown.  Within each cluster, the points are relatively close to each other.  Importantly, three larger, differently colored stars are positioned near the center of each cluster.  These stars represent the centroids of the respective clusters.  The plot is two-dimensional, with no axes or labels visible.  The x and y axes are not labeled, and the scale is not specified.  The different colors of the data points and the centroids help to visually represent the clusters and their centers.  The centroids have shifted positions compared to the previous scatter plot.  This indicates that the algorithm has recomputed the centroids based on the new cluster assignments.


**Description of the Slide:**

This slide, titled "Example: Re-compute Centroids," displays a scatter plot illustrating a step in the K-means clustering algorithm.  The plot shows data points colored by cluster, and the centroids are positioned near the center of each cluster.  This visual representation illustrates the recomputation of centroids based on the new cluster assignments.  The slide is part of a lecture on clustering algorithms, specifically demonstrating the K-means algorithm.  The different colors of the data points and the centroids help to visually represent the clusters and their centers.  The recomputation of the centroids is the key concept being illustrated.  The slide follows the previous example of reassignment, showing the next step in the iterative process of the K-means algorithm.  The updated positions of the centroids indicate that the algorithm has refined the cluster centers based on the latest data assignments.  The purpose of this slide is to visually demonstrate the iterative nature of the K-means algorithm, where points are repeatedly reassigned to clusters until the algorithm converges.  The updated positions of the centroids are the key visual difference from the previous slide.
