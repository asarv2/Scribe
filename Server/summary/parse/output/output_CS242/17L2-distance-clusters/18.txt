# Slide 18

**Example with matplotlib**

```python
import numpy as np
import matplotlib
import matplotlib.pyplot as plt
from scipy.spatial import distance_matrix
xyLocations = [[random.randrange(0,10), random.gauss(5,3)] for i in range(25)]
xyLocs = pd.DataFrame(xyLocations)
distances = distance_matrix(xyLocs.values,xyLocs.values)
print(distances[:5,:5])
[[0.         4.95848922 5.50745126 2.31171825 4.68541685]
 [4.95848922 0.         1.96229046 2.67138844 0.4903888 ]
 [5.50745126 1.96229046 0.         3.600969   2.3972855 ]
 [2.31171825 2.67138844 3.600969   0.         2.37484206]
 [4.68541685 0.4903888  2.3972855  2.37484206 0.        ]]
plt.imshow(distances)
```

**Description of the Figure:**

A heatmap is displayed, representing a distance matrix.  The heatmap is a 2D array of colored squares, where each square's color corresponds to a distance value.  The x and y axes of the heatmap correspond to the indices of the data points.  The color scale (likely a gradient from blue to yellow or red) indicates the magnitude of the distance.  Denser, warmer colors (yellow/red) represent larger distances, while cooler colors (blue) represent smaller distances.  The heatmap is positioned in the lower right quadrant of the slide.  The purpose of the heatmap is to visually represent the pairwise distances between 25 data points, likely in a multi-dimensional space.  The code snippet above the heatmap shows how the distance matrix is calculated using the `distance_matrix` function from the `scipy.spatial` library.  The `print(distances[:5,:5])` command displays the first five rows and columns of the distance matrix, providing a numerical representation of the distances.  The `plt.imshow(distances)` command creates the heatmap visualization.  The context of the slide suggests that this is an example of how to visualize distance matrices, which are crucial for clustering algorithms.


**Description of the Slide:**

This slide provides an example of visualizing a distance matrix using matplotlib.  The code snippet demonstrates how to calculate and display a distance matrix, which is a common task in data science, particularly when working with clustering algorithms.  The heatmap is a visual representation of the pairwise distances between data points, which is essential for understanding the relationships between data points and for choosing appropriate clustering methods.  The slide is part of a lecture on Introduction to Data Science, focusing on the practical application of distance measures in data analysis.
