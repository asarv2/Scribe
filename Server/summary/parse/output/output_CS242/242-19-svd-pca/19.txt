# Slide 19

**How to apply PCA in python**

```python
import pandas as pd
import matplotlib.pyplot as plt
from sklearn import decomposition
from sklearn import preprocessing

# read in data and mean center
data = pd.read_csv('iris.dat')
X = data.iloc[:,0:4].values
X_scaled = preprocessing.scale(X, with_std=False)

# initialize PCA model with four components
pca = decomposition.PCA(n_components=4)

# fit PCA model with scaled data and output transformed data
X_trans = pca.fit_transform(X_scaled)
```

---

**Description:**

This slide presents Python code for applying Principal Component Analysis (PCA) to a dataset.  It's part of an Intro to Data Science lecture, demonstrating the practical implementation of PCA using the scikit-learn library.

The code snippet shows the steps involved in applying PCA:

1. **Import necessary libraries:** `pandas` for data manipulation, `matplotlib.pyplot` for potential plotting, `sklearn.decomposition` for PCA, and `sklearn.preprocessing` for data scaling.

2. **Load and prepare data:** The code loads data from a CSV file named 'iris.dat' using pandas.  It then extracts the relevant features (columns 0 to 3) into a NumPy array `X`.  Crucially, it scales the data using `preprocessing.scale(X, with_std=False)`.  This step is important for PCA, as it ensures that features with larger values don't disproportionately influence the principal components.  The `with_std=False` parameter likely indicates that the data is scaled by subtracting the mean of each feature, but not by dividing by the standard deviation.

3. **Initialize PCA model:** A PCA model is created using `decomposition.PCA(n_components=4)`.  This specifies that the dimensionality should be reduced to 4 components.

4. **Apply PCA:** The `fit_transform` method is used to apply PCA to the scaled data (`X_scaled`). The result, the transformed data, is stored in `X_trans`.

The code is presented in a way that clearly shows the steps involved in applying PCA in Python, using the scikit-learn library.  The context of the slide is to demonstrate how to implement PCA in a practical setting, likely following the theoretical explanation of PCA in previous slides.  There are no figures on this slide.
