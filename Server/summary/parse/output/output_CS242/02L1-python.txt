# Slide 1

Introduction to Data Science
Purdue CS24200
Tony Bergstrom
Slides adapted from Jen Neville, CS 24200


---

**Description of the Slide:**

This is the title slide for a lecture on Introduction to Data Science.  The background is a dark gray.  The title "Introduction to Data Science" is large and prominent, centered at the top of the slide.  Below that, in a slightly smaller font, are the course information: "Purdue CS24200" and "Tony Bergstrom" (presumably the instructor).  Finally, at the very bottom, in a very small font, is a credit line: "Slides adapted from Jen Neville, CS 24200".  There are no figures or images on this slide.  The text is simple, clear, and provides basic course information.


# Slide 2

# Python Basics

A review of what you should know from 38003


---

**Description of the Slide:**

This slide, a plain gray background, has a single, centered heading: "Python Basics".  Below that, in a smaller font, is a subtitle: "A review of what you should know from 38003".  The slide is straightforward and indicates the topic of the upcoming content: a review of Python programming concepts, likely building upon material covered in a prerequisite course (38003).


# Slide 3

# Python Environments

For this course, we will use Python 3
We will, at least initially, use Python notebooks
For labs, you will code directly using notebooks in Vocareum
For homeworks, you can use Vocareum or your own local environment
To install on your local machine, we recommend using Anaconda


---

**Description of the Slide:**

This slide, a plain, light gray background, details the Python environment for the Intro to Data Science course.  The heading "Python Environments" is in a larger, bold font, clearly indicating the topic.  Below that, bullet points describe the specific Python version (Python 3) and the use of Python notebooks for the course.  The slide also explains the use of Vocareum for labs and homework assignments, and recommends Anaconda for local installations.  The information is presented in a clear, concise manner, providing necessary details for students to set up their Python environment for the course.


# Slide 4

# Anaconda

Download Anaconda: https://www.anaconda.com/download/
Install by following the instructions on the download page.
Run Anaconda Navigator
Install Jupyter Notebook by clicking “Launch Jupyter Notebook” from anaconda GUI.
To run the notebook, start the notebook server from the command line:
jupyter notebook
You should see the notebook open in your browser


---

**Description of the Slide:**

This slide, a light-gray background, provides instructions on installing and running Anaconda, a Python distribution commonly used in data science.  The heading "Anaconda" is in a large, bold font.  Below that, the slide lists steps for downloading, installing, and launching Jupyter Notebook, a popular interactive coding environment.  The slide also includes a link to the Anaconda download page.  A small, green Anaconda logo is present in the upper right corner of the slide.  The logo is a stylized circle with a grid-like pattern inside, and the word "ANACONDA" is written below it in a similar green color.  The presence of the logo and the link to the download page clearly indicate that this slide is part of a larger set of instructions for setting up the necessary software environment for the Intro to Data Science course.  The instructions are presented in a step-by-step format, making them easy to follow.


# Slide 5

# Jupyter Notebooks

Jupyter notebooks let you create and share documents that contain
code, equations, visualizations and narrative text
Notebooks support many programming languages, including Python, R, Julia, and Scala
It is based on a set of open standards for interactive computing (like html and CSS)


---

**Description of the Slide:**

This slide, a light gray background, introduces Jupyter Notebooks.  The heading "Jupyter Notebooks" is in a large, bold font, clearly indicating the topic.  Below the heading, the slide provides a description of Jupyter Notebooks, explaining that they are used to create and share documents containing code, equations, visualizations, and narrative text.  The slide also mentions that Jupyter Notebooks support various programming languages, including Python, R, Julia, and Scala.  Finally, it notes that Jupyter Notebooks are based on open standards for interactive computing, similar to HTML and CSS.  There are no figures on this slide.  The text is presented in a clear and concise manner, providing a general overview of Jupyter Notebooks for the Intro to Data Science course.


# Slide 6

# Why Python?

Interpreted language
Dynamically typed: variables do not have a predefined type
Rich, built-in collection types:
* Lists, Tuples, Dictionaries (i.e., maps), Sets
Concise but unobtrusive object system
* Every value is an object
* Classes guide but do not dominate object construction

Slides adapted from UPenn CIS530 python tutorial


---

**Description of the Slide:**

This slide, a light gray background, presents reasons for using Python in the context of the Intro to Data Science course.  The heading "Why Python?" is in a large, bold font, clearly indicating the topic.  Below the heading, bullet points list key characteristics of the Python language, such as being interpreted, dynamically typed, and having rich built-in data structures (lists, tuples, dictionaries, and sets).  The slide also highlights Python's concise and unobtrusive object system, where every value is an object, and classes play a guiding but not dominant role in object construction.  The text is presented in a clear and concise manner, providing a rationale for choosing Python as the programming language for the course.  There are no figures on this slide.  The footer indicates the source of the information, which is a tutorial from UPenn CIS530.


# Slide 7

# Language features

Indentation instead of braces
Newline separates statements
Several sequence types
* Strings “…”: made of characters, immutable
* Lists […]: made of anything, mutable
* Tuples (...): made of anything, immutable
Powerful subscripting (slicing)
Functions are first class objects
Exceptions as in Java

Slides adapted from UPenn CIS530 python tutorial


---

**Description of the Slide:**

This slide, a light gray background, details various language features of Python, likely in the context of the Intro to Data Science course.  The heading "Language features" is in a large, bold font, indicating the topic.  Below the heading, bullet points describe key Python features: indentation instead of braces, newline separation of statements, and different sequence types (strings, lists, and tuples) with their respective mutability characteristics.  The slide also mentions powerful subscripting (slicing), functions as first-class objects, and exception handling similar to Java.  The footer indicates that the slide content is adapted from a Python tutorial from the University of Pennsylvania's CIS530 course.  There are no figures on this slide.  The text is presented in a clear and concise manner, providing a summary of Python's syntax and features relevant to the course.


# Slide 8

# Dynamic typing

Java: statically typed
* Variables are declared to refer to objects of a given type
* Methods use type signatures to enforce contracts

Python
* Variables come into existence when first assigned to
* A variable can refer to an object of any type
* All types are (almost) treated the same way
* Main drawback: type errors are only caught at runtime

Slides adapted from UPenn CIS530 python tutorial


---

**Description of the Slide:**

This slide, a light-gray background, discusses the concept of dynamic typing in programming languages, specifically comparing Java and Python.  The heading "Dynamic typing" is in a large, bold font, clearly indicating the topic.  The slide presents a comparison table-like format, contrasting Java's static typing with Python's dynamic typing.  The bullet points under "Java" describe how variables are declared with specific types and how methods use type signatures to enforce these types.  In contrast, the bullet points under "Python" explain how variables are created when assigned a value, can hold objects of any type, and treat all types similarly.  The final bullet point under "Python" highlights the potential runtime errors associated with dynamic typing.  The footer indicates that the content is adapted from a Python tutorial from the University of Pennsylvania's CIS530 course.  There are no figures on this slide.  The text is presented in a clear and concise manner, providing a comparison of static and dynamic typing for the Intro to Data Science course.


# Slide 9

# Running Python On Unix...

```
% python                  # opens interpreter
>>> 3+3
6
% python filename.py      # runs script via command line
% jupyter notebook        # opens jupyter notebook
```

Slides adapted from UPenn CIS530 python tutorial


---

**Description of the Slide:**

This slide, a light-gray background, provides examples of how to run Python code on a Unix-like operating system (likely within the context of the Intro to Data Science course).  The examples are presented in a command-line interface style, using the `%` symbol to represent the shell prompt.  The slide shows three different ways to interact with Python:

1. **`% python`**: This command opens the Python interpreter, allowing interactive coding.  The example shows the calculation `3+3` being executed within the interpreter.

2. **`% python filename.py`**: This command runs a Python script named `filename.py` from the command line.

3. **`% jupyter notebook`**: This command launches the Jupyter Notebook environment, a popular interactive development environment for data science tasks.

The text is presented in a clear, step-by-step format, demonstrating different ways to execute Python code.  The comments to the right of each command (`# opens interpreter`, etc.) explain the purpose of each command.  The footer indicates that the slide content is adapted from a Python tutorial from the University of Pennsylvania's CIS530 course.  There are no figures on this slide.


# Slide 10

# Playing with Python

```
>>> 2+3
5
>>> 2.0/3
0.66666666666666663
>>> x=4.5
>>> int(x)
4
>>> x='abc'
>>> x[0]
'a'
>>> x[1:3]
'bc'
>>> x[:2]
'ab'
```

Slides adapted from UPenn CIS530 python tutorial


---

**Description of the Slide:**

This slide demonstrates various Python code examples, likely within the context of an Intro to Data Science course.  The heading "Playing with Python" indicates the slide's focus on interactive Python code execution.  The content consists of a series of Python expressions and their expected outputs, displayed in a typical interactive Python interpreter session.  Each line starting with `>>>` represents a user input, and the subsequent output is shown on the next line.  The examples cover basic arithmetic operations (addition, division), type conversion (integer conversion), and string manipulation (accessing characters and substrings).  The examples are presented in a clear, concise manner, showing how to use Python for different tasks.  The footer indicates that the slide content is adapted from a Python tutorial from the University of Pennsylvania's CIS530 course.  There are no figures on this slide.




# Slide 11

```markdown
# Playing with Python

>>> x=['a','b','c']
>>> x[1]
'b'
>>> x[1:]
['b', 'c']
>>> x[1]='d'
>>> x
['a', 'd', 'c']
>>> d={'alice':1234, 'bob':5678, 'clare':9012}
>>> d['bob']
5678
>>> d['bob'] = 7777
>>> d
{'clare': 9012, 'bob': 7777, 'alice': 1234}
>>> d.keys()
dict.keys(['clare', 'bob', 'alice'])
>>> d.items()
dict.items([('clare', 9012), ('bob', 7777), ('alice', 1234)])
>>> del d['bob']
```

Slides adapted from UPenn CIS530 python tutorial


---

**Description of the Slide:**

This slide, titled "Playing with Python," presents a series of interactive Python code examples.  The content is formatted as a Python interpreter session, showing user inputs (preceded by `>>>`) and the corresponding outputs.  The examples demonstrate various Python functionalities, including list manipulation (accessing elements, slicing, modifying elements), dictionary creation and manipulation (accessing values, updating values, deleting keys), and basic data types.  The output of each command is shown immediately below the input, demonstrating the results of the Python operations.  The examples are designed to illustrate how to use Python for basic data manipulation tasks.  The footer indicates that the slide content is adapted from a Python tutorial from the University of Pennsylvania's CIS530 course.  There are no figures on this slide.


# Slide 12

```
x = 34 - 23  # A comment.
y = 'Hello'  # Another one.
z = 3.45
if z == 3.45 or y == 'Hello':
    x = x + 1
    y = y + ' World'  # String concat.
    print(x)
    print(y)
```

Slides adapted from UPenn CIS530 python tutorial


---

**Description of the Slide:**

This slide presents a simple Python code example.  The code defines variables `x`, `y`, and `z` with integer, string, and float values, respectively.  It then uses an `if` statement to check a condition involving `z` and `y`.  If the condition is true, it increments `x` and concatenates the string ' World' to `y`. Finally, it prints the updated values of `x` and `y`.  The code is formatted in a way that clearly shows the structure of the Python program.  There are no figures on this slide.  The code is a simple example of conditional statements and string concatenation in Python, likely intended to illustrate basic programming concepts in the context of the Intro to Data Science course.  The comments (`# A comment`, etc.) are used to explain the purpose of each line of code.  The footer indicates that the slide content is adapted from a Python tutorial from the University of Pennsylvania's CIS530 course.


# Slide 13

```markdown
To Understand the Code
Indentation matters to the meaning of the code:
* Block structure indicated by indentation
The first assignment to a variable creates it
* Variable types don't need to be declared, Python figures out the variable types on its own
Assignment uses = and comparison uses ==.
For numbers + * / % are as expected.
* Special use of + for string concatenation; Special use of % for string formatting (as with printf in C)
Logical operators are words (and, or, not) not symbols
Simple printing can be done with print()
Slides adapted from UPenn CIS530 python tutorial
```

---

**Description:**

This slide provides a summary of key Python coding concepts, likely in the context of an Intro to Data Science course.  The title, "To Understand the Code," suggests that the content is about understanding how Python code works.  The bullet points explain important aspects of Python syntax and semantics:

* **Indentation:** Python uses indentation to define code blocks, unlike other languages that use braces.
* **Variable Creation:** The first time a variable is assigned a value, it is created.
* **Dynamic Typing:** Python automatically determines the data type of a variable based on the assigned value.
* **Operators:**  Standard arithmetic operators (+, -, \*, /, %) work as expected.  The plus sign (+) is also used for string concatenation.  The percent sign (%) is used for string formatting, similar to `printf` in C.
* **Logical Operators:** Python uses words (e.g., `and`, `or`, `not`) for logical operations, not symbols.
* **Printing:** The `print()` function is used for displaying output.

The footer indicates that the content is adapted from a Python tutorial from the University of Pennsylvania's CIS530 course.  There are no figures on this slide.  The text is presented in a clear and concise manner, providing a summary of Python's syntax and features relevant to the course.


# Slide 14

**Basic datatypes**

Integers (default for numbers)
z = 5 / 2  # Python 2 uses integer division, Python 3 does not
Floats
x = 3.456
Strings
"abc"       # Can use "" or '' to specify strings
'abc'
"matt's"    # Unmatched quotes can appear within the string
"""a'b"c""" # Use triple double quotes to specify multiline strings,
            # or combinations of ' and " inside a string

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide, titled "Basic datatypes," lists examples of different data types in Python.  It demonstrates how integers, floats, and strings are represented in Python code.  The examples include integer division (`z = 5 / 2`), a float (`x = 3.456`), and various string representations, including single-quoted, double-quoted, and triple-quoted strings.  The comments (`# Python 2 uses integer division...`) explain the context of the examples, particularly the difference in integer division behavior between Python 2 and Python 3.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The examples are presented in a clear, concise manner, providing a basic understanding of data types in Python.


# Slide 15

**Whitespace**

Whitespace is meaningful in Python: especially indentation and placement of newlines.
Use a newline to end a line of code. Use \ when must go to next line prematurely.
No braces {} to mark blocks of code in Python...
Use consistent indentation instead.
* The first line with less indentation is outside of the block
* The first line with more indentation starts a nested block
Often a colon appears at the start of a new block (E.g., for function and class definitions)
Start comments with # – the rest of line is ignored.

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide discusses the importance of whitespace, particularly indentation, in Python code.  It emphasizes that Python uses indentation to define code blocks instead of curly braces.  The slide explains that a newline ends a line of code, and the backslash character (`\`) is used to continue a line onto the next line.  The slide also notes that comments begin with the `#` symbol.  The content is presented in a bulleted list format, making it easy to understand the key points.  The footer indicates that the slide content is adapted from a Python tutorial from the University of Pennsylvania's CIS530 course.  There are no figures on this slide.  The text is presented in a clear and concise manner, providing a summary of Python's syntax and features relevant to the Intro to Data Science course.


# Slide 16

**Assignment**

Binding a variable in Python means setting a name to hold a reference to some object.

* Assignment creates references, not copies
Names in Python do not have an intrinsic type. Objects have types.
* Python determines the type of the reference automatically based on what data is assigned to it.

You create a name the first time it appears on the left side of an assignment expression:

```
x = 3
```

A reference is deleted via garbage collection after any names bound to it have passed out of scope.

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide explains the concept of variable assignment in Python.  It emphasizes that assignment in Python creates references to objects, not copies of them.  The slide also highlights that variables themselves don't have inherent types; rather, the objects they reference have types. Python automatically determines the type of the reference based on the assigned data.  The example `x = 3` demonstrates how a variable is created when assigned a value.  Finally, the slide notes that references are removed from memory through garbage collection when no longer in use.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The text is presented in a clear and concise manner, explaining a fundamental concept in Python programming.




# Slide 17

**Sequence Types**

Tuple
* A simple **immutable** ordered sequence of items
* Items can be of mixed types, including collection types

Strings
* **Immutable**; Conceptually very much like a tuple

List
* **Mutable** ordered sequence of items of mixed types

All three sequence types (tuples, strings, and lists) share much of the same syntax and functionality.

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide, titled "Sequence Types," describes the fundamental sequence data types in Python.  It presents a bulleted list outlining the characteristics of tuples, strings, and lists.  The key takeaway is that tuples and strings are immutable, meaning their contents cannot be changed after creation, while lists are mutable, allowing modifications.  The slide also highlights that these three data types share similar syntax and functionalities, making them versatile tools for organizing and manipulating data.  The footer indicates that the content is adapted from a Python tutorial from the University of Pennsylvania's CIS530 course.  There are no figures on this slide.  The text is presented in a clear and concise manner, providing a summary of sequence types in Python for the Intro to Data Science course.


# Slide 18

**Sequence Types**

Tuples are defined using parentheses (and commas).
```
>>> tu = (23, 'abc', 4.56, (2, 3), 'def')
```
Lists are defined using square brackets (and commas).
```
>>> li = ["abc", 34, 4.34, 23]
```
Strings are defined using quotes ("', or """).
```
>>> st = "Hello World"
>>> st = 'Hello World'
>>> st = """This is a multi-line
string that uses triple quotes."""
```


---

**Description:**

This slide demonstrates how to create tuples, lists, and strings in Python.  It shows examples of defining these sequence types using parentheses, square brackets, and quotation marks, respectively.  The examples include tuples with various data types (integers, strings, floats, and nested tuples), lists with mixed data types, and strings with single quotes, double quotes, and triple quotes (for multiline strings).  The examples are presented in a way that clearly shows the syntax for creating these data structures.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The examples are presented in a clear and concise manner, providing a basic understanding of sequence types in Python.  The use of the `>>>` prompt indicates that these are examples of interactive Python code.


# Slide 19

**Sequence Types**

We can access individual members of a tuple, list, or string using square bracket "array" notation. Note that all are 0 based

```
>>> tu = (23, 'abc', 4.56, (2,3), 'def')
>>> tu[1]
'abc'
# Second item in the tuple.
>>> li = ["abc", 34, 4.34, 23]
>>> li[1]
34
# Second item in the list.
>>> st = "Hello World"
>>> st[1]
'e'
# Second character in string.
```

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide demonstrates how to access elements within tuples, lists, and strings using Python's indexing (square bracket) notation.  The examples show how to retrieve the second element (index 1) from each data structure.  The output of each `[1]` operation is shown directly below the line containing the indexing operation.  The comments explain the purpose of each line, specifically identifying the element being accessed.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The examples are presented in a clear and concise manner, providing a basic understanding of indexing in Python sequences.  The `>>>` symbol indicates that these are examples of interactive Python code.




# Slide 20

**Slicing**

Lists can be indexed with positive or negative numbers

```
>>> tu = (23, 'abc', 4.56, (2,3), 'def')
```

Positive index: count from the left, starting with 0.

```
>>> tu[1]
'abc'
```

Negative lookup: count from right, starting with -1.

```
>>> tu[-3]
4.56
```

```
>>> tu = (23, 'abc', 4.56, (2,3), 'def')
```

Return a copy of the container with a subset of the original members. Start copying at the first index, and stop copying before the second index.

```
>>> tu[1:4]
('abc', 4.56, (2,3))
```

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide demonstrates slicing in Python, specifically with tuples.  It shows how to access elements of a tuple using both positive and negative indices.  The example `tu = (23, 'abc', 4.56, (2,3), 'def')` defines a tuple.  The code `tu[1]` accesses the second element ('abc') of the tuple.  `tu[-3]` accesses the fourth element (4.56) from the right.  Finally, `tu[1:4]` demonstrates slicing, returning a new tuple containing elements from index 1 up to, but not including, index 4.  The examples are presented in a clear, step-by-step format, illustrating the use of slicing in Python.  The `>>>` symbols indicate interactive Python code execution.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.


# Slide 21

**The 'in' operator**

Boolean test whether a value is in a collection

```
>>> t = [1, 2, 4, 5]
>>> 3 in t
False
>>> 4 in t
True
>>> 4 not in t
False
```

For strings, tests for substrings

```
>>> a = 'abcde'
>>> 'c' in a
True
>>> 'cd' in a
True
>>> 'ac' in a
False
```

Be careful: the `in` keyword is also used in the syntax of for loops and list comprehensions.

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide demonstrates the use of the `in` operator in Python, which checks if a value exists within a collection (like a list or tuple) or if a substring exists within a string.  The examples show how the `in` operator returns `True` if the value is found and `False` otherwise.  The examples also illustrate how the `in` operator works with strings, checking for substrings.  The slide also includes a cautionary note about the `in` keyword's use in other Python constructs like loops and list comprehensions.  The `>>>` symbols indicate interactive Python code execution.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The examples are presented in a clear and concise manner, providing a basic understanding of the `in` operator in Python.




# Slide 22

**The '+' operator**

The + operator produces a new tuple, list, or string by concatenating its arguments.

```
>>> (1, 2, 3) + (4, 5, 6)
(1, 2, 3, 4, 5, 6)
>>> [1, 2, 3] + [4, 5, 6]
[1, 2, 3, 4, 5, 6]
>>> "Hello" + " " + "World"
'Hello World'
```

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide demonstrates the use of the `+` operator for concatenating sequences (tuples, lists, and strings) in Python.  The examples show how the `+` operator creates a new sequence containing all the elements of the original sequences combined.  The examples are presented in a clear, step-by-step format, illustrating the concatenation operation.  The `>>>` symbols indicate interactive Python code execution.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The examples are presented in a way that clearly shows the result of the concatenation operation.  The output of each operation is shown directly below the input, demonstrating the concatenation result.




# Slide 23

**Lists and tuples**

We can change lists in place. The name li still points to the same memory reference when we're done.

```
>>> li = ['abc', 23, 4.34, 23]
>>> li[1] = 45
>>> li
['abc', 45, 4.34, 23]
```

You can’t change a tuple. You can make a fresh tuple and assign its reference to a previously used name.

```
>>> tu = (23, 'abc', 4.56, (2, 3), 'def')
>>> tu[2] = 3.14
TypeError: 'tuple' object does not support item assignment
```

The immutability of tuples means they’re faster than lists.

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide, titled "Lists and tuples," demonstrates the difference between mutable lists and immutable tuples in Python.  The first part of the slide shows how a list (`li`) can be modified in place.  The code `li[1] = 45` changes the second element of the list from 23 to 45, and the output shows the updated list.  This demonstrates that lists are mutable; changes made to a list directly affect the original list object.

The second part of the slide shows an attempt to modify a tuple (`tu`).  The code `tu[2] = 3.14` tries to change the third element of the tuple, but this results in a `TypeError`.  This demonstrates that tuples are immutable; attempting to change an element of a tuple will raise an error.  The slide concludes by stating that the immutability of tuples makes them potentially faster than lists in certain situations.

The slide uses the `>>>` prompt to indicate interactive Python code.  The examples are presented in a clear, concise manner, highlighting the key difference in mutability between lists and tuples.  The footer indicates that the content is adapted from a Python tutorial from the University of Pennsylvania's CIS530 course.  There are no figures on this slide.


# Slide 24

**Operations on lists**

Adding new elements to a list
```
>>> li = [1, 11, 3, 4, 5]
>>> li.append('a')  # Note the method syntax
>>> li
[1, 11, 3, 4, 5, 'a']
>>> li.insert(2, 'i')
>>> li
[1, 11, 'i', 3, 4, 5, 'a']
```
+ creates a fresh list (with a new memory reference)
extend operates on list `li` in place.
```
>>> li.extend([9, 8, 7])
>>> li
[1, 2, 'i', 3, 4, 5, 'a', 9, 8, 7]
```

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide demonstrates various operations for adding elements to a Python list.  The examples use the `append()` and `insert()` methods to add elements to the end and at a specific index, respectively.  The `extend()` method is also shown, which adds multiple elements from an iterable (in this case, a list) to the end of the existing list.  The `>>>` symbols indicate interactive Python code execution.  The examples show the results of these operations, demonstrating how the list `li` is modified.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The examples are presented in a clear, concise manner, illustrating how to manipulate lists in Python.  The comment `# Note the method syntax` highlights the use of methods to modify lists.  The text explaining the `extend()` method clarifies that it modifies the original list in place, unlike `append()` which creates a new list.


# Slide 25

**Operations on lists**

```
>>> li = ['a', 'b', 'c', 'b']
>>> li.index('b')  # index of first occurrence*
1
>>> li.count('b')  # number of occurrences
2
>>> li.remove('b') # remove first occurrence
>>> li
['a', 'c', 'b']
>>> li = [5, 2, 6, 8]
>>> li.reverse()  # reverse the list *in place*
>>> li
[8, 6, 2, 5]
>>> li.sort()  # sort the list *in place*
>>> li
[2, 5, 6, 8]
>>> li.sort(some_function)  # sort in place using user-defined comparison
```

---

**Description:**

This slide demonstrates various operations that can be performed on lists in Python.  The examples show how to use methods like `index()`, `count()`, `remove()`, `reverse()`, and `sort()` to manipulate lists.  The `>>>` symbols indicate interactive Python code execution.  The comments in the code explain the purpose of each operation.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The examples are presented in a clear, concise manner, illustrating how to perform common list operations in Python.  The asterisk (*) next to some operations indicates that they modify the list in place.  The `some_function` suggests that a custom function could be used for the sorting operation.  The slide is focused on the methods available for list manipulation.


# Slide 26

**Dictionaries: A Mapping type**

Dictionaries store a mapping between a set of keys and a set of values

* Keys can be any immutable type
* Values can be any type
* Values and keys can be of different types in a single dictionary

You can define, modify, view, lookup, delete the key-value pairs in the dictionary

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide introduces dictionaries in Python, highlighting their role as mapping data structures.  It explains that dictionaries store key-value pairs, where keys must be immutable types (like strings, numbers, or tuples), and values can be of any data type.  The slide also emphasizes that keys and values within a single dictionary can have different types.  The final line indicates the various operations that can be performed on dictionaries, such as defining, modifying, viewing, looking up, and deleting key-value pairs.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The text is presented in a clear and concise manner, providing a basic understanding of dictionaries in Python.  There are no figures on this slide.




# Slide 27

**Creating and accessing dictionaries**

```
>>> d = {'user':'bozo', 'pswd':1234}
>>> d['user']
'bozo'
>>> d['pswd']
1234
>>> d['bozo']
KeyError: bozo
>>> d['user'] = 'clown'
>>> d
{'user':'clown', 'pswd':1234}
Keys must be unique. Assigning to an existing key replaces its value.
>>> d['id'] = 45
>>> d
{'user':'clown', 'id':45, 'pswd':1234}
Dictionaries are unordered; New entry might appear anywhere in the output. (Dictionaries work by hashing)
```

**Description:**

This slide demonstrates how to create and access elements in Python dictionaries.  The examples show how to define a dictionary `d` with key-value pairs.  The code then demonstrates how to access values using their corresponding keys (`d['user']`, `d['pswd']`).  It also shows a `KeyError` when trying to access a non-existent key (`d['bozo']`).  The slide further illustrates updating a value associated with an existing key (`d['user'] = 'clown'`) and adding a new key-value pair (`d['id'] = 45`).  The output shows the updated dictionary after each operation.  The final line notes that dictionaries are unordered and that the order of entries in the output might vary.  The `>>>` symbols indicate interactive Python code execution.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The examples are presented in a clear, concise manner, providing a basic understanding of dictionaries in Python.




# Slide 28

**Updating dictionaries**

```
>>> d = {'user':'bozo', 'p':1234, 'i':34}
>>> del d['user']           # Remove one.
>>> d
{'p':1234, 'i':34}
>>> d.clear()               # Remove all.
>>> d
{}
>>> a = [1,2]
>>> del a[1]	 	
	
        # (del also works on lists)
>>> a
[1]
>>> d = {'user':'bozo', 'p':1234, 'i':34}
>>> d.keys()                # List of current keys, Python 3 returns iterable object
dict.keys(['user', 'p', 'i'])
>>> d.values()              # List of current values.
dict.values(['bozo', 1234, 34])
>>> d.items()               # List of item tuples.
dict.items([('user','bozo'), ('p',1234), ('i',34)]]
```

**Description:**

This slide demonstrates various operations for updating dictionaries in Python.  The examples show how to remove a key-value pair using `del d['user']`, clear all key-value pairs using `d.clear()`, and demonstrate that `del` also works on lists.  The slide also shows how to retrieve the keys, values, and items of a dictionary using `d.keys()`, `d.values()`, and `d.items()`, respectively.  The `>>>` symbols indicate interactive Python code execution.  The comments in the code explain the purpose of each operation.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The examples are presented in a clear, concise manner, illustrating how to manipulate dictionaries in Python.  The output of each operation is shown directly below the input, demonstrating the effect of the operation on the dictionary.  The slide is focused on dictionary manipulation, specifically updating and retrieving information from dictionaries.


# Slide 29

**Booleans**

True and False are constants in Python
Other values equivalent to True and False:
* False: zero, None, empty container or object
* True: non-zero numbers, non-empty objects
Comparison operators: ==, !=, <, <=, etc.
* X and Y have same value: X == Y
* Compare with: X is Y
* X and Y are two variables that refer to the identical object

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide discusses Boolean values in Python.  It defines what values are considered `True` and `False` in Python, including zero, `None`, and empty containers.  It also explains the difference between comparing values (`X == Y`) and comparing object identity (`X is Y`).  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The text is presented in a clear and concise manner, providing a summary of Boolean values and comparison operators in Python.  There are no figures on this slide.




# Slide 30

**Boolean Logic Expressions**

You can also combine Boolean expressions.

* **a and b**  True if a is True and b is True
* **a or b**  True if a is True or b is True
* **not a**  True if a is False

Use parentheses as needed to disambiguate complex Boolean expressions.

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide explains how to combine Boolean expressions in Python using the `and`, `or`, and `not` operators.  It provides the truth tables for these operators, showing when the combined expression evaluates to `True` or `False`.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The examples are presented in a clear, concise manner, providing a basic understanding of Boolean logic in Python.  There are no figures on this slide.




# Slide 31

**if Statements**

Use of indentation for blocks, Colon (:) after boolean expression

```
if x == 3:
    print("X equals 3.")
elif x == 2:
    print("X equals 2.")
else:
    print("X equals something else.")
print("This is outside the 'if'.")
```

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide demonstrates the use of `if`, `elif`, and `else` statements in Python.  The code example shows a conditional statement that checks the value of the variable `x`.  If `x` is equal to 3, the first `print` statement is executed.  If `x` is equal to 2, the second `print` statement is executed.  Otherwise, the `else` block is executed, printing a different message.  Finally, a `print` statement outside the `if` block is executed regardless of the conditions within the `if` statement.  The code is formatted to clearly show the indentation used to define the blocks of code associated with each condition.  The `>>>` symbols are not present, indicating that this is not an interactive Python session but rather a demonstration of the code's structure.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.




# Slide 32

**while Loops**

```
>>> x = 3
>>> while x < 5:
	 print(x, "still in the loop")
	 x = x + 1
3 still in the loop
4 still in the loop
>>> x = 6
>>> while x < 5:
	 print(x, "still in the loop")
>>>
```

**Description:**

This slide demonstrates the use of a `while` loop in Python.  The code shows a `while` loop that continues as long as the value of `x` is less than 5.  Inside the loop, it prints the current value of `x` and the message "still in the loop".  After each iteration, `x` is incremented by 1.  The first example shows the loop executing twice, printing "3 still in the loop" and "4 still in the loop".  The second example shows the loop not executing at all because the condition `x < 5` is false from the start.  The `>>>` symbols indicate interactive Python code execution.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The examples are presented in a clear, concise manner, illustrating how to use `while` loops for repeated execution of code blocks.




# Slide 33

**for Loops**

For-each is Python's only for construction
A for loop steps through each of the items in a collection type, or any other type of object which is "iterable"
```
for <item> in <collection>:
    <statements>
```
If `<collection>` is a list or a tuple, then the loop steps through each element of the sequence.
If `<collection>` is a string, then the loop steps through each character of the string.
```
for someChar in "Hello World":
    print(someChar)
```
`<item>` can be more complex than a single variable name.
When the elements of `<collection>` are themselves sequences, then `<item>` can match the structure of the elements.
```
for (x, y) in [('a', 1), ('b', 2), ('c', 3), ('d', 4)]:
    print(x)
```

**Description:**

This slide explains the `for` loop construct in Python.  It emphasizes that the `for` loop in Python is a "for-each" loop, iterating over the elements of an iterable object.  The syntax is shown, with `<item>` representing the variable that will hold each element during an iteration, and `<collection>` representing the iterable object.  The slide provides examples of iterating over a string ("Hello World") and a list of tuples.  The examples demonstrate how the `for` loop can handle different data structures and how the `<item>` variable can be more complex than a simple variable name (e.g., a tuple `(x, y)` in the second example).  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The examples are presented in a clear, concise manner, illustrating the use of `for` loops for iterating over various data structures in Python.




# Slide 34

**for Loops and Dictionaries**

```
>>> ages = { "Sam" :4, "Mary" :3, "Bill" :2 }
>>> ages
{'Bill': 2, 'Mary': 3, 'Sam': 4}
>>> for name in ages.keys():
	 print(name, ages[name])
Bill 2
Mary 3
Sam 4
>>>
```

**Description:**

This slide demonstrates how to iterate through a dictionary using a `for` loop.  The code defines a dictionary `ages` containing names as keys and ages as values.  The `for` loop iterates over the keys of the dictionary using `ages.keys()`.  Inside the loop, `ages[name]` accesses the value associated with each key, and the code prints the key-value pair.  The output shows the names and corresponding ages printed in a formatted way.  The `>>>` symbols indicate interactive Python code execution.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The example demonstrates how to use a `for` loop to efficiently access and process data from a dictionary.  The output shows the expected result of the loop, printing each name and its corresponding age.  The code is clear and concise, illustrating a common task in data processing.


# Slide 35

**String Operations**

A number of methods for the string class perform useful formatting operations:

```
>>> "hello".upper()
'HELLO'
>>> "Hello".lower()
'hello'
```

Check the Python documentation for many other handy string operations.

Helpful hint: use `<string>.strip()` to strip off final newlines from lines read from files

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide, titled "String Operations," demonstrates string manipulation methods in Python.  The content focuses on the `.upper()` and `.lower()` methods, which convert strings to uppercase and lowercase, respectively.  The examples show how to use these methods with string literals.  The `>>>` symbols indicate interactive Python code execution.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The examples are presented in a clear, concise manner, illustrating how to perform basic string formatting operations in Python.  The slide also includes a helpful hint for removing trailing newlines from strings, which is a common task when reading data from files.  There are no figures on this slide.




# Slide 36

**Defining Functions**

There is no header file, declaration of types to return, or types for arguments
Functions begin with "def"

```python
def pow(x, n=1):
    """x to the nth power"""  # documentation string, for help()
    if n <= 0:
        return 1
    else:
        half = pow(x, n // 2)
        two = half * half
        if (n % 2 is 1):
            return x * two  # return indicates the value to be sent back
        return two
```

`pow(2)`  # First line with less indentation is outside the function

Slides adapted from UPenn CIS530 python tutorial


**Description:**

This slide presents a Python function definition for calculating the power of a number.  The function `pow(x, n)` calculates x raised to the power of n.  The function is recursive, calling itself to compute the result efficiently.  The code is formatted to clearly show the function definition, including the docstring, conditional statements, and return values.  The example `pow(2)` demonstrates how to call the function.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The code example is presented in a way that clearly shows the structure of a Python function, including the docstring, conditional statements, and return statements.  The `>>>` symbols are not present, indicating that this is not an interactive Python session but rather a demonstration of the code's structure.  The function definition is complete, including the base case (n <= 0) and the recursive step.  The use of `n // 2` for integer division is also important.  The comment `# return indicates the value to be sent back` clarifies the purpose of the `return` statement.


# Slide 37

**Python and Types**

Python determines the data types of variable bindings in a program automatically: "Dynamic Typing"
But Python's not casual about types, it enforces the types of objects: "Strong Typing"
You can't just append an integer to a string. You must first convert the integer to a string itself.

x = "the answer is "  # Decides x is bound to a string.
y = 23                # Decides y is bound to an integer.
# print x + y         # Python will complain about this.
print(x + str(y))     
the answer is 23

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide discusses the concept of dynamic and strong typing in Python.  It explains that Python automatically determines variable types (dynamic typing), but it enforces the types of objects (strong typing).  The example demonstrates that you cannot directly concatenate a string and an integer; you must convert the integer to a string using `str()` before concatenation.  The code `print(x + str(y))` shows the correct way to combine the string "the answer is " and the integer 23.  The output "the answer is 23" is shown below the code, demonstrating the result of the correct operation.  The comments within the code explain the type binding decisions made by Python.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The examples are presented in a clear, concise manner, illustrating the importance of data type awareness in Python.




# Slide 38

**Python and Types**

The syntax for a function call is:

```python
>>> def myfun(x, y):
        return x * y
>>> myfun(3, 4)
12
```

Parameters in Python are "Call by Assignment."
Sometimes acts like "call by reference" (for mutable datatypes) and sometimes like "call by value" (for immutable datatypes).
All assignment in Python, including binding function parameters, uses reference semantics.
All functions in Python have a return value... even if no return line inside the code.
Functions without a return return the special value None.
None is a special constant in the language.
None is used like NULL, void, or nil in other languages.
None is also logically equivalent to False.
The interpreter doesn’t print None

Slides adapted from UPenn CIS530 python tutorial


**Description:**

This slide discusses function calls and parameter passing in Python.  It explains that Python uses "call by assignment" for parameters, which can sometimes behave like call by reference (for mutable objects) or call by value (for immutable objects).  The example `def myfun(x, y): return x * y` defines a simple function that multiplies two values.  The call `myfun(3, 4)` demonstrates how to execute the function.  The slide also explains that all functions in Python have a return value, even if no explicit `return` statement is present.  If no return statement is present, the function implicitly returns `None`.  The slide further explains the concept of `None` as a special constant in Python, analogous to `NULL`, `void`, or `nil` in other languages.  The final line notes that the Python interpreter does not print the value `None` if it's the result of a function call.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The examples are presented in a clear, concise manner, illustrating the behavior of function calls and parameter passing in Python.




# Slide 39

**Functions are first-class objects**

Functions can be used as any other data type. They can be:

* Arguments to functions
* Return values of functions
* Assigned to variables
* Parts of tuples, lists, etc

```python
>>> def myfun(x):
        return x * 3
>>> def applier(q, x):
        return q(x)
>>> applier(myfun, 7)
21
```

**Description:**

This slide demonstrates that functions in Python are first-class objects, meaning they can be treated like any other data type.  The example shows how a function (`myfun`) can be passed as an argument to another function (`applier`).  The function `applier` takes a function `q` and a value `x` as input, and it applies the function `q` to the value `x`.  In this case, `applier(myfun, 7)` calls `myfun` with the argument 7, resulting in the output 21.  The code is presented in a way that clearly shows the function definitions and the function call, illustrating the concept of functions as first-class objects.  The `>>>` symbols indicate interactive Python code execution.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.




# Slide 40

**Importing and Modules**

Use classes & functions defined in another file.

* A Python module is a single file with the same name (plus the .py extension)
* Like Java import

Where does Python look for module files?

* The list of directories where Python looks: sys.path

To add a directory of your own to this list, append it to this list.
sys.path.append('/my/new/path')

Slides adapted from UPenn CIS530 python tutorial


---

**Description:**

This slide discusses importing modules in Python.  It explains that a Python module is a single file containing functions and classes, similar to how modules work in Java.  The slide highlights the `sys.path` variable, which is a list of directories Python searches for modules.  The example `sys.path.append('/my/new/path')` shows how to add a custom directory to the search path.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The content is presented in a clear, concise manner, providing information on how to import modules from external files and how to modify the search path for those modules.  There are no figures on this slide.




# Slide 41

```markdown
Import
import somefile
# Everything in somefile.py can be referred to by:
somefile.className.method("abc")
somefile.myFunction(34)
from somefile import *
# Everything in somefile.py can be referred to by:
className.method("abc")
myFunction(34)
Some useful modules to import, included with Python:
Module: sys
- Lots of handy stuff.
Module: math
- Mathematical code.
Exponents; sqrt
Module: Random
- Random number code.
Randrange; Uniform; Choice; Shuffle
Slides adapted from UPenn CIS530 python tutorial
```

**Description:**

This slide discusses importing modules in Python, likely within the context of the Intro to Data Science course.  The slide shows examples of how to import functions and classes from a module named `somefile.py`.  The first example uses the `import somefile` statement, allowing access to the module's contents using the `somefile.` prefix (e.g., `somefile.className.method("abc")`).  The second example uses `from somefile import *`, which imports all names from the module without the module prefix.  The slide also provides a list of useful modules that are commonly imported in Python, including `sys`, `math`, and `random`.  The list includes specific functions within the `random` module, such as `randrange`, `uniform`, `choice`, and `shuffle`.  The footer indicates that the content is adapted from a Python tutorial from the University of Pennsylvania's CIS530 course.  There are no figures on this slide.  The text is presented in a clear and concise manner, providing information on importing modules and useful modules for the course.




# Slide 42

```markdown
It's all objects

Everything in Python is really an object.
We've seen hints of this already...
"hello".upper()
list3.append('a')
dict2.keys()
Programming in Python is typically done in an object oriented fashion.
Python doesn't use separate class interface definitions much. You just define the class
and then use it.

Slides adapted from UPenn CIS530 python tutorial
```

**Description:**

This slide, titled "It's all objects," discusses the object-oriented nature of Python.  It states that everything in Python is an object, and provides examples of object methods (`"hello".upper()`, `list3.append('a')`, `dict2.keys()`) to illustrate this point.  The slide also notes that Python's object-oriented programming style is typically done without explicit class interface definitions; instead, classes are defined and then used directly.  The footer indicates that the content is adapted from a Python tutorial from the University of Pennsylvania's CIS530 course.  There are no figures on this slide.  The text is presented in a clear and concise manner, providing a summary of Python's object-oriented features.




# Slide 43

Defining a class and methods
A class is a special data type which defines how to build a certain kind of object.
* The class also stores some data items that are shared by all the instances of this class.
* But no static variables!
Define a method in a class by including function definitions within the scope of the class block.
There must be a special first argument `self` in all of method definitions which gets bound to the calling instance.
* Self is like `this` in Java
* Self always refers to the current class instance
A constructor for a class is a method called `__init__` defined within the class.
Slides adapted from UPenn CIS530 python tutorial


**Description:**

This slide, titled "Defining a class and methods," explains the concept of classes and methods in Python.  It describes a class as a special data type that defines how to create objects of a particular kind.  The slide also notes that classes store data items shared by all instances of the class, but there are no static variables.  Crucially, it explains that methods within a class must include a special first argument named `self`, which refers to the current instance of the class.  This is analogous to the `this` keyword in Java.  The slide also mentions that a constructor for a class is a method called `__init__`.  The slide is part of a larger set of lecture notes on Python programming, likely within the context of an Intro to Data Science course.  The content is presented in a clear and concise manner, providing a fundamental understanding of classes and methods in Python.  There are no figures on this slide.




# Slide 44

```markdown
A simple class definition: student

class student:
   """A class representing a student."""
   def __init__(self,n,a):
       self.full_name = n
       self.age = a
   def get_age(self):
       return self.age
There is no “new” keyword as in Java.
Merely use the class name with () notation and assign the result to a variable.
__init__ serves as a constructor for the class. 
Example:
b = student("Bob", 21)
An __init__ method can take any number of arguments.
Like other functions & methods, arguments can be defined with default values, making them optional to the caller. 
But no real overloading
Slides adapted from UPenn CIS530 python tutorial
```

**Description:**

This slide provides a Python class definition for a `student` class.  The class is designed to represent student information, likely in the context of an Intro to Data Science course.  The `__init__` method is a constructor that initializes the `full_name` and `age` attributes of a `student` object.  The `get_age` method returns the student's age.  The example `b = student("Bob", 21)` demonstrates how to create an instance of the `student` class and assign it to the variable `b`.  The slide also explains that Python classes don't require a separate "new" keyword for object creation, and the `__init__` method acts as a constructor.  The slide is part of a larger set of lecture notes on object-oriented programming in Python, likely within the context of the Intro to Data Science course.  There are no figures on this slide.




# Slide 45

```markdown
Syntax for access
Although you must specify `self` explicitly when defining the method, you don't include it when calling the method.
Python passes it for you automatically.
Defining a method:
def set_age(self, num):
	self.age = num
Calling a method:
>>> x.set_age(23)
>>> f = student("Bob Smith", 23)
>>> f.full_name  # Access an attribute.
"Bob Smith"
>>> f.get_age()  # Access a method.
23
No public, private, protected, etc...
Slides adapted from UPenn CIS530 python tutorial
```

**Description:**

This slide demonstrates the syntax for accessing attributes and methods of a class in Python.  It shows how to define a method (`set_age`) that takes a `self` parameter, but when calling the method, `self` is not explicitly included.  The example shows how to create an instance of a `student` class (`f = student("Bob Smith", 23)`) and then access its attributes (`f.full_name`) and methods (`f.get_age()`).  The output shows the expected results of these operations.  The slide is part of a larger set of lecture notes on object-oriented programming in Python, likely within the context of an Intro to Data Science course.  The examples are presented in a clear, concise manner, illustrating the syntax for accessing class members.  The `>>>` symbols indicate interactive Python code execution.  The slide is focused on the syntax of accessing attributes and methods of a class in Python.  The example demonstrates how to call a method on an object, and how to access an attribute of an object.  The output shows the expected results of these operations.


# Slide 46

```markdown
File Processing with Python
The basics of reading:
fileptr = open('filename')
somestring = fileptr.read()
for line in fileptr:
	print(line)
fileptr.close()
Use libraries for easier parsing of file types
import csv                      #csv file reader
import sys
f = open(sys.argv[1], 'rt')
reader = csv.reader(f)
for row in reader:
	print(row)                   #['1', 'a', '3.5']
	print(float(row[2])+1)       #4.5
f.close()
Slides adapted from UPenn CIS530 python tutorial
```

**Description:**

This slide provides code examples for file processing in Python, likely within the context of an Intro to Data Science course.  The code demonstrates basic file reading using the `open()` function and a `for` loop.  It also shows how to use the `csv` module for reading CSV (Comma Separated Values) files.

* **Basic File Reading:** The first part of the code shows how to open a file (`fileptr = open('filename')`), read its entire content into a string (`somestring = fileptr.read()`), and then iterate through each line (`for line in fileptr: print(line)`).  Finally, the file is closed (`fileptr.close()`).

* **CSV File Reading:** The second part of the code imports the `csv` and `sys` modules.  It opens a CSV file specified as a command-line argument (`f = open(sys.argv[1], 'rt')`).  It then creates a `csv.reader` object (`reader = csv.reader(f)`) to parse the file.  The code iterates through each row (`for row in reader:`) in the CSV file.  Inside the loop, it prints the row (`print(row)`) and then converts the third element of the row to a float, adds 1 to it, and prints the result (`print(float(row[2])+1)`).  Finally, the file is closed (`f.close()`).

The comments `#['1', 'a', '3.5']` and `#4.5` are inline comments showing the expected output of the code when processing a sample CSV row.  The slide is focused on demonstrating how to read and process data from files, including CSV files, using Python.  The code examples are presented in a clear, concise manner, illustrating the use of Python's built-in functions and modules for file processing.


