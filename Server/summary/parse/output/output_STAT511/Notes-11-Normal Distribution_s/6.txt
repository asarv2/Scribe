# Slide 6

**Example 11.2 Revisited (cont.)**

Suppose X is the height of a randomly chosen woman. The distribution of heights in this population is given by X ~ N(65, 3.5<sup>2</sup>). What is the probability that a randomly selected adult female will be shorter than 68 inches?

I.  Write this in mathematical notation.
   P(X < 68) = ?

II. Calculate the z-score. This gives us the number of standard deviations x is from μ.
   z-score for 68 = (68 - 65) / 3.5 = 0.857 ≈ 0.86

III. Draw a picture: Line up the two distributions:
    X ~ N(65, 3.5<sup>2</sup>) or Z ~ N(0, 1<sup>2</sup>)

IV. P(X < x) = P(Z < z)
   P(X < 68) = P(Z < 0.86)

**Aside on what we did above:**

* We want to find the probability that a randomly selected individual has a characteristic.
* In this example, that characteristic is height is less than 68 inches.
* We know that this probability is the proportion of individuals in the population whose height is less than 68 inches.
* Because X = height of a randomly chosen female is a continuous random variable, we relate proportion to the area of part of the density curve for heights in this population.
* Finally, we make use of the fact that the areas are the same whether we use the density curve for the heights or the density curve for the z-scores of the weights.

**Using a Z table to find P(Z < 0.86):**

* We have reduced our problem to finding P(Z < 0.86).
* In general, to find P(Z < z), we look up the probability in a Z-table. This probability is called a cumulative probability Φ(z).
* Locate the z-score you calculated (0.86) on the left column and the top row of the table.
* Then read the corresponding cumulative probability from the body of the table.
* The cumulative probabilities shown give areas under the curve to the left of each z-score.


**Figure Description:**

* **Figure 1 (Embedded):** The figure shows two bell-shaped curves, both representing normal distributions. The curve on the left, labeled "X ~ N(65, 3.5<sup>2</sup>)", is centered around 65 on the x-axis. A vertical line extends from this curve to the x-axis at the value 68. A portion of the curve to the left of the 68 line is shaded. The curve on the right, labeled "Z ~ N(0, 1<sup>2</sup>)", is centered around 0 on the x-axis. A vertical line extends from this curve to the x-axis at the value 0.86. A portion of the curve to the left of the 0.86 line is shaded. The two curves are visually aligned to show the correspondence between the X and Z distributions. The shaded areas visually represent the probabilities being calculated.  The figure is embedded within the text, illustrating the concept of standardizing a normal distribution using z-scores.  The figure is crucial for understanding the relationship between the original variable (height) and the standardized variable (z-score).

* **Table A.3 (Embedded):** A portion of a table is shown, likely a standard normal distribution table (Z-table).  The table is organized with columns and rows, likely representing different z-scores and their corresponding cumulative probabilities.  The table is used to find the probability associated with the z-score of 0.86. The table is embedded within the text, providing the numerical data needed to calculate the probability.


**Description of the Slide:**

This slide continues the example from the previous slide, demonstrating how to find the probability of a randomly selected woman being shorter than 68 inches using the standard normal distribution. The figure is crucial for understanding the relationship between the original variable (height) and the standardized variable (z-score). The slide shows how to use a z-table to find the probability associated with the z-score. The example is part of a Statistical Methods course, illustrating the application of normal distributions and z-scores to real-world problems. The table provides the numerical data needed to calculate the probability.
