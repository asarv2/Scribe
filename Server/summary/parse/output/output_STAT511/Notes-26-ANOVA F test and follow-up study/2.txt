# Slide 2

In order to determine the rejection region or calculate p-value, we need to understand the sampling distribution of the F-statistic.

An F distribution arises in connection with a ratio in which there is one number of degrees of freedom (df) associated with the numerator and another number of degrees of freedom associated with the denominator.

Test statistic: Under Null hypothesis H<sub>0</sub>: μ<sub>1</sub> =  ⋯ = μ<sub>𝐼</sub>, the test statistic

𝐹𝑇𝑆= 𝑆𝑆𝑇/(𝐼−1)
𝑆𝑆𝐸(𝑁−𝐼) = 𝑀𝑆𝑇
𝑀𝑆𝐸

follows an F distribution with dfs 𝐼−1 and 𝑁−𝐼.

P value = 𝑃( 𝐹≥𝐹𝑇𝑆 )

Rejection region: { 𝐹> 𝐹<sub>1−α,𝐼−1,𝑁−𝐼</sub> }

Example 26.1 revisit

With μ<sub>𝑖</sub> denoting the true average compression strength for boxes of type 𝑖 (𝑖= 1, 2, 3, 4),

1. H<sub>0</sub>:μ<sub>1</sub> = μ<sub>2</sub> = μ<sub>3</sub> = μ<sub>4</sub>
2. H<sub>𝑎</sub>: at least two of the μ<sub>𝑖</sub>’𝑠 are different.
3. Test statistic value is 𝑓= 25.09.
4. Rejection region is { 𝐹> 𝐹<sub>0.95,3,20</sub> = 3.10 }
5. Since 25.09 > 3.10, H<sub>0</sub> is resoundingly rejected at significance level 0.05. True average compression strength does appear to depend on box type.


Unequal sample size ANOVA: when cell sizes are different, the DFs, SST, SSTr and SSE are given by

𝐷𝐹𝑇= 𝑁−1 = (∑
𝑛𝑖
𝐼
𝑖=1
) −1
𝐷𝐹𝑇𝑟= 𝐼−1
𝐷𝐹𝐸= 𝑁−𝐼

𝐷𝐹𝑇= 𝐷𝐹𝑇𝑟+ 𝐷𝐹𝐸


**Figure Description:**

A graph of an F-distribution is present in the lower right quadrant of the slide. The x-axis represents the F-statistic values, and the y-axis represents the probability density.  Several different F-distributions are shown, each with different degrees of freedom (df). The curves are labeled with their respective degrees of freedom (e.g., df1=2, df2=10).  A shaded area is present under the curve, representing the rejection region for a given significance level (α).  The shaded area is crucial for determining the critical value of the F-statistic, which is used to make a decision about the null hypothesis. The figure is placed below the text explaining the F-distribution and its use in hypothesis testing.  The figure is there to illustrate the concept of the F-distribution and how it is used to determine the rejection region in an ANOVA test.  It visually demonstrates the relationship between the F-statistic, degrees of freedom, and the significance level.


**Additional Notes:**

The slide is part of a lecture on Statistical Methods, specifically focusing on Analysis of Variance (ANOVA) for comparing means of more than two groups. The example (Example 26.1 revisit) is a continuation of a previous example, likely involving data on compression strength of different types of boxes. The slide introduces the concept of the F-distribution, which is essential for ANOVA tests. The example demonstrates how to use the F-distribution to test the hypothesis that the average compression strength is the same across different box types. The unequal sample size ANOVA section provides a formula for calculating degrees of freedom when the sample sizes are not equal.
