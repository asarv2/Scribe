# Slide 5

**Statistical Methods**

Test at significance level α = 0.05. Critical value 𝐹<sub>0.05,2,12</sub> = 3.89.

We will reject H<sub>0</sub> if 𝑓<sub>𝑇𝑆</sub> > 3.89, otherwise will not. Observe 𝑓<sub>𝑇𝑆</sub> = 8.358 > 𝐹<sub>0.05, 2,12</sub>. Reject H<sub>0</sub>.

This study provides evidence that Paxil intake affects serotonin levels in young men.


What to check before using an ANOVA to test the hypotheses?


Assumptions

*   𝑋<sub>𝑖𝑗</sub>'s within any sample (or group) are independent; different samples (or groups) are independent;
*   All population distributions are normal, with population mean μ<sub>𝑖</sub> for the 𝑖𝑡ℎ group;
    * We have a normal distribution requirement for the residuals (difference between the data values and the group mean). In other words, the QQ plot for all data value 𝑋<sub>𝑖𝑗</sub> − 𝑋̅<sub>𝑖⋅</sub>
*   The variance σ<sup>2</sup> of different groups are the same.
    * Usually, we want the largest sample SD can be no greater than twice the size of the smallest sample SD.


Example 26.4 Smoke status example - Is there is an association between smoking and GPR.

To test this, a researcher used data from an anonymous survey taken of STAT 3xx students in Fall 2002. Assume that this data set is equivalent to a random sample. Each student in the survey was asked which category they fell into: never smoked, former smoker, currently smokes. Each student was also asked their GPR.

Explanatory: Smoking status with 3 levels

Response: GPR

One way Analysis of GPR By smoking habit


**(Figure Description):**

Three figures are present on the slide, arranged horizontally.

1.  **Boxplot (left):** A boxplot displays the distribution of GPR (likely a numerical value) across the three smoking categories (never smoked, former smoker, currently smokes).  The boxes represent the interquartile range (IQR), the lines within the boxes represent the median, and the whiskers extend to the minimum and maximum values within 1.5 times the IQR.  Outliers are plotted as individual points.  The boxplots are side-by-side, allowing for a visual comparison of the GPR distributions across the different smoking categories.  This figure is used to visually assess potential differences in GPR among the smoking groups.

2.  **Histogram (middle):** A histogram displays the distribution of GPR values for each smoking category.  The horizontal axis represents the GPR values, and the vertical axis represents the frequency or count of observations within each GPR range.  The histogram is divided into bins, and the height of each bar corresponds to the number of observations falling within that bin.  The histograms are side-by-side, allowing for a visual comparison of the GPR distributions across the different smoking categories.  This figure is used to visually assess potential differences in GPR among the smoking groups.

3.  **Normal Q-Q Plot (right):** A normal Q-Q plot is a scatter plot that plots the quantiles of a sample against the quantiles of a standard normal distribution.  If the data are normally distributed, the points on the plot will fall approximately along a straight line.  Deviations from a straight line indicate deviations from normality.  The plot is used to assess the normality assumption for the data.  The plot is placed on the right side of the slide, likely to be used in conjunction with the boxplots and histograms to assess the assumptions for the ANOVA test.


**Slide Description:**

This slide introduces the assumptions of ANOVA and provides an example of how to check these assumptions.  The example focuses on smoking status and GPR, and the figures are used to visually assess the normality and equal variance assumptions.  The slide is part of a lecture on Statistical Methods, specifically focusing on ANOVA.
