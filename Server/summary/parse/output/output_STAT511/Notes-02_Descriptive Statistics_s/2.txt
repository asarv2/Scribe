# Slide 2

**Describing a Single Numerical Variable**

Example 2.1 revisited: the data on 20 popular brands of cereals. We are now interested in summarizing the sugar variable values and understanding the distribution of these values.

The Distribution of numerical data: The distribution of a variable tells us
(1) what values the variable takes
(2) how often it takes these values.

**Graphical Descriptions**

**Stem-and-Leaf Displays:** (usually for rounded values with at least two digits)

Given data set (0, 10, 12, 23, 45, 13, 54, 5, 15, 34, 63, 24, 64, 23),
the stem-and-leaf plot is:

Stem (leading digit(s)) | Leaf (trailing digit(s))
------- | --------
0 | 5
1 | 0 2 3 5
2 | 3 3 4
3 | 4
4 | 5
5 | 4 5
6 | 3 4

**R code:**
```R
x <- c(0, 10, 12, 23, 45, 13, 54, 5, 15, 34, 63, 24, 64, 23)
stem(x, scale = 2)
```

**Stacked Dot Plot:** the stacked dot plot can be considered as a rotated stem-and-leaf plot.

Given the same data set,
the stacked dot plot is:

[Image of a stacked dot plot]

**R code:**
```R
foo <- function(x) {
  round(x + 5, -1)
}

x <- c(0, 10, 12, 23, 45, 13, 54, 5, 15, 34, 63, 24, 64, 23)
stripchart(foo(x), method = "stack", offset = .5, at = 0, pch = 19, col = "steelblue")
```


**Description of the Slide:**

This slide, part of a Statistical Methods lecture, continues the discussion from the previous slide by focusing on describing a single numerical variable (sugar content in the cereal example). It explains that understanding the distribution of a numerical variable involves identifying the values it takes and how frequently each value occurs.  The slide introduces two graphical methods for visualizing this distribution: stem-and-leaf displays and stacked dot plots.  A stem-and-leaf display is shown for a sample data set, and the corresponding R code is provided.  A stacked dot plot is also shown, along with its corresponding R code.  The R code demonstrates how to create these plots using the R programming language, which is often used in statistical analysis. The plots help to visualize the distribution of the sugar content in the cereal data, showing the frequency of different sugar values.
