# Slide 2

**Parameter estimation**

Question: how to estimate the three parameters in the simple regression model given the data set?

Assume the linear model and sample data consisting of *n* observed pairs (𝑥₁, 𝑦₁), …, (𝑥ₙ, 𝑦ₙ). We want to fit a line to the data, which also provides estimates of the model parameters of the true regression line. Denote the fitted line as 

𝑦̂ = 𝑏₀ + 𝑏₁𝑥.

Maximum likelihood estimators, moment estimators are both possible, here we proposed the least square estimator. The least square estimator finds the straight line which are closest to the data points.


Principle of Least Squares: Define the sum of squared error,

𝑓(𝑏₀, 𝑏₁) = ∑[𝑦ᵢ−(𝑏₀ + 𝑏₁𝑥ᵢ)]²
       𝑖=1
       𝑛


The point estimates of 𝛽₀ and 𝛽₁, denoted by 𝑏̂₀ and 𝑏̂₁, and called the least squares estimates, are those values that minimize 𝑓(𝑏₀, 𝑏₁).

The minimizing values of 𝑏₀ and 𝑏₁ are found by taking partial derivatives of 𝑓(𝑏₀, 𝑏₁) with respect to both 𝑏₀ and 𝑏₁, equating them both to zero, and solving the equations. The solution of the above optimization is

𝑏₁ = 𝛽̂₁ = 
∑(𝑥ᵢ−𝑥̅)(𝑦ᵢ−𝑦̅)
∑(𝑥ᵢ−𝑥̅)²
=
∑𝑥ᵢ𝑦ᵢ−(∑𝑥ᵢ)(∑𝑦ᵢ)/𝑛
∑𝑥ᵢ² −(∑𝑥ᵢ)²/𝑛
=: 𝑆𝑥𝑦
𝑆𝑥𝑥


𝑏₀ = 𝛽̂₀=
∑𝑦ᵢ−𝛽̂₁ ∑𝑥ᵢ
𝑛
= 𝑦̅ −𝛽̂₁𝑥̅  


**(Figure Description):**

A graph is shown, labeled "(b)".  It's a scatter plot with a line overlaid. The scatter plot shows a set of points (x, y) that roughly follow a linear trend. The line represents a possible regression line.  The line is not perfectly aligned with the data points, but it's positioned to minimize the vertical distances between the data points and the line.  The line is labeled "Line: 𝑦̂ = 𝑏₀ + 𝑏₁𝑥".  The graph is positioned above the text "Parameter estimation" and is intended to visually illustrate the concept of fitting a line to data points. The figure helps to visualize the idea of minimizing the sum of squared errors to find the best-fitting line.  The x and y axes are labeled, and the points are labeled with subscripts (x₁, y₁), etc.  The figure is relevant to the discussion of parameter estimation in linear regression, showing how the least squares method finds the line that best fits the data.


**Description of the Slide:**

This slide introduces the concept of parameter estimation in simple linear regression. It explains how to estimate the parameters (intercept and slope) of the regression line using the least squares method. The slide also includes a graph illustrating the idea of fitting a line to data points and minimizing the sum of squared errors. The slide is part of a lecture on statistical methods, specifically focusing on regression analysis.
