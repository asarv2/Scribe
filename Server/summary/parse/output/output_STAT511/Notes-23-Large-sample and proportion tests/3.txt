# 3

**STAT 511 Course Notes – set 23 (W12)**

**Yuan Qu**

1. **Hypotheses:**

H<sub>0</sub>: μ = 30
versus
H<sub>a</sub>: μ < 30 (so the pavement will not be used unless the null hypothesis is rejected).

2. **For a sample of 52 data points and 𝑠= 12.2647,**

TS = (𝑋̅ − μ<sub>0</sub>) / (𝑠/√𝑛) = (28.76 − 30) / (12.2647/√52) = −1.24 / 1.701 = −0.73

3. **We use the sample T test statistic, the p-value = P(T < −0.73) = 0.2344.**
We use the large sample Z test statistic, the p-value = P(Z < −0.73) = 0.2327.

4. **(1) Since p-value > 0.05, we do not reject H<sub>0</sub>.** (2) There’s no evidence that the pavement should be used.


**Case 4: Hypothesis tests about the one-sample population proportion**

Let X<sub>1</sub>, …, X<sub>n</sub> be random sample from Bernoulli (p) distribution, with unknown p. The estimator p̂ = (∑X<sub>i</sub>)/n is unbiased, E(p̂) = p and its standard deviation is σ<sub>p̂</sub> = √p(1 −p)/n.

When H<sub>0</sub>: p = p<sub>0</sub> is true,
E(p̂) = p<sub>0</sub> and σ<sub>p̂</sub> = √p<sub>0</sub>(1 −p<sub>0</sub>)/n.

We consider the following hypothesis test for the parameter p:

H<sub>0</sub>: p = p<sub>0</sub>  vs  H<sub>1</sub>: p > p<sub>0</sub> (or ≠, or <)


**Large sample approximated test:**

A large-sample tests concerning population proportion p is a special case of the more general large-
sample procedure. CLT guarantees, that (𝑋̅−𝐸(𝑋))/√𝑉𝑎𝑟(𝑋) has an approximation N(0,1) distribution, when
sample size is large.

Apply this to binomial distribution, X̅ = (1/n)∑X<sub>i</sub> = Y/n = p̂.

Under null hypothesis H<sub>0</sub>: p = p<sub>0</sub>, E(X) = p<sub>0</sub>, Var(X) = p<sub>0</sub>(1 −p<sub>0</sub>). The standardized variable


---

**Slide Description:**

This slide continues the discussion of hypothesis testing, focusing on the case of a single population proportion (Case 4).  It introduces the Bernoulli distribution and the estimator p̂.  The slide clearly states the null and alternative hypotheses for testing a population proportion.  It also presents the large sample test statistic, which is crucial for testing population proportions when the sample size is large.  The slide includes a worked example (Example 23.1) from the previous slide, where the sample mean DCP value is compared to a design specification of 30.  The slide also mentions the use of a T-test and a Z-test statistic, and the p-values calculated for each test.  The slide concludes by stating the decision rule based on the p-value and the conclusion drawn from the test.  The slide number (3 of 9) and the course name (STAT 511 Course Notes) are clearly visible at the top.  The author's name (Yuan Qu) is also present.  The title "Hypothesis tests about the one-sample population proportion" clearly indicates the topic of the lecture.  The use of bullet points and clear headings makes the information easy to follow.  The slide also includes formulas for calculating the test statistic and p-value.
