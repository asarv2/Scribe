Slide 6

\textbf{Primal Network Simplex}

\textit{Modification of (spanning) tree and variables:}

(1) choose entering and leaving arcs

(2) update primal and dual flows.

\textit{Choose arc with $z_{ij} < 0$ as entering arc}

\textit{Find cycle including entering arc and leaving arc}

\textit{Update primal and dual flows}


\textbf{Description:}

This slide describes the steps involved in the Network Simplex Method for optimizing a network flow problem.  It shows a graphical representation of a network with nodes and edges, where the edges are labeled with costs or capacities.  The text describes the algorithm's iterative process of choosing an entering arc (an edge with a negative dual variable $z_{ij}$) and a leaving arc to form a cycle.  The algorithm then updates the primal and dual flows along the edges within the cycle to improve the solution.  The diagram illustrates the cycle formed by the entering and leaving arcs, visually representing the changes in the spanning tree.  This slide likely explains the core iterative step of the network simplex algorithm, showing how the algorithm modifies the spanning tree to find the optimal solution.
