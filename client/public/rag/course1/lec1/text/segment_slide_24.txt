 on programs manipulate these data objects. All objects in Python are going to have a type. And the type is going to tell Python the kinds of operations that you can do on these objects. So if an object is the number 5, for example, you can add the number to another number, subtract the number, take it to the power of something, and so on. As a more general example, for example, I am a human, so that's my type. And I can walk, speaking, lish, et cetera. Chubaka is going to be a type wookie, right? So you can walk, do that sound that I can't do, right? So you can do that, but I can't. I'm not even going to try. And so on. OK. So once you have these Python objects, everything is an object in Python. There are actually two types of objects. One are scalar objects. So that means these are very basic objects in Python, from which everything can be made. So these are scalar objects. They can't be subdivided. The other type of object is a non-scalar object. And these are objects that have some internal structure. So for example, the number 5 is a scalar object, because it can't be subdivided. But a list of numbers, for example, 5, 6, 7, 8, is going to be a non-scalar object, because you can subdivided. You can subdivided it into, you can find parts to it. It's made up of a sequence of numbers, right? OK. So here's the list of all of the squares.