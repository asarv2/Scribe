 scalar objects in Python. We have integers. For example, all of the whole numbers. Floats, which are all of the decimal real numbers, anything with a decimal. Bulls are bullions. There's only two values to the bullions. That's true and false. Note the capitalization, capital T and capital F. And this other thing called none type. It's special. It has only one value called none. And it represents the absence of a type. And it sometimes comes in handy for some programs. If you want to find the type of an object, you can use this special command called type. And then in the parentheses, you put down what you want to find the type of. OK. So you can write into the shell type of five. And the shell will tell you that's an integer. If you happen to want to convert between two different types, Python allows you to do that. And to do that, you use this.