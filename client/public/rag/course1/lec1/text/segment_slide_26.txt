 You put the type that you want to convert to right before the object that you want to convert to. So float3 will convert the integer3 to the float3.0. And similarly you can convert any float into an integer and converting to an integer just truncates. So it just takes away the decimal and whatever is after it, it does not round and keeps just the integer part. Okay, so for this slide I'm going to talk about it but if you'd like, go to if you have the slides up.